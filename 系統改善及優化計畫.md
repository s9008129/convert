# MeetingScribe 系統改善及優化計畫

> **版本**：v1.0（2026-07-07）
> **依據**：本專案 codebase 深度分析（v4.1，逐檔逐行證據）＋ 台灣政府機關「國語＋台語＋英語夾雜」會議轉錄最佳實務深度研究（含學術文獻與司法院等實際導入案例）。
> **用途**：作為後續改善的**唯一追蹤清單**。每項完成後勾選 checkbox 並在 PR 中引用項目編號（如 `P0-1`）。

---

## 目錄

1. [現況診斷總結](#一現況診斷總結)
2. [研究結論摘要（最佳實務）](#二研究結論摘要最佳實務)
3. [改善追蹤清單（依優先級）](#三改善追蹤清單依優先級)
   - [P0 嚴重缺陷修復（立即）](#p0嚴重缺陷修復立即)
   - [P1 核心品質建設（1-2 個迭代）](#p1核心品質建設1-2-個迭代)
   - [P2 效能與部署優化（2-3 個迭代）](#p2效能與部署優化2-3-個迭代)
   - [P3 長期強化（持續）](#p3長期強化持續)
4. [驗收指標](#四驗收指標)
5. [三平台部署守則](#五三平台部署守則)

---

## 一、現況診斷總結

本專案（v4.1）架構方向正確——「萃取優先三階段＋自我校驗」設計理念符合防杜撰最佳實務，但深度分析發現 **24 項**邏輯錯誤、盲點與衝突，其中多項直接抵銷了既有設計的效果：

### 最關鍵的六個問題

| # | 問題 | 證據 | 影響 |
|---|---|---|---|
| 1 | **設定黑洞**：後端不讀 `config.yaml` 的 whisper/VAD/ollama 區塊，只讀環境變數（`backend/core/config.py:224` 的 `env_file=".env"`） | `platform_config.get_global_config()` 僅被用於 `llm.provider`、`llm.lmstudio.*` | 使用者調 `config.yaml` 的 beam_size / VAD / num_ctx **對後端零效果**，且 `docker-compose.override.yml:49` 還掛載它造成假象 |
| 2 | **生產 ASR 路徑品質缺陷**：預設 transformers（Breeze-ASR-26）路徑為固定 30 秒硬切、無 VAD、無重疊（`transcription.py:321`）；VAD 參數只在 faster_whisper 回滾路徑生效（`transcription.py:277`） | `for start_index in range(0, len(audio_array), chunk_samples)` | 每 30 秒可能切斷字詞造成丟字/重複；config 中全部 VAD 調校無效 |
| 3 | **initial_prompt 永不生效**：`transcription.py:268` 條件為 `WHISPER_LANGUAGE != "auto"`，而預設即 `auto` | `if settings.ASR_INITIAL_PROMPT and settings.WHISPER_LANGUAGE != "auto"` | 繁中偏置提示詞形同虛設，簡體漂移只能靠下游弱偵測補救 |
| 4 | **語意校正機制完全缺席**：全系統無同音字/專有名詞校正；System Prompt 又（正確地）禁止 LLM 臆測 | 第 4 節分析：僅有約 45 字的簡體硬編碼偵測與英文行刪除 | ASR 聽錯的機關名、職稱、公文用語（如「陳核→進成核」）**原樣進入正式會議紀錄** |
| 5 | **兩套 compose 行為分歧**：標準版 `faster_whisper`+`Breeze-ASR-25`+`zh`（`docker-compose.yml:79-81`）vs GPU 版 `transformers`+`Breeze-ASR-26`+`auto`（`docker-compose-windows-gpu.yml:85-88`） | 兩檔環境變數直接對照 | 部署用哪個檔案決定完全不同的 ASR 行為與逐字稿快取簽章 |
| 6 | **摘要失敗靜默降級**：摘要失敗僅 warning，直接以逐字稿冒充會議紀錄並標記 COMPLETED（`task_processor.py:176-180`、`:304-306`） | `summary=None → _format_result 用逐字稿` | 使用者拿到「逐字稿偽裝的會議紀錄」而不自知——政府文書場景不可接受 |

### 其他重要問題（節錄）

- **Ollama `keep_alive="0"`**（`summarization.py:852`）：三階段流程對長逐字稿呼叫十數次，每次卸載再重載 20GB 模型，效能最差的正是最需要效能的長音檔場景。
- **最終生成 token 預算漏算**：`notes_merge_budget` 以短 merge prompt 估算，但最終階段使用約 1,200 token 的公務紀錄 System Prompt（`summarization.py:236,252-258,748-753`），輸入可能超過 `8192-3072≈5120` 被 Ollama 靜默截斷。
- **假並行**：僅單一消費者協程（`task_processor.py:68-83`），`MAX_CONCURRENT_TASKS=2` 無效。
- **驗證脆弱＋二次改寫**：驗證用精確字串比對（含雙空格 `主  席：`）；驗證通過後又被 `_remove_english_segments`/`_ensure_structure` 改寫且不再驗證（`task_processor.py:296-323`）。
- **Mac 名實不符**：宣稱 MLX/MPS 加速，實際 `transcription.py:96-99` 遇 MPS 直接降級 CPU；後端無 MLX 實作；`config.macos.yaml:` 內含硬編碼個人絕對路徑。
- **System Prompt 三處重複**：`backend/core/prompts.py`、`backend/core/config.py:188`、`config.yaml:83-133` 需手動同步。
- **版本號散落**：VERSION=4.1，但程式註解混雜 v2.1/2.2/3.5.4/4.0/4.2.1。
- **模型選型待驗證**：Breeze-ASR-26 是**台語專用**模型（見下節），非 ASR-25 升級版；把它當純國語會議唯一主力需要實測背書。

---

## 二、研究結論摘要（最佳實務）

> 完整研究含出處連結，此處摘錄直接影響本計畫的結論。

### ASR 模型（台灣華語／台語／中英夾雜）

- **Breeze-ASR-25**（Whisper large-v2 微調）是「台灣華語＋中英夾雜」最佳開源模型：CommonVoice16 zh-TW CER **7.97**、ASCEND 中英夾雜 MER **16.38**（large-v3 為 25.13），原生輸出繁體台灣用語。**但台語 CER 高達 49.99%，即不支援台語。**
- **Breeze-ASR-26 是台語專用模型**（聽台語→直接輸出華語漢字），Taigi Benchmark 平均 CER **30.13%**（全場最佳，勝過雅婷 32.11%）。訓練資料為約 1 萬小時**合成**台語語音，對真實口音/專業術語的表現需實測。
- **台語 CER 約 30% 是現實天花板**（司法院系統台語也僅約 85% 正確率）——台語段落必須配置人工校對，這是固定成本而非可被模型消除的問題。
- 數位發展部官方開源 **Taiwan-Tongues-ASR-CE**（MIT）支援國/台/客/英混講，提供 CT2 格式，合規敘事佳但無官方 CER，值得平行實測。

### ASR 參數（faster-whisper 實證參數組）

`hotwords`（每個 30 秒窗都注入、≤223 tokens，放專有名詞）與 `initial_prompt`（只影響第一窗，放風格/繁體引導）分工使用；`condition_on_previous_text=False` 是長檔防重複迴圈第一要務；`no_repeat_ngram_size=3`＋`repetition_penalty=1.1` 是中文重複字幻覺特效藥；`compression_ratio_threshold` 中文建議收緊至 2.2；VAD `min_silence_duration_ms` 降至 500；**不要預設降噪**（DeepFilterNet 實測反而使 WER 劣化約 20%）。

### 語意校正機制（LLM 後校正，GER 學術系列）

- 中文 ASR 錯誤以**同音替換為壓倒性主體**（替換錯誤約為刪除＋插入的 20 倍）→ 拼音/注音輔助校正特別有效（ChineseHP、PY-GEC）；命名實體用詞彙表檢索校正（DANCER：實體 CER 相對降 46%）。
- **naive 全文 LLM 校正可能劣於不校正**（過度改寫有量化實證）。對策：**選擇性校正**（只送 `avg_logprob` 低信心段，Confidence-Guided EC 再降 10% WER）＋**同音驗證閘門**（LLM 的每個替換以注音比對，非同音近音一律退回）。
- 詞彙表 ≤200 條全量注入 prompt；更大詞庫用注音 n-gram 模糊檢索 top-10~30 注入（RAG-GEC：CER 降 6.22%）。

### 說話者分離

pyannote **community-1**（pyannote.audio 4.0）為開源首選（AliMeeting 遠場 DER 20.3%），務必給 `min/max_speakers`、用 exclusive 模式；完全封閉內網可用 sherpa-onnx（45MB、CPU 可跑、免 token）。分軌錄音直接分軌辨識、跳過 diarization。

### 本地 LLM（24GB VRAM）

- **Gemma 4 已發布**（2026-04，Apache 2.0）：`26B-A4B`（MoE，Q4 約 15GB，速度接近 8B）與 `31B` dense（QAT 約 17-18GB）。**QAT 量化版（2026-06）是 24GB 卡首選**。
- **Ollama 預設 context 短，忘記調 `num_ctx` 靜默截斷長逐字稿是最常見品質事故**。建議 `num_ctx=32768`＋`OLLAMA_FLASH_ATTENTION=1`＋`OLLAMA_KV_CACHE_TYPE=q8_0`。
- 量化底線 Q4（優先官方 QAT），勿為長 context 降 Q3/IQ2（非拉丁文字受害更大）。
- 長逐字稿採 **Map-Reduce**（分段抽取結構化決議/待辦→彙整去重）比一次餵入 128K 穩（lost-in-the-middle 實證）——本專案三階段架構方向正確，需修預算與格式問題。

### 政府機關合規

- 行政院生成式 AI 參考指引：機密會議須**封閉式地端**；**AI 產出須由業務人員最終判斷負責**→人工校對＋核章節點不可省，系統定位為「產稿工具」。
- 文書處理手冊體例：名詞「紀錄」動詞「記錄」；出席/列席嚴格區分；**逐字稿與會議紀錄是兩套文件，應分層產出**。
- 對標：司法院系統國語 92%、台語 85% 正確率是政府場景現實天花板；中研院 **TranscriptHub**（MIT 開源，WhisperX 核心）是最直接可參考的政府級實作。

---

## 三、改善追蹤清單（依優先級）

> 標記說明：`[ASR]` 轉錄品質、`[LLM]` 會議紀錄品質、`[Prompt]` 提示工程、`[Fix]` 邏輯錯誤修復、`[Perf]` 效能、`[Deploy]` 部署、`[FE]` 前端、`[Test]` 測試。

### P0｜嚴重缺陷修復（立即）

> 原則：先讓「既有設計宣稱的行為」成真，再談新能力。

- [ ] **P0-1 `[Fix][ASR]` 修復 initial_prompt 在 `language=auto` 下永不套用**
  `backend/services/transcription.py:268`。改為：無論語言模式皆傳入 initial_prompt（transformers 路徑經 `prompt_ids`、faster_whisper 路徑經參數）；並依研究改寫 prompt 內容為「以下是台灣的會議內容，請使用繁體中文與全形標點符號。」以強化繁體/標點誘導。
- [ ] **P0-2 `[Fix][ASR]` 修復 transformers 生產路徑的 30 秒硬切**
  `transcription.py:321`。改用 HF pipeline 原生 `chunk_length_s=30, stride_length_s=(5,5)` 重疊解碼，或前置 Silero VAD 切段（沿用既有 VAD 設定）再逐段辨識，消除切字斷詞。同步讓 config 的 VAD 參數在 transformers 路徑實際生效，或在文件明示其適用範圍。
- [ ] **P0-3 `[Fix]` 消除設定黑洞：單一設定來源**
  決策：後端一律以 `backend/core/config.py`（環境變數）為準，`config.yaml` 僅供 CLI——則必須：(a) 在 `config.yaml` 頂部加大字警告與對照表；(b) 從 `docker-compose.override.yml:49` 移除 config.yaml 掛載或改為真的被讀取；(c) 後端啟動時 log 實際生效的 ASR/LLM 關鍵參數，杜絕「改了沒生效」的除錯黑洞。
- [ ] **P0-4 `[Fix]` 統一兩套 compose 的 ASR 設定**
  `docker-compose.yml:79-81` 與 `docker-compose-windows-gpu.yml:85-88` 收斂為同一組預設（模型/後端/語言），差異只允許出現在裝置層（CPU/GPU）。同步修正 `Dockerfile.gpu:102`、`docker-compose-windows-gpu.yml:10` 中 ASR-25/26 混寫的註解。
- [ ] **P0-5 `[Fix][LLM]` 摘要失敗不得偽裝成功**
  `task_processor.py:176-180,304-306`。摘要失敗時任務標記為 `COMPLETED_WITH_WARNING`（或新增狀態），輸出檔案首行加顯著警告區塊「⚠️ 本檔僅為逐字稿，會議紀錄生成失敗」，前端以明顯樣式呈現並提供重試按鈕。
- [ ] **P0-6 `[Fix][LLM]` 修正最終生成階段 token 預算**
  `summarization.py:236-258,748-753`。`notes_merge_budget` 必須以「最終階段實際 System Prompt（約 1,200 tokens）＋模板」計算保留量，確保 `輸入+輸出 ≤ num_ctx`，杜絕 Ollama 靜默截斷。
- [ ] **P0-7 `[Perf][LLM]` 移除 `keep_alive="0"`**
  `summarization.py:852`。三階段管線期間改 `keep_alive="10m"`（或管線開始時設定、結束時歸零），避免每階段重載 20GB 模型。RTX 4090 與 M4 Pro 記憶體皆足以常駐。
- [ ] **P0-8 `[Fix]` 版本號單一來源**
  以 `VERSION` 檔為唯一來源，清理 `backend/main.py:6`（v3.5.4）、`schemas.py:100`（2.1.0）、`config.py:2`、`requirements.txt:3`、`config.macos.yaml:4`、compose labels 等陳舊版號；健康檢查回傳值一律讀 VERSION。
- [ ] **P0-9 `[Fix]` `.gitignore` 補齊大型二進位排除**
  `Faster-Whisper-XXL/`、`*.7z`、`input/`、`output/` 加入 `.gitignore`，避免 1.4GB 檔案誤入版控（本計畫提交時一併完成）。

### P1｜核心品質建設（1-2 個迭代）

> 主軸：建立目前完全缺席的「語意校正機制」＋讓 ASR 模型選型有數據依據。

- [ ] **P1-1 `[ASR]` 建立機關詞彙表（hotwords）機制** ⭐ 語意校正第一層
  新增 `data/glossary/`：機關簡稱（國發會、工程會、主計總處…）、職稱（簡任/薦任/委任、專門委員、參事…）、公文動詞（函頒、陳核、簽辦、奉核、備查…）、常用英文專名。faster_whisper 路徑經 `hotwords` 參數每窗注入（≤223 tokens）；transformers 路徑注入 prompt_ids。前端支援上傳「本場會議詞彙表」（與會者名單、案名）。
- [ ] **P1-2 `[ASR]` 確定性後處理層** ⭐ 語意校正第二層
  逐字稿完成後依序執行：(a) OpenCC `s2twp` 統一繁體（取代目前僅 45 字的硬編碼偵測，`summarization.py:415`）；(b) 幻覺黑名單正則（「請訂閱」「謝謝觀看」「字幕由…提供」）＋連續重複句去重；(c) 公文用字白名單替換（高頻固定錯誤如「紀錄/記錄」誤用）。
- [ ] **P1-3 `[LLM]` 選擇性 LLM 校正模組（新服務 `correction.py`）** ⭐ 語意校正第三層
  依 Confidence-Guided EC 實證設計：
  - 只校正 `avg_logprob` 低於門檻或詞彙表模糊命中（注音 n-gram）的段落，**不做全文校正**（naive 校正有劣化實證）；
  - 分段 300-500 字、前後帶 10-15% 唯讀上下文、`temperature=0`；
  - Prompt 採研究提供之骨架：「只修正明顯語音辨識錯字（同音/近音）、逐字忠於原文、無法確定一律保留、整段無錯原樣輸出」＋詞彙表分類注入＋3-5 個同音錯字範例＋1 個「無錯誤原樣輸出」負例＋輸出修改對照表；
  - 此模組在「逐字稿→會議紀錄」之間執行，與既有三階段管線解耦，可獨立開關（`ENABLE_TRANSCRIPT_CORRECTION`）。
- [ ] **P1-4 `[LLM]` 同音驗證閘門** ⭐ 語意校正第四層（防過度改寫）
  對 P1-3 的每個替換做字元 diff＋注音/拼音比對（可用 `pypinyin`/`dragonmapper`）：非同音近音的改動一律退回原文；單段改動 >10% 字元則整段放棄校正。記錄修改對照表供人工複核。
- [ ] **P1-5 `[ASR]` ASR 模型選型實測（ASR-25 vs ASR-26 vs Taiwan-Tongues-ASR-CE）**
  以機關**真實會議錄音** 30-60 分鐘建立黃金標準測試集（人工逐字稿），實測三模型 CER（純國語段、中英夾雜段、台語段分開計分）。決策依據：ASR-26 為台語專用（合成語料訓練），純國語表現未經驗證；ASR-25 國語最強但台語 CER 約 50%。**依實測結果決定：單一模型 or 語言偵測分流雙模型。**沿用既有 `data/validation/` 基礎擴充。
- [ ] **P1-6 `[ASR]` faster_whisper 路徑套用實證參數組**
  `condition_on_previous_text=False`、`no_repeat_ngram_size=3`、`repetition_penalty=1.1`、`compression_ratio_threshold=2.2`、`no_speech_threshold=0.5`、temperature fallback 階梯、VAD `min_silence_duration_ms` 2000→500、`hotwords`（接 P1-1）、`word_timestamps=True`（供低信心標色與 diarization 對齊）。
- [ ] **P1-7 `[Prompt]` System Prompt 單一來源化**
  `backend/core/prompts.py` 為唯一正本；`config.py:188` 改 import；`config.yaml` 內嵌版移除、CLI 改讀同一來源。加 CI 檢查（`scripts/check_docs.sh` 擴充）防再度分叉。
- [ ] **P1-8 `[Prompt][Fix]` 驗證器改為容錯的結構化比對**
  `_validate_summary_quality:363-378` 的精確字串比對（雙空格 `主  席：` 等）改為正則容錯（允許空格數量/全半形變異），驗證「欄位存在性」而非「字面完全一致」；欄位標準化交給後處理統一輸出格式。降低無謂 refine 輪次。
- [ ] **P1-9 `[Fix][LLM]` 後處理與驗證順序重構**
  現況：驗證通過→又被 `task_processor._format_result` 改寫→不再驗證（`task_processor.py:296-323`）。改為：所有文字級後處理（英文段移除、結構補全）移入 `summarization.py` 驗證**之前**，驗證是最後一關；`_ensure_structure:498` 的單空格欄位格式與 prompt/validator 統一。
- [ ] **P1-10 `[Fix]` `_remove_english_segments` 加白名單保護**
  `task_processor.py:239-247` 對英文比例 >0.5 的行整行刪除，會誤刪英文專名行。改為：詞彙表命中行、欄位值行不刪；刪除行為記入 log 供稽核。
- [ ] **P1-11 `[Prompt]` 逐字稿與會議紀錄分層輸出**
  依文書處理手冊體例，輸出改為兩件套：(a) 逐字稿（發言紀錄體例、含時間戳）；(b) 會議紀錄（現行公文格式）。`include_transcript` 選項升級為獨立下載檔案，而非附錄。

### P2｜效能與部署優化（2-3 個迭代）

- [ ] **P2-1 `[Deploy]` Docker「零 rebuild」策略統一（頻寬紅線）** ⭐
  - `docker-compose-windows-gpu.yml` 比照標準版掛載 `../backend`、`../frontend`、prompts 相關檔案（改 code/prompt 只需 `docker compose restart`，**不需 rebuild**）；生產/開發差異用 profile 或註解區隔，不要用兩套互相矛盾的檔案。
  - 部署文件**刪除所有 `build --no-cache` 教學**（`docker-compose-windows-gpu.yml:21,177`、部署手冊同步修訂）；rebuild 只在 `requirements.txt`/Dockerfile 變更時需要，且必用層快取。
  - 模型快取已用 named volume（正確），文件加註**嚴禁 `docker compose down -v`**（會清空 6GB 模型重新下載）；提供 `scripts/download_models.py` 離線預載說明（可從有網路的機器拷貝 volume）。
  - HF 下載設 `HF_HUB_ENABLE_HF_TRANSFER=0` 並鎖定 revision（已鎖，保持），確保不因 revision 漂移重新下載。
- [ ] **P2-2 `[Deploy][LLM]` Gemma 4 設定優化（RTX 4090 / 24GB）**
  - 模型標籤明確化：預設 `gemma4:31b-it-qat`（QAT 約 17-18GB），並驗證 `_resolve_compatible_model`（`summarization.py:1213`）對 qat/q4 變體的解析；後備選項 `gemma4:26b-a4b-qat`（MoE，約 15GB、速度接近 8B，剩餘 VRAM 可開更大 context）。
  - `num_ctx` 8192→**32768**（4090 with QAT + KV q8_0 可承受），同步調整 `_build_local_context_plan` 預算常數——這將大幅減少長逐字稿的切塊數與合併輪次，直接提升品質與速度。
  - 部署文件加入 Ollama 環境變數：`OLLAMA_FLASH_ATTENTION=1`、`OLLAMA_KV_CACHE_TYPE=q8_0`。
  - M4 Pro 48GB：同款 QAT 模型可跑；`config.macos.yaml` 的 `gemma3:27b-it-qat` 升級為與 Windows 同款 gemma4，消除雙模型維護。
- [ ] **P2-3 `[Deploy]` macOS M4 Pro 路徑名實相符**
  二擇一並落實：(a) 實作 MLX-Whisper 後端（`mlx-community` 模型，真 MPS 加速），或 (b) 移除所有「MPS 加速 3-5 倍」宣稱（`start-mac-native.sh:33,270`、`config.macos.yaml:40-48`）改標明 CPU 推理。清除 `config.macos.yaml` 硬編碼個人路徑 `/Users/hsiaojohnny/...`，改相對路徑或環境變數。
- [ ] **P2-4 `[Perf]` 真並行或誠實設定**
  `MAX_CONCURRENT_TASKS>1` 目前無效（單消費者，`task_processor.py:68-83`）。RTX 4090 場景：ASR 與 LLM 可流水線並行（任務 A 在 LLM 階段時任務 B 可開始 ASR）。最小改法：兩個消費者協程＋GPU 階段互斥鎖；或將設定固定為 1 並在 compose/文件移除誤導值。
- [ ] **P2-5 `[Perf]` 上傳串流化**
  `file_manager.validate_file_size:86` 一次 `await file.read()` 全檔進記憶體（GPU compose 允許 500MB）。改分塊寫入暫存檔＋邊寫邊算大小上限。統一 `MAX_FILE_SIZE_MB` 四處不一致（Dockerfile ENV=100 / config.py=200 / windows-gpu=500 / docstring=100）。
- [ ] **P2-6 `[Fix]` 健康檢查與逾時
  ** `/api/health`（非 quick）在 30s `TimeoutMiddleware` 下可能 503（`main.py:81`、`routes.py:50-74`）：外部依賴檢查各設 3-5s 個別逾時、並行執行；`check_ollama_health` 副作用（`summarization.py:1111` 每次改 `_resolved_model`）改為只讀。
- [ ] **P2-7 `[Fix]` CORS 收斂**
  `main.py:87-90` `allow_origins=["*"]`＋`allow_credentials=True` 改為明確來源清單（環境變數 `ALLOWED_ORIGINS`），內網部署預設同源。
- [ ] **P2-8 `[Deploy]` 死碼與遺留清理**
  移除 `_summarize_with_local_llm`（`summarization.py:778`，無人呼叫）、`app.js` 重複的 `setDownloadButtonsEnabled`（`:271` vs `:342`）、已停用且內容錯置的 `.github/workflows/build-windows.yml`、`ENABLE_BATCH_UPLOAD` no-op（`routes.py:106-108`——實作或移除）。CLI（`main.py`+`src/`）與後端功能重疊且品質較低（無三階段/驗證、`num_ctx` 不匹配 `src/summarizer.py:78`）：標記 deprecated 或收斂為呼叫後端 API。

### P3｜長期強化（持續）

- [ ] **P3-1 `[ASR]` 說話者分離（diarization）**
  pyannote community-1（exclusive 模式、必給 `min/max_speakers`，HF 模型離線 clone 後可斷網）；封閉內網替代：sherpa-onnx（45MB、CPU）。對齊規則：句級多數決＋`<0.5s` 切換併入相鄰段。輸出議事體例「○○○（職稱）：」。分軌錄音直接分軌辨識跳過 diarization（`ffmpeg channelsplit`）。
- [ ] **P3-2 `[FE]` 低信心段標色與人工複核 UI**
  利用 `word_timestamps`＋`avg_logprob`（P1-6）在前端逐字稿檢視器將低信心段標色（借鏡 noScribe），顯示 P1-3/P1-4 的修改對照表；複核原則：低信心段 100%、決議/數字/日期句全檢、其餘抽查 10-20%。此為政府合規（AI 產出須人工負責）的介面落地。
- [ ] **P3-3 `[FE]` 進度與狀態真實化**
  前端進度階段目前以百分比推估（`app.js:511-518`），改由後端 WebSocket 推送真實 stage（上傳/ASR/校正/萃取/合併/生成/驗證）；`COMPLETED_WITH_WARNING`（P0-5）狀態顯示；下載檔名與後端實際檔名一致（`app.js:363`）。
- [ ] **P3-4 `[Test]` 測試補強**
  針對本計畫修復項建立回歸測試：initial_prompt 套用（P0-1）、切窗重疊（P0-2）、token 預算（P0-6）、校正閘門同音判定（P1-4）、驗證器容錯（P1-8）、後處理順序（P1-9）。移除測試不存在路徑的 `test_mlx_direct.py`（或隨 P2-3(a) 恢復意義）。建立 CER 評估腳本納入 CI（黃金標準測試集，P1-5 產出）。
- [ ] **P3-5 `[LLM]` 任務佇列持久化**
  `queue_manager._tasks` 純記憶體，重啟全失（`queue_manager.py`）。以 SQLite 持久化任務狀態，重啟後可恢復/續跑，孤兒上傳檔可追溯。
- [ ] **P3-6 `[ASR]` 台語段落策略**
  依 P1-5 實測結果：若台語佔比高，導入語言偵測分流（台語段走 Breeze-ASR-26、國語段走 ASR-25 類模型）；UI 標記台語段落來源並強制人工校對（CER 約 30% 為業界天花板，司法院亦僅 85%）。
- [ ] **P3-7 `[Deploy]` 詞彙表治理流程**
  機關詞庫（P1-1）版本化管理：新增詞條審核流程、跨會議共用庫 vs 單場會議庫分層、匯出/匯入（跨機關部署）。
- [ ] **P3-8 `[LLM]` 評估體系常態化**
  每次模型/prompt 變更跑四指標：整段 CER、詞表關鍵詞召回率、非詞表字元錯誤率（NNE-CER，防改壞）、過度改寫率；摘要另加決議召回率＋LLM-as-judge。上線門檻：CER 降且 NNE-CER 不升、過度改寫率 <2%、簡體洩漏 0。

---

## 四、驗收指標

| 指標 | 目標值 | 依據 |
|---|---|---|
| 國語段 CER | ≤ 8% | Breeze-ASR-25 CommonVoice zh-TW 7.97 |
| 中英夾雜段 MER | ≤ 17% | ASCEND-MIX 16.38 |
| 台語段 CER | ≤ 30-35%（必配人工校對） | Taigi Benchmark 天花板 30.13、司法院 85% |
| 詞彙表關鍵詞召回率 | ≥ 95% | DANCER/RAG-GEC 實證 |
| 校正過度改寫率 | < 2% | Confidence-Guided EC |
| 簡體字洩漏 | 0 | OpenCC 全文掃描 |
| 會議紀錄必要欄位完整率 | 100%（含（待確認）） | 文書處理手冊體例 |
| 摘要失敗靜默降級 | 0（一律顯性警告） | P0-5 |
| 改 code/prompt 的 Docker 更新成本 | `restart`，0 rebuild、0 模型重下載 | P2-1 |

---

## 五、三平台部署守則

| 平台 | ASR | LLM | 關鍵設定 |
|---|---|---|---|
| **Windows 11 + RTX 4090（24GB）** | Breeze 系列 @ CUDA float16 | Ollama `gemma4:31b-it-qat`（約 17-18GB） | `num_ctx=32768`、`OLLAMA_FLASH_ATTENTION=1`、`OLLAMA_KV_CACHE_TYPE=q8_0`、`keep_alive=10m` |
| **Mac M4 Pro（48GB 統一記憶體）** | 短期 CPU（誠實標示）；中期 MLX-Whisper（P2-3） | 同款 gemma4 QAT（LM Studio 或 Ollama） | 統一記憶體充裕，`num_ctx` 可同 32K；移除硬編碼路徑 |
| **Docker（辦公室頻寬受限）** | 同 Windows 設定 | 主機 Ollama（`host.docker.internal`，模型不進容器） | **鐵律**：程式碼用 volume 掛載（restart 即生效）；模型用 named volume（永不隨 rebuild 消失）；禁 `build --no-cache`；禁 `down -v`；rebuild 僅限依賴/基底變更 |

---

## 附錄：主要研究出處（節錄）

- Breeze-ASR-25 / 26 model cards（MediaTek Research, Hugging Face）；arXiv 2506.11130、2603.19259
- HyPoradise（NeurIPS 2023）、Whispering LLaMA（EMNLP 2023）、RobustGER（ICLR 2024）、ChineseHP（arXiv 2407.01909）、DANCER（arXiv 2403.17645）、Confidence-Guided EC（arXiv 2509.25048）、RAG-GEC（arXiv 2509.23630）
- Koenecke et al. 2024（Whisper 幻覺量化，arXiv 2402.08021）；OpenAI Whisper Prompting Guide
- pyannote community-1（pyannote.ai blog）；AliMeeting（arXiv 2202.01986）
- Gemma 4 / QAT 發布（Google Blog, 2026-04 / 2026-06）；Ollama context-length docs
- 行政院文書處理手冊；行政院及所屬機關（構）使用生成式 AI 參考指引；資通安全責任等級分級辦法
- 司法院法庭/會議中文語音辨識系統（judicial.gov.tw、iThome 156691）；中研院 TranscriptHub（iThome 168547）
- 數位發展部 Taiwan-Tongues-ASR-CE（github.com/adi-gov-tw）
