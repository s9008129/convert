# ============================================
# 政府智慧會議紀錄生成系統 Python 依賴
# （版本以根目錄 VERSION 檔為準；建議改用 uv + pyproject.toml 管理）
# ============================================

# Web 框架
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
websockets==12.0

# Whisper / ASR 語音轉錄
faster-whisper==1.2.1
ctranslate2>=4.5.0
torch>=2.1.0,<3.0.0
transformers>=4.49.0,<5.0.0
accelerate>=0.27.0,<2.0.0
safetensors>=0.4.2,<1.0.0
mlx-whisper==0.4.3 ; platform_system == "Darwin" and platform_machine == "arm64"

# Hugging Face 模型管理
huggingface_hub>=0.20.0
requests>=2.28.0

# LLM 客戶端
openai==1.12.0
httpx==0.26.0

# 資料驗證
pydantic==2.6.1
pydantic-settings==2.1.0

# 非同步任務
asyncio-throttle==1.0.2

# 工具
python-dotenv==1.0.1
aiofiles==23.2.1
PyYAML==6.0.1
packaging>=21.0

# 日誌
loguru==0.7.2

# 音訊處理
ffmpeg-python==0.2.0
av>=12.0.0

# CUDA 支援（如果可用）
# nvidia-cublas-cu12
# nvidia-cudnn-cu12

# DOCX 文件生成
python-docx>=0.8.11

# 注意：v4.2 新增的語意校正依賴放在 requirements-correction.txt
# （獨立檔案是為了保住 Docker pip 大層快取——本檔一改，torch 等
#  數 GB 套件就會重新下載。新增小型依賴請一律加在補充檔。）

# 測試框架
pytest>=7.4.0
pytest-asyncio>=0.21.0
