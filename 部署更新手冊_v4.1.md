# 📘 MeetingScribe 會議系統 — 更新部署手冊（v4.1）

> 給「非技術人員」的逐步操作手冊。照著做就好，**不需要看懂指令內容**。
> 指令一律是「整段反白 → 複製 → 在終端機貼上 → 按 Enter」。
> 預計時間：**約 10～20 分鐘**（大部分時間是電腦自己在跑，你等就好）。

---

## 🎯 這次更新做了什麼？（30 秒看懂）

1. **修好會議紀錄的品質問題**：之前產出的紀錄會混入英文、口語贅字、校正過程，甚至亂編不存在的單位／人名。現在已修正，並要求「查不到的內容一律寫『（待確認）』，不准亂編」。
2. **升級雲端 AI 模型**：改用較新、較省成本的 `gemini-3.1-flash-lite`。

這些修正的「程式」**已經放到 GitHub 上了**。你要做的，只是去「跑這個系統的那台電腦」上，讓它**抓最新程式 → 套用 → 重開**。

> 💚 **放心**：這次更新**不會下載大檔案、不會吃掉你的網路流量**。原因在步驟六會用「便當盒」的比喻解釋清楚。

---

## ✅ 開始前的準備（請先確認）

- [ ] 你能操作 **跑這個系統的那台電腦**（IP 是 `10.97.15.58`）。
      → 通常是用「**遠端桌面 / Remote Desktop**」連過去，或直接坐到那台電腦前面。
- [ ] 那台電腦**有開機、能連網路**。
- [ ] 那台電腦上的 **Docker Desktop 正在執行**（工作列右下角有一隻鯨魚圖示）。

> ⚠️ 重點：以下所有操作，都是在 **`10.97.15.58` 那台電腦上**做，**不是**在你自己的電腦上。

---

## 🪜 步驟一：打開「終端機」

在 `10.97.15.58` 那台電腦上：

1. 按鍵盤左下角的 **開始**（Windows 圖示）。
2. 輸入 **`powershell`**。
3. 點開 **「Windows PowerShell」**（看到一個藍色或黑色的視窗就對了）。

> 之後所有指令，都貼在這個視窗裡，貼完按 **Enter**。

---

## 🪜 步驟二：進到專案資料夾

你需要知道「這個系統的程式放在哪個資料夾」。它通常長這樣（裡面會有 `docker` 子資料夾和 `README.md`）：

- 例如：`D:\meetingscribe`、`C:\projects\convert`、`D:\dev\convert` …（**每台電腦不一定一樣**）

在 PowerShell 貼上下面這行，**把引號裡換成你的實際資料夾路徑**，再按 Enter：

```powershell
cd "D:\你的專案資料夾"
```

> 💡 **不知道資料夾在哪？** 貼這行可以幫你找出來（它會印出資料的存放路徑，往上一層就是專案資料夾）：
> ```powershell
> docker inspect meetingscribe-app --format "{{ range .Mounts }}{{ .Source }}`n{{ end }}"
> ```
> 如果連這個都沒反應，請先做完**步驟三的判斷**，或截圖找工程師協助。

---

## 🪜 步驟三：抓最新的程式

貼上這行，按 Enter：

```powershell
git pull
```

- ✅ 看到 **`Updating ...` 或一堆檔名** → 成功，繼續。
- ✅ 看到 **`Already up to date.`** → 也沒問題（代表程式已是最新），繼續。
- ❌ 看到 **`not a git repository`** → 這台電腦的程式不是用 git 抓的，**請停在這裡找工程師**（或把畫面截圖回來問 AI 助理）。

---

## 🪜 步驟四：⭐ 檢查「模型設定檔」`.env`（很重要，別跳過）

這一步是為了確保**雲端模型真的會更新**。

1. 用**記事本**打開專案資料夾裡的 **`.env`** 檔案。
   - 在 PowerShell 貼這行最快（會用記事本打開）：
     ```powershell
     notepad .env
     ```
   - 如果跳出「找不到檔案」，代表沒有 `.env`，**這步可以直接跳過**，前往步驟五。
2. 在記事本裡用 **Ctrl+F** 搜尋 **`GEMINI_MODEL`**。
   - **有找到**（例如 `GEMINI_MODEL=gemini-2.5-flash-lite`）→ 把它整行改成：
     ```
     GEMINI_MODEL=gemini-3.1-flash-lite
     ```
     然後 **Ctrl+S 存檔、關閉**。
   - **沒找到** → 不用改，直接關閉即可（系統會自動用新的預設值）。

> 為什麼要檢查？因為 GPU 版會優先讀 `.env`。如果 `.env` 裡留著舊的模型名，會「蓋掉」我們的更新。

---

## 🪜 步驟五：判斷你用「哪一種版本」（決定接下來貼哪組指令）

貼上這行，按 Enter：

```powershell
docker ps
```

看畫面中 **`IMAGE`（映像檔）** 那一欄的文字，對照下表：

| 看到的映像檔名稱 | 你的版本 | 接下來做 |
|---|---|---|
| `meetingscribe:windows-gpu-cuda12` | **GPU 版**（RTX 4090，最常見） | 步驟六 **A 組** |
| `meetingscribe:latest` | **標準版** | 步驟六 **B 組** |

> 你的這台是 RTX 4090，**九成是 GPU 版（A 組）**。若不確定，先試 A 組；若 A 組指令報「找不到檔案」之類的錯，再改用 B 組。

---

## 🪜 步驟六：套用更新 + 重啟

### 📦 先用 30 秒搞懂：這次到底會不會「吃網路流量」？（重要）

> 你最在意的就是「**不要浪費網路頻寬**」。先看這個比喻，你就能完全放心：

把這套系統想成一個 **便當盒** 🍱：

- **便當盒本身**（作業系統、AI 函式庫）＝很重很大，第一次裝好後**幾乎不會再變**。
- **裡面的菜**（我們這次修改的程式碼）＝這次**只換了菜**。

所謂「重新建置（build）」，**只是把新的菜重新裝進原本的便當盒**，
**不會重新買便當盒、也不會重新上網買食材**。

只有當「**食材清單**」（套件清單 `requirements.txt`）真的改了——**這種情況很少**——
電腦才需要上網補貨，那才是 `--no-cache` 在做的事。

> ✅ **這次 v4.1 更新只改了「菜」（程式碼），沒有改「食材清單」。**
> 所以無論你是哪一種版本，照下面做都**不會下載大檔案、不會吃網路流量**，請安心。

#### 🧭 一張表看懂：要不要重建？會不會吃網路？

| 你的情況 | 要重建映像嗎？ | 會上網下載嗎？ | 對應做法 |
|---|---|---|---|
| **標準版** + 這次更新 | ❌ **不用重建** | ❌ **完全不會** | 走 **B 組**（只要重開） |
| **GPU 版** + 這次更新 | ✅ 要重建（但只換菜） | ❌ **幾乎不會**（純本機動作） | 走 **A 組**（普通 `build`，**不要**加 `--no-cache`） |
| 任何版本 + 「食材清單變了」 | ✅ 要重建 | ✅ **會下載**（補貨） | 才需要 `--no-cache`（**這次用不到**） |

> 🚫 **黃金守則：除非有工程師明確告訴你「套件清單改了」，否則永遠不要自己加 `--no-cache`。** 加了它＝叫電腦把整個便當盒重買一次，白白吃掉大量網路流量。

---

> ⏳ **時間預期**：A 組（GPU 版）電腦會跑約 **3～10 分鐘**，畫面會一直捲動文字，**這是正常的**，耐心等到它停下來、出現新的輸入游標即可。B 組（標準版）通常**幾秒到一分鐘**就好。

### 🅰️ A 組指令（GPU 版）── 用「普通 build」，不下載

**整段一起複製貼上**，按 Enter（它會一行一行自動跑）：

```powershell
cd docker
docker compose -f docker-compose-windows-gpu.yml build
docker compose -f docker-compose-windows-gpu.yml up -d
```

> 注意上面**沒有** `--no-cache`，這是刻意的——它會重用已經裝好的套件，**不上網**，只把新程式碼換進去。

### 🅱️ B 組指令（標準版）── 連重建都不用，更省

標準版的程式碼是「即時連動」的，所以**完全不需要重建映像**，只要把容器重開讓它讀到新程式碼：

```powershell
cd docker
docker compose up -d
```

> 注意這裡**沒有** `--build`，也是刻意的——標準版不需要重建，這樣最快、最省資源。

✅ 兩組跑完後，若最後出現類似 **`Started`、`Running`、`meetingscribe-app  Started`** 等字樣，代表已套用更新並重新啟動。

---

> 🆘 **真的遇到「需要下載套件」的少見情況怎麼辦？**（例如工程師說這次有改 `requirements.txt`）
> 才改用下面這組（**只有 GPU 版、且確定套件清單變了**才用，會消耗網路）：
> ```powershell
> cd docker
> docker compose -f docker-compose-windows-gpu.yml build --no-cache
> docker compose -f docker-compose-windows-gpu.yml up -d
> ```

---

## 🪜 步驟七：等待系統啟動完成

系統重開後，**載入語音模型需要幾分鐘**。請等 **3～5 分鐘**，再做下一步確認。

可以貼這行看狀態：

```powershell
docker ps
```

- 看 **`STATUS`** 那一欄：
  - 顯示 **`Up ... (healthy)`** → 🎉 完全就緒！
  - 顯示 **`Up ... (health: starting)`** → 還在啟動，再等 1～2 分鐘重貼一次。
  - 顯示 **`Restarting`** 一直重複 → 出問題了，請看最後的「🆘 出問題怎麼辦」。

---

## 🪜 步驟八：確認更新成功（兩個檢查）

### 檢查 1：網頁打得開
在**任何一台電腦**的瀏覽器網址列輸入：

```
http://10.97.15.58:9527
```

能看到系統畫面 = 服務正常運作。

### 檢查 2：⭐ 實際產一份會議紀錄（這才是真正的驗收）
1. 在網頁上**上傳一個音檔**，選好模式，等它跑完。
2. 下載產出的會議紀錄，**用眼睛確認以下幾點**：

| 檢查項目 | 應該要 | 不應該出現 |
|---|---|---|
| 開頭第一行 | 是「**會議名稱：**」 | ❌ 任何英文、開場白 |
| 整份內容 | 全部繁體中文 | ❌ `Analysis of the Transcript`、`Evaluation Criteria` 等英文 |
| 格式 | 乾淨的欄位 | ❌ `[請從文本中提取…]` 這種方括號 |
| 文字 | 台灣繁體字 | ❌ 簡體字 |
| 查不到的欄位 | 寫「**（待確認）**」 | ❌ 亂編的單位 / 人名 / 日期 |

✅ 以上都符合 = **更新成功，大功告成！**

---

## 🆘 出問題怎麼辦？

> **黃金原則**：只要看到**紅色錯誤訊息**，先別慌、**先截圖**，停在那裡。把截圖貼給 AI 助理、或找工程師，都能快速判斷。

| 狀況 | 處理方式 |
|---|---|
| `git pull` 顯示 `not a git repository` | 程式不是用 git 部署的 → 截圖找工程師 |
| 建置（build）跑很久（5～15 分鐘） | **正常**，耐心等它跑完 |
| `docker ps` 的 STATUS 一直 `Restarting` | 啟動失敗 → 貼 `docker logs meetingscribe-app --tail 50` 把訊息截圖求助 |
| **雲端模式**報錯 / 沒反應 | ①回頭確認**步驟四**的 `.env` 已改成 `gemini-3.1-flash-lite`；②可能是 API 金鑰尚未開通新模型權限 → 暫時先用「**本地模式**」即可正常運作；③仍不行就截圖求助 |
| 會議紀錄**還是有英文 / 還是亂編** | 多半是「程式沒真的更新」：①確認步驟三 `git pull` 有看到檔名更新；②**GPU 版**要確認步驟六的 `build` 有真的跑完；③再重做一次步驟六。（仍**不需要** `--no-cache`）|
| 想**還原**到更新前的版本 | 請工程師在專案資料夾執行：`git reset --hard add9d1c`，再重做步驟六重新建置 |

---

## 📎 附錄：給工程師看的技術摘要（你不用懂，給協助你的人看）

- **本次變更（已在 `main`）**：
  - `28a7a60` — 會議紀錄品質根治＋System Prompt 多代理對抗硬化
  - `afa4850` — Gemini 雲端模型 `gemini-2.5-flash-lite` → `gemini-3.1-flash-lite`
- **主要修改檔**：`backend/core/prompts.py`、`backend/services/summarization.py`、`backend/services/task_processor.py`、`config.yaml`、`docker/docker-compose.yml`、`config.macos.yaml`、`.env.example`、`VERSION`(4.0→4.1)。
- **為何 GPU 版需重建、標準版不用**：`docker/docker-compose-windows-gpu.yml` 未掛載 `backend/` 卷冊，程式碼經 `Dockerfile.gpu` 的 `COPY --chown=appuser:appuser . .`（第 118 行）烤進映像，故 `.py` 變更需重新 `build`；標準版 `docker-compose.yml` 有掛載 `../backend:/app/backend`（第 106 行），程式碼即時連動，僅需 `docker compose up -d` 重建容器即可。
- **為何普通 `build` 不吃網路**：`Dockerfile.gpu` 先 `COPY requirements.txt` 再 `pip install`（第 78–90 行），最後才 `COPY . .`（第 118 行）。Docker 逐層快取、自首個變動指令起才失效；`.py`-only 變更只讓最後一層 `COPY . .` 失效（純本機檔案複製），`apt-get`/`pip install` 層命中快取、零下載。`--no-cache` 會強制丟棄所有層、連帶重抓 apt/pip 數百 MB，**故本次嚴禁使用**。
- **模型生效優先序**：GPU compose 透過 `env_file: ../.env` 載入且自身未設 `GEMINI_MODEL`，故 `.env` 的 `GEMINI_MODEL` 會覆蓋 `backend/core/config.py` 的新預設值 — 須一併檢查（見步驟四）。
- **驗收**：產出會議紀錄第一行為「會議名稱：」、無英文分析段／`Evaluation Criteria`／方括號模板／簡體字；未明示欄位為「（待確認）」。
- 註：`/api/health` 的 `version` 欄位目前顯示 `0.0.0`（容器內 `VERSION` 讀取回退），**不可**作為更新成功依據，請以「實際產出內容」為準。

---

*手冊版本：v4.1｜建立日期：2026-06-04*
