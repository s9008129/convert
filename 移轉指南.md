# 🚀 會議轉錄工具 - 工作站移轉指南

> 本指南將協助您將會議轉錄工具移轉到新的工作站。  
> 即使您不熟悉技術，只要按照步驟操作即可完成。

---

## 📋 目錄

1. [事前準備清單](#-事前準備清單)
2. [步驟一：複製專案資料夾](#步驟一複製專案資料夾)
3. [步驟二：確認 Ollama 和模型](#步驟二確認-ollama-和模型)
4. [步驟三：安裝 Python 環境](#步驟三安裝-python-環境)
5. [步驟四：設定 CUDA 加速](#步驟四設定-cuda-加速)
6. [步驟五：執行測試](#步驟五執行測試)
7. [常見問題排解](#-常見問題排解)

---

## ✅ 事前準備清單

在開始之前，請確認新工作站具備以下條件：

| 項目 | 需求 | 您的狀態 |
|------|------|----------|
| 作業系統 | Windows 10/11 64位元 | ☐ |
| 顯示卡 | NVIDIA RTX 4090 | ☐ 已確認 |
| Ollama | 已安裝 | ☐ 已確認 |
| LLM 模型 | Gemma3:12B | ☐ 已確認 |
| 網路連線 | 首次執行需要下載模型 | ☐ |

---

## 步驟一：複製專案資料夾

### 1.1 需要複製的檔案

將整個專案資料夾複製到新工作站，建議放在 `D:\dev\convert` 或類似位置。

**必須包含的檔案：**
```
convert/
├── 開始轉錄.bat        ← 主程式入口（雙擊執行）
├── main.py             ← Python 主程式
├── config.yaml         ← 設定檔（重要！）
├── requirements.txt    ← Python 套件清單
├── src/                ← 程式碼資料夾
│   ├── __init__.py
│   ├── ollama_client.py
│   ├── summarizer.py
│   └── whisper_transcriber.py
├── input/              ← 放入要轉錄的音訊檔
└── output/             ← 輸出的會議記錄
```

### 1.2 不需要複製的檔案

以下資料夾可以不複製（會自動產生）：
- `.venv/` - Python 虛擬環境（會重新建立）
- `temp/` - 暫存檔
- `logs/` - 日誌檔
- `__pycache__/` - Python 快取

---

## 步驟二：確認 Ollama 和模型

### 2.1 確認 Ollama 已安裝

1. 按下 `Win + R`，輸入 `cmd`，按 Enter
2. 輸入以下指令：
   ```
   ollama --version
   ```
3. 如果顯示版本號（如 `ollama version 0.1.xx`），表示已安裝 ✅

**如果未安裝：**
- 前往 https://ollama.ai/download 下載安裝

### 2.2 確認 Gemma3:12B 模型

1. 在命令提示字元輸入：
   ```
   ollama list
   ```
2. 確認清單中有 `gemma3:12b`

**如果沒有此模型，請執行：**
```
ollama pull gemma3:12b
```
> ⏳ 下載大小約 8GB，請耐心等待

### 2.3 修改設定檔

用記事本開啟 `config.yaml`，找到 `model` 設定，確認為：

```yaml
llm:
  ollama:
    model: "gemma3:12b"    # ← 改成這個
```

> 💡 **提示**：Gemma3:12B 比 270M 大 45 倍，可以正確處理長文本摘要

---

## 步驟三：安裝 Python 環境

### 3.1 確認 Python 版本

1. 開啟命令提示字元（cmd）
2. 輸入：
   ```
   python --version
   ```
3. 需要 **Python 3.10 或更新版本**

**如果未安裝或版本太舊：**
1. 前往 https://www.python.org/downloads/
2. 下載 Python 3.11 或 3.12
3. 安裝時 **務必勾選「Add Python to PATH」**

### 3.2 建立虛擬環境

1. 開啟命令提示字元
2. 切換到專案資料夾：
   ```
   cd D:\dev\convert
   ```
3. 建立虛擬環境：
   ```
   python -m venv .venv
   ```
4. 啟動虛擬環境：
   ```
   .venv\Scripts\activate
   ```
5. 您會看到提示符號前面出現 `(.venv)`

### 3.3 安裝必要套件

在虛擬環境中執行：
```
pip install --upgrade pip
pip install -r requirements.txt
```

> ⏳ 這會安裝 faster-whisper、PyYAML、httpx 等套件

---

## 步驟四：設定 CUDA 加速

RTX 4090 可以大幅加速語音轉錄，需要安裝 CUDA 支援套件。

### 4.1 安裝 CUDA 套件

在虛擬環境中執行：
```
pip install nvidia-cudnn-cu12 nvidia-cublas-cu12
```

### 4.2 確認 CUDA 可用

```
python -c "import torch; print('CUDA:', torch.cuda.is_available()); print('GPU:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'N/A')"
```

應該顯示：
```
CUDA: True
GPU: NVIDIA GeForce RTX 4090
```

---

## 步驟五：執行測試

### 5.1 準備測試檔案

1. 找一個短的音訊檔（1-2 分鐘即可）
2. 放入 `input` 資料夾

### 5.2 執行轉錄

1. **雙擊「開始轉錄.bat」**
2. 程式會自動：
   - ✅ 檢查 Ollama 服務
   - ✅ 檢查 GPU 狀態
   - ✅ 檢查 Python 環境
   - ✅ 檢查並安裝缺少的套件
   - ✅ 掃描輸入檔案

3. 按任意鍵開始處理

### 5.3 確認結果

處理完成後：
1. 開啟 `output` 資料夾
2. 找到 `XXX_摘要.md` 檔案
3. 用記事本或 VS Code 開啟檢視

---

## 🔧 常見問題排解

### ❓ 雙擊 bat 檔案視窗一閃即逝

**原因**：可能有錯誤但來不及看到

**解決方式**：
1. 開啟命令提示字元（cmd）
2. 切換到專案資料夾：`cd D:\dev\convert`
3. 手動執行：`開始轉錄.bat`
4. 這樣可以看到錯誤訊息

---

### ❓ 顯示「Python not found」

**解決方式**：
1. 確認已安裝 Python 3.10+
2. 確認安裝時有勾選「Add Python to PATH」
3. 如果已安裝但找不到，請重新安裝並勾選 PATH 選項

---

### ❓ 顯示「Ollama is not running」

**解決方式**：
1. 在開始選單搜尋「Ollama」並執行
2. 確認系統匣（右下角）有 Ollama 圖示
3. 或在 cmd 執行：`ollama serve`

---

### ❓ 顯示「CUDA not available」或使用 CPU 模式

**解決方式**：
1. 確認已安裝 NVIDIA 驅動程式
2. 執行 `nvidia-smi` 確認 GPU 可被偵測
3. 安裝 CUDA 套件：
   ```
   pip install nvidia-cudnn-cu12 nvidia-cublas-cu12
   ```

---

### ❓ 轉錄很慢（超過音訊長度的 2 倍）

**可能原因**：正在使用 CPU 而非 GPU

**解決方式**：
1. 確認顯示「Detected GPU: NVIDIA GeForce RTX 4090」
2. 確認 `config.yaml` 中 `device: "auto"` 或 `device: "cuda"`
3. 檢查 CUDA 套件是否正確安裝

---

### ❓ LLM 摘要品質很差或輸出空白

**原因**：模型太小（如 gemma3:270m）

**解決方式**：
1. 使用較大的模型：`gemma3:12b` 或 `qwen2.5:7b`
2. 修改 `config.yaml` 中的 `model` 設定
3. 確認模型已下載：`ollama pull gemma3:12b`

---

### ❓ 出現「Out of Memory」錯誤

**原因**：GPU 記憶體不足

**解決方式**：
1. 關閉其他佔用 GPU 的程式
2. 在 `config.yaml` 中將 `compute_type` 改為 `int8_float16`
3. 或使用較小的 Whisper 模型：`model: "medium"`

---

## 📞 還有問題？

如果以上方法都無法解決，請：

1. 開啟 `logs` 資料夾，找到最新的日誌檔
2. 記錄錯誤訊息
3. 聯繫技術支援人員

---

## 📝 快速指令參考

| 用途 | 指令 |
|------|------|
| 檢查 Python 版本 | `python --version` |
| 檢查 Ollama 版本 | `ollama --version` |
| 列出 Ollama 模型 | `ollama list` |
| 下載模型 | `ollama pull gemma3:12b` |
| 檢查 GPU | `nvidia-smi` |
| 啟動虛擬環境 | `.venv\Scripts\activate` |
| 安裝套件 | `pip install -r requirements.txt` |
| 手動執行程式 | `python main.py` |

---

*最後更新：2025-11-28*
