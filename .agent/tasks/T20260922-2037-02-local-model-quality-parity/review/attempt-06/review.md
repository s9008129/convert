# 獨立計畫審查 — T20260922-2037-02-local-model-quality-parity（Stage 02，attempt-06）

- 受審對象：`.agent/tasks/T20260922-2037-02-local-model-quality-parity/plan.md`（自稱 `PLAN_REVISION 7`，`plan.md:4`）。
- `REVIEWED_PLAN_REVISION`: **7**
- `REVIEWED_PLAN_SHA256`: **`59cf852ab0a860fd973ab5017fd20c99b6de3e76bee6af9e7afeb279a7f53f7a`**（開審量測；收審複量見文末「收尾」）。
- 審查時間：2026-09-23 00:15–00:45（Asia/Taipei）。
- 審查者：獨立 Stage 02（fresh context；read-only）。未參與本計畫修訂；未修改 `plan.md`、產品碼、`review/attempt-01`–`05`、`e2e/*`、研究文件。
- 唯一寫入：本檔（＋同目錄 `snapshot_plan.sha256`）。所有重現實驗皆寫 `/tmp`，未在 repo 留任何暫存檔。
- 受審範圍：rev 7 的變更＝header rev 7 修訂紀錄＋`§8 P3 波`；`git diff HEAD` 證實 rev 6 本文未被就地改寫（HEAD 版 `plan.md:4` 仍為 rev 6），修訂單調性成立。
- 環境事實（如實記錄）：審查期間工作樹存在**平行進行的未提交變更**（`plan.md` 之 rev 7、`backend/core/text_postprocess.py` 之 CORE-5 實作片段、`e2e/attempt-C2-cloud-baseline/`）。受審結論只綁定上列 hash；`plan.md` 之後若再被修改，本次審查不自動沿用。
- 已讀：`~/.codex/policies/plan-review-gate.md`、`goal-alignment-design-economy.md`、`workflow-routing.md §7`；`plan.md`（rev 7）；`review/attempt-01`–`05`；`e2e/attempt-C1-gemma31b-fix/verify_independent.md`；`attempt-B2-27b-fix/`（`run_notes.md`、`verify_independent.md`）；`attempt-B1-moe-fix/run_notes.md`；HEAD 版 `backend/core/text_postprocess.py`、`backend/services/summarization.py`、`backend/core/config.py`；C1 交付物（交付 md、逐字稿、模型原始輸出 `/tmp/pred_2026-09-22_234336.txt`，來源已由 C1 報告驗證）。

## Goal Baseline（先於計畫框架、從權威來源重建）

1. 第一性結果（使用者原話 N-4）：人工看地端紀錄「品質非常不夠理想，跟雲端 Gemini 差很多」→ 要**接近雲端的品質，且可被證明**；機制必須模型無關、macOS／Windows 一體適用。
2. rev 6 已核准的成果是「出處標註可查核性」的閘門與吸附修復——它證明的是「標註查得到」，不是「內容夠好」。
3. rev 7 自我定位（本審查同意此定位合理）：先造能量「內容涵蓋缺口」的尺（CORE-3）→ 修「補強假陽性／不收斂」（CORE-4）與「吸附倒退／非冪等」（CORE-5）兩個已證實的確定性缺陷 → 以同一把尺跑 D1（Gemma）＋C2（雲端）對照並公開缺口；本波明示不宣稱品質已對齊。
4. 本波的成功標準＝「尺可信、缺陷真的修好、缺口被量到且公開」；任何讓閘門對真缺陷無感、或讓語意改動造成偽 FAIL 的設計，都直接傷害第一性目標的可證明性。
5. 非目標（本波）：把品質缺口本身關掉（留待後續波）；Windows 實機驗證（`[UNVERIFIED]`，計畫已明示）。

## 逐項發現

### R1（高｜類型：不可驗證＋語意契約）`snapped_backward` 的定義與驗收自相矛盾，現行文字不可照字實作

- `plan.md:305-306`：新增觀察值 `snapped_backward`（**target < 原時間戳的筆數**），`§8.6-4`（`plan.md:326`）要求「D1 E2E `snapped_backward == 0`」。
- 但吸附機制**設計上**就會把「段落內任意時間戳」吸到該段 `start`——目標必然早於原時間戳。以 C1 真實資料實測（本審查自行重跑，HEAD 版函式）：
  - 現行（閉區間）對 C1 原始輸出：`changed=8`，且**8 筆全部 target<原值**（其中 6 筆是 C1 §5.2 的「本來是段首被拉回前一段」缺陷、1 筆是 `00:10:04→00:08:15` 的真修正、1 筆同上重複值）。
  - 改半開後（原型）仍有「段落內（非段首）時間戳 → 吸附到段首」的合法路徑；只要 D1 出現 1 筆這種標註，字面定義即 ≥1 → **偽 FAIL**（該行為是 CORE-1 要的功能，不是缺陷）。
  - 反之若實作成「吸附後再量是否倒退」，計數恆為 0 → **偽 PASS、無鑑別力**。
- 旁證：審查期間工作樹已有未提交的 CORE-5 實作，實作端**已把此觀察值改名為 `snapped_across_segment` 且改義成「落點段落不含原時間戳」**（對規則 1/3 由建構保證恆 0，註解自承是絆索）——此現象本身即證明 `§8.4` 的定義在實作端無法照字落地、被迫重新詮釋（先前 F1/F2/F-C/F-D 的同類漂移模式）。
- 證據：`plan.md:305-306`、`plan.md:326`；HEAD `backend/core/text_postprocess.py:868-878`（規則 1/3 皆吸到段 start）；C1 `verify_independent.md` §5.2（缺陷類＝「本來就寫對的段首」被拉回前一段）；本審查 `/tmp/snap_exp.py` 實測輸出。
- 建議（最小修正）：把觀察值改成**缺陷類語意**，例如「原時間戳已是任一真實段落的 `start`、卻被吸附到更早時間（跨回前段）的筆數」；同步更新 `§8.4`／`§8.6-4`／W9（`plan.md:314`）與產品 log／E2E 欄位名一致，並加單元測試釘住「段首不得被移早」。

### R2（中｜類型：設計不足＋驗收可偽陰性）滑窗 bigram ≥0.75 會讓「部分真遺漏」不再觸發補強，且視窗長度未定義

- `plan.md:293-295` 同時宣示：「判定改為滑窗 bigram 召回（暫定 0.75）」與「**不得**……讓真遺漏不再觸發補強」。本審查以該規則實算（normalize＝NFKC＋去標點空白；視窗長度＝標籤長度）：
  - 預期被修好的 B2 假陽性：的／之 `0.833`✓、征收／徵收 `0.875`✓、及／、`0.867`✓ → 會被視為已涵蓋（修復目標方向正確）。
  - **不該被洗掉的真遺漏卻 ≥0.75**：否定詞消失「不得轉傳…→得轉傳…」`0.917`、日期數字不同「9月30日→9月20日」`0.818`、人名消失「李飛負責…→負責…」`0.778` → 全部判「已涵蓋」，**不再觸發補強**。
  - 對照：整條刪除 `0.000`（仍觸發，驗收①可滿足）；詞尾短漏「……數量」`0.714`（仍觸發）。
  - **視窗長度未定義**：若解讀成「任意長度連續視窗」，跨句拼裝（同一句只出現前半、另一句只出現後半）可達 `0.875` → 也可「已涵蓋」，語意不可判定。
  - 另：B2 六筆假陽性中「嚴禁…轉傳…」「黴味…報價」兩筆大改寫 recall 僅 `0.500`／`0.400`，**仍會觸發補強**——修復效益與「消滅 710 s 假陽性」的宣稱不完全相符（D1 ≤1 輪的寬限可吸收，但應如實）。
- 驗收面：`§8.3` 驗收（`plan.md:297-298`）只有①整條刪除②列舉字面變體，**偵測不到**上述偽陰性類；`§8.7`（`plan.md:337-338`）的人工複核只針對「缺失清單」，而「被誤判已涵蓋」的項目**不會出現在任何缺失清單**，此緩解手段對該風險類別無效（除非另外定義）。
- 證據：`plan.md:293-298`、`plan.md:337-338`；B2 `verify_independent.md` §7.1 六筆對照表；本審查 `/tmp/bigram_exp.py` 實算輸出。
- 建議（最小修正）：①明定視窗長度（建議＝標籤長度、左右可容固定位移）；②對「否定詞（不／不得／嚴禁）、數字／日期、主體人名」等關鍵 token 要求 exact 命中（或改以「關鍵 token 集合覆蓋」而非純字元 bigram）；③新增驗收③：「否定／數字／人名 2–3 字變異必須仍觸發補強」；④把 0.75 的常數註解與 §8.7 人工複核寫成可執行判準。

### R3（高｜類型：風險未揭露＋設計經濟）半開區間會移除一類「良性吸附」，且與 D1 閘門邊界效應未被處理

- `plan.md:302` 改半開 `[start, end)`＋段首優先。以真實逐字稿推演（C1 音檔）：
  - 段落表存在「上段 end＝下段 start」相鄰鏈（如 `[00:19:39-00:19:49]`→`[00:19:49-00:21:01]`）：半開＋段首優先可正確不動（修正倒退一格）✓。
  - 但「時間戳＝某段 `end` 且**非任何段** `start`」的標註（如 C1 的 `00:10:04`，逐字稿 `[00:08:15-00:10:04]` 的 end，下一段 `00:10:06` 起）：半開語意下不再被吸附，維持非段首；且新儀器（inside 同步半開，`plan.md:307-308`）下該筆也**變成不可回溯**。
  - 以 C1 交付資料實測：HEAD 產出 `on_start 20/20=1.0`、`traceable(closed)=1.0`；半開原型輸出 `on_start 19/20=0.95`、`traceable(half-open)=19/20=0.95`——**恰好壓在 rev 6 閘門線（≥0.95）**。若 D1 出現 ≥2 筆同類標註即 `18/20=0.90 < 0.95`：**偽 FAIL，肇因是語意改動而非產品缺陷**。
  - 計畫對此只要求「修改前先量測既有三份紀錄的受影響筆數、>0 明示」（`plan.md:307-308`），未定義 D1 若因此壓線／破線的處置，也未在 `§8.4` 揭露「此類良性吸附將消失」。
- 設計經濟（替代方案）：本審查以同一份 C1 資料推演「**閉區間＋段首優先**」原型 → `changed=1`（正是 `00:10:04→00:08:15` 這個良性修正）、段首倒退 0 筆、`f(f(x))` byte 不變（冪等成立）。即「不倒退＋冪等」**不需要**改成半開即可達成；半開是額外語意變更（要犧牲良性吸附），計畫未論證其必要性。
- 證據：`plan.md:302`、`plan.md:305-308`、`plan.md:326`；逐字稿 `[00:08:15-00:10:04]`／`[00:10:06-…]` 與 `[00:19:34-00:19:34]`（零長度段真實存在）；本審查 `/tmp/snap_exp.py`、`/tmp/snap_exp2.py` 實測。
- 建議（最小修正）：二選一——(i) 改採「閉區間＋段首優先」（保留良性吸附，並以測試釘住「段首不倒退」與「冪等」）；或 (ii) 維持半開，但把「end 非 start」類別正式寫入計畫：先量測三份舊紀錄＋C1 原始輸出的此類筆數（C1＝1）、加單元測試固定語意、並**事先聲明** D1 若因該類標註使 `on_start`／`traceable` 壓線或低於閘門時的判定與路由（語意效應 vs 產品缺陷）。

### R4（低｜類型：事實錯誤＋措辭）

- `plan.md:272`（R23）「假陽性集合**註定逐輪相同**」與其引用證據不合：B2 實測第 1 輪 11 項、第 2 輪 6 項，**集合有變**。準確敘述應為「假陽性無法被模型自癒而持續存在（問題清單固定不變），導致輪次白燒」。
- `plan.md:272` 引註 `summarization.py:2180-2240`（補強迴圈）：實際迴圈主體為 `:2154-2193`（`while` 於 2154、迴圈內 finalize／validate 於 2180-2193）；判定成立、行號略有偏移。
- `plan.md:296`「不收斂保護：問題集合相同→停止」只攔「完全相同」；B2 型「11→6 遞減但仍為假陽性」不會被攔（D1 ≤1 輪寬限內尚可接受；建議 guard 同時比對「問題項目集合未縮小即停」）。

## 三題獨立判定

**(a) §8.6-7 不把「地端 vs 雲端覆蓋缺口」設為阻斷閘門——合理（有條件），不是放水。**

判準與理由：①否決權要守的決策是什麼？本波並沒有要宣告「品質對齊達成」，`§8.6-7` 明文禁止把「閘門全綠」講成「品質已達雲端水準」；②該數字的效度尚未建立——單次抽樣（temp 0.6／0.7）＋人工 probe 近似尺，把它設為全域閘門正是 rev 3 已判定過的錯誤類型（`exact_tag_ratio`：量尺缺陷造成 system-wide false FAIL）；③替代義務是否保留？有——`§8.6-5` 強制同尺公開 `coverage_core`／`coverage_all`，`§8.6-7` 缺口 >20 個百分點必須列出下一波可執行槓桿，且不得宣稱對齊。符合「影響力≠否決權；否決須有決策效度理由」。**反例邊界**（若做這些就是放水）：不量、不公開、把 D1 缺口藏起來、或用「閘門全綠」暗示品質已達標——計畫目前都明文禁止。
補一句治理提醒：本波結束時 PRIMARY_OUTCOME（品質對齊）仍是 NOT_ACHIEVED；`result.md`／對外訊息必須照 Status v2 呈現，且「下一波必須以缺口為目標並預先定義可閘門化條件（例如以 2–3 次抽樣先界定變異）」，否則『量測波』可能無限延長（PRIORITY_INVERSION）。

**(b) §8.3 bigram ≥0.75——會讓部分真遺漏不再觸發補強（有條件反對）。**

推演結論：整條刪除仍觸發（0.000），不會全面放水；但**短而關鍵的實質差異**（否定詞 0.917、日期數字 0.818、人名 0.778）會落在門檻之上被當成「已涵蓋」，與同段自訂的「不得讓真遺漏不再觸發補強」原則直接衝突；且視窗長度未定義時，跨句拼裝（0.875）也可過。驗收①②偵測不到這些類別（且 B2 兩筆大改寫假陽性其實仍會觸發補強）。最小修正見 R2。

**(c) §8.4「半開區間＋段首優先」——能修好「倒退一格」與「非冪等」，但不是無代價、也不是唯一解。**

實測（本審查以現行 HEAD 程式＋真實逐字稿重現）：①現行閉區間：C1 原始輸出 `changed=8`（含 6 筆段首倒退）、再套用一次又改 4 筆 → `f(f(x)) != f(x)`；對已交付 md 再跑也改 4 筆（非冪等成立）。②半開＋段首優先原型：對同一原始輸出 `changed=0`、對已交付 md `changed=0`、`f(f(x))` byte 相同（冪等成立）——兩個頭號缺陷確實修好。前提條件：零長度段落必須退化為單點（真實逐字稿存在 `[00:19:34-00:19:34]`）；「段首優先」必須同時套用於規則 1（同發言者）與規則 3（跨發言者）兩支，否則重疊段落仍可能再位移。代價＝R3（`end 非 start` 的良性吸附消失、新儀器下變不可回溯、D1 閘門可能壓線/偽 FAIL）；且「閉區間＋段首優先」原型證明可同時達成兩目標且保留良性吸附（changed=1、冪等、無段首倒退）——故半開非最小充分設計。

## 已收斂的前輪阻斷項（本審查查核）

- attempt-01 F1／F2／F3：**已收斂**（§1／§5 文字已與 `kept_far` 實作一致；`metric_version` 已入儀器與 W3；研究 §10.3 已改實跑值）。
- attempt-02 N1（修訂身分）：**已收斂**；rev 4 起版本單調，`git diff HEAD` 證實 rev 7 只新增 header 區塊＋`§8`，未就地覆寫 rev 6 本文。
- attempt-03 R1（修訂紀錄如實）：**已收斂**（rev 5 已實際改寫三處絕對語氣；現文 §1／§5 與實作一致）。
- attempt-04 F-C／F-D：**已收斂**（§2 R20 已分述 MoE／27B 成因與正確計數；W3 已列 `metric_version`）。
- attempt-05（判「核准」，rev 6，`sha 3b89a437…`）：M1–M4 為建議性、未列阻斷；本審查不另新增阻斷。

## 驗證成立的正面事實（本次查核）

- R23 前提為真：紀錄側於驗證**前**先過 `finalize_record`（含 `to_taiwan_traditional`，HEAD `text_postprocess.py:466`）——`summarization.py:2138-2151`；筆記側僅 `_normalize_action_key`（`:914-916`，無簡繁折疊）；`merged_notes` 進迴圈後固定（`:2104`、`:2154-2193`）；每輪為整份紀錄重生成。
- R24 前提為真：HEAD `text_postprocess.py:868/876` 閉區間＋取 `[0]`；實測 8 筆改寫、6 筆段首倒退、二次套用再 4 筆（非冪等），與 C1 §5.1–5.3 相符。
- R25 前提為真：C1 `verify_independent.md` §5.4 逐字 grep 的缺口清單（省員／小秘書／嘉義／選舉／視察／排水管／露臺／藤蔓／李飛）與「五閘門全綠」並存。
- 「既有測試抓不到」為真：`tests/test_platform_provider_routing.py:204-209` 只做「同輸入跑兩次」（決定性），無 `f∘f`。
- 雲端不變性條款（`§8.6-6`）與 fail-soft 設計一致（`transcript=None` 或非 `speaker_traceability` 模板即完全不作用）。
- CORE-3 的「不用第二個 LLM、雙向對稱正規化、byte 級可重現、`warnings`＋`metric_version`」設計與「模型無關／跨平台」需求一致，未發現 gold-plating。

## 收尾

- 開審量測：`59cf852a…53f7a`（2026-09-23 00:33 CST）；收審複量（本檔寫入後）：同值（見同目錄 `snapshot_plan.sha256`）。
- 本審查未修改任何其他檔案；`review/attempt-06/` 僅含本檔與 `snapshot_plan.sha256`。

## 判決

PLAN_REVISION_REQUIRED
