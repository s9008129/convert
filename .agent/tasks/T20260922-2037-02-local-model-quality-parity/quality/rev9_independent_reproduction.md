# P3 rev 9 獨立重現與對抗性驗證報告（fresh context；read-only for 產品碼）

> 角色：獨立重現與對抗性驗證者（fresh context）。**未採信**既有報告（`quality/*.md`、`review/*`、前一輪工作總結）的數字；
> 本報告所有數字皆為 2026-09-23 在本機重跑取得（探針腳本見 `/tmp/rev9fp/`，repo 內無任何暫存檔）。
> 環境：repo `/Users/hsiaojohnny/dev/convert`，branch `fix/qwen-local-quality-parity`，HEAD `c298cc8`；
> 未修改任何產品碼／測試／`plan.md`／`review/*`／`doc/*`；未執行任何 git 寫入；未干擾平行中的 D1 E2E、LM Studio（127.0.0.1:1234）與 port 9527。
> 唯一寫入＝本檔（append-only 新檔）。
> 統一執行模板：`cd /Users/hsiaojohnny/dev/convert && DATA_DIR=/tmp/probe_scratch uv run --frozen python /tmp/rev9fp/<probe>.py`（探針內 `sys.path.insert(0, "/Users/hsiaojohnny/dev/convert")`，直接 import repo 真函式）。

---

## ★ FALSIFIED 醒目段落（先讀這裡）

**核心宣稱沒有翻車**：規則 0 的有效性（A1 全域段首被改 11→0）、真實素材冪等、嚴格整條刪除 14/14 判遺漏、守衛對真實素材 0 額外假陽性、
不收斂保護生效——以上全部 `[REPRODUCED]`。以下 5 條 `[FALSIFIED]` 全部屬**宣稱邊界**（宣稱過寬或實作與描述不符），無 CORE 級翻車。

**F1｜宣稱過寬：吸附冪等不涵蓋 HH:MM（2 欄）精度標註**（plan §8.4「保證 `f(f(x)) == f(x)`」）。
最小重現：
```
cd /Users/hsiaojohnny/dev/convert && DATA_DIR=/tmp/probe_scratch uv run --frozen python - <<'PY'
import sys; sys.path.insert(0, "/Users/hsiaojohnny/dev/convert")
import backend.core.text_postprocess as tp
from backend.core.templates import get_template
T = get_template("section_meeting")
TR = "[00:23:20-00:24:05] 發言者1：前段。\n[00:24:11-00:26:40] 發言者1：後段。\n"
o1, _ = tp.snap_source_tags_to_transcript("（發言者1，00:25）", TR, template=T)
o2, _ = tp.snap_source_tags_to_transcript(o1, TR, template=T)
print(o1, o2)   # 實測 → （發言者1，00:24） （發言者1，00:23）＝非冪等
PY
```
量級：隨機 4000 組（一半含 2 欄）→ **93 組非冪等**；只含 HH:MM:SS 的 2000 組 → **0 組**。真實四素材 2 欄標註 = **0 筆**（全為 3 欄），故對本波素材無實質影響，但宣稱缺「精度前提」。

**F2｜`_normalize_action_key` 對稱性描述與實測不符（標點折疊不完整；簡繁不折疊）**。
最小重現：`cd /Users/hsiaojohnny/dev/convert && DATA_DIR=/tmp/probe_scratch uv run --frozen python /tmp/rev9fp/b4b5_guard.py`（首段輸出）：
`S._normalize_action_key("（稽查股、征收股）") != S._normalize_action_key("稽查股征收股")`（`、` 不折疊）；
`征收股` vs `徵收股` 亦不相等（docstring 已聲明靠 LCS 兜底 0.9412，此半屬如實界線）。
不折疊集合實測：`、！？…—·．.`；折疊集合：`，。；／/（）「」『』【】[]：,` 與 `\s\r\n-`。

**F3｜守衛「失真不會被洗白」不成立（B6 反例清單多項洗白）**。
最小重現（逗號子句否定詞；同句、不同子句仍在切句後同句）：
```
cd /Users/hsiaojohnny/dev/convert && DATA_DIR=/tmp/probe_scratch uv run --frozen python - <<'PY'
import sys; sys.path.insert(0, "/Users/hsiaojohnny/dev/convert")
from backend.services.summarization import SummarizationService as S
HEAD = "# 0903-科務會議紀錄\n時間：114年9月3日 10:00\n地點：3樓會議室\n主持人：科長\n出席人員：全體同仁\n\n## 壹、決議事項\n"
rec = HEAD + "1.嚴禁酒後開車，科內群組訊息、照片可外流至外部群組或平台（如LB）。（發言者1，00:10:00）\n"
key = "嚴禁將科內群組訊息、照片外流至外部群組或平台（如LB）"
nk = S._normalize_action_key(key); ns = S._normalize_action_key(rec)
missing, ratios = S._find_missing_action_keys({nk}, ns, local_sentences=S._split_record_sentences(rec))
print(ratios, missing)   # 實測 ratio 0.6000、missing=set()＝洗白（目標子句已被反轉）
PY
```
洗白清單：`毋須→須` 0.8333／`不宜→宜` 0.8333／`免→須` 0.7500／`不建議→建議` 0.9000／`17→117` 0.8889／`800→8000` 1.0000／
`9月30日→10月30日` 0.8333／國字 `一萬三千六百→一萬三千七百` 0.9333／`3.5%→5.5%` 0.8889（全部詳見 B-6）。

**F4｜`_action_key_best_window` 描述「最佳連續視窗」但實作回傳「迭代序第一個 ≥0.6 的窗」（非最大值）**。
實測（`/tmp/rev9fp/window_scan.py`、`/tmp/rev9fp/window_pick.py`）：K2 別子句案例存在 ≥0.6 的兩個窗——`r=0.8462`（含「嚴禁」）與 `r=0.6923`（不含）；
實作回傳 `0.6923`（`win='駕科內群組訊息得轉傳至外部發言者1'`）。同一機制反向也成立：忠實改寫「嚴禁將科內群組訊息外流至外部渠道」被回傳窗 `'禁將科內群組訊息外流至外部渠道發言'`（起於「禁」字）誤攔（0.7692）。
→ 「log 所述最佳 LCS 比例」可能低估真最大值；守衛的嚴/鬆部分取決於窗邊界（現行 CPython 下為確定性——候選集合是 int set，已用 `PYTHONHASHSEED=0/1/2/3/5/7/9/11/13/42/99/12345` 驗證跨進程一致）。

**F5｜簡體 key 可繞過否定詞守衛**（行級簡體偵測缺口，`to_taiwan_traditional` 不轉含「严」但無其他可偵測簡體字的行）。
最小重現：`/tmp/rev9fp/b4b_simp.py` → `key='严禁轉傳科內群組訊息至外部'`（未折疊）＋紀錄「將科內群組訊息外流至任何外部渠道…」→ **洗白 ratio 0.6923**；
同情境改用正體 `key='嚴禁轉傳科內群組訊息至外部'` → **判遺漏**（守衛啟動）。機制底層（行級偵測）是既有程式，守衛本身是本波新引入。

---

## 0. 共通方法、素材與對照版本

* 素材紀錄（`data/cache/e2e/<dir>/backend_data/outputs/*.md`，sha256 前 16 字元實測）：
  * `p2-27b-fix-01/…_dc3c8f7a.md` → `b0ac8563c190e0c6`（tags=52）
  * `p2-gemma31b-fix-01/…_ac1edcec.md` → `7d2de68463ada245`（tags=20）
  * `p2-moe-fix-01/…_0cc199da.md` → `94d2f96d1c0960c2`（tags=14）
  * `p2-27b-01/…_f012e80c.md`（A1 素材，修復前產物）→ `d48913472857df0c`（tags=61）
* 逐字稿：四目錄 `transcript.txt` 內容相同（sha256 前 16：27b-fix-01/A1 皆 `bd6b52d7c0392f6a`；gemma `199d37b5a2ecdfd2`；moe `667365a479887fca`）；每份 **183 段**。
* 對照版本重建（**未改任何檔案**）：以 `inspect.getsource` 取 `snap_source_tags_to_transcript` 原文→
  * `rev8(無規則0)`＝機械移除規則 0 區塊（`if any(seg[0] == seconds …): kept_on_start … return tag`）；
  * `v1.0(全舊)`＝再去掉段首優先，monkeypatch `_pick_containing_segment` 為 v1.0「閉區間取第一個命中」。
  * 舊版判定對照（B 節）：`git show b3db34a:backend/services/summarization.py`（唯讀）確認 v1.0 只有 `missing = {key for key in expected if key not in normalized_summary}`＝逐字子字串比對，**無 LCS、無守衛**。
* 本節所有探針（皆為本次重跑）：`a1_snap.py`、`a2_discrim.py`、`a3_boundary.py`、`a3c_tripwire.py`、`a3d_idem_hhmm.py`、`a3e_rand.py`、
  `precision_count.py`、`b4b5_guard.py`、`b4b_simp.py`、`b5self.py`、`b5del3.py`、`b5del2.py`、`b5del_tamper.py`、`b5fp.py`、`b6b_embed.py`、`b6c_embed.py`、
  `clause_sep.py`、`window_scan.py`、`window_pick.py`、`hashseed_check.py`、`faithful_fp.py`、`b7_convergence.py`。

---

## A. 標註吸附（`backend/core/text_postprocess.py`）

### A-1 四素材統計 ＋ 二次套用冪等

* **宣稱**：三份修復後紀錄 `changed=0`、`kept_on_start=52/20/14`、二次套用 byte 相同；A1 素材 `changed=3`、`kept_on_start=58/61`、`backward_moves=3`、`max_backward_seconds=70`；五素材二次套用 byte 相同。
* **方法**：`cd /Users/hsiaojohnny/dev/convert && DATA_DIR=/tmp/probe_scratch uv run --frozen python /tmp/rev9fp/a1_snap.py`（真函式 `snap_source_tags_to_transcript` 套用兩次＋`measure_tag_traceability`）。
* **原始輸出**（本次重跑，`/tmp/rev9fp/rerun_a1.txt`）：

| 素材 | tags | changed | snapped | kept_on_start | backward_moves | max_bwd(s) | forward | kept_far | untraceable | 二次 byte 相同 | 二次 changed |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 27B(fix) | 52 | 0 | 0 | 52 | 0 | 0 | 0 | 0 | 0 | True | 0 |
| Gemma(fix) | 20 | 0 | 0 | 20 | 0 | 0 | 0 | 0 | 0 | True | 0 |
| MoE(fix) | 14 | 0 | 0 | 14 | 0 | 0 | 0 | 0 | 0 | True | 0 |
| A1(27b-01) | 61 | **3** | 3(全 snapped_exact) | **58** | **3** | **70** | 0 | 0 | 0 | True | 0 |

  * A1 `changed_values` 實測＝`[[1521,1451],[1521,1451],[1521,1451]]`（三筆同一時間戳 `00:25:21→00:24:11`；該值＝某段 `end` 且非任一 `start`）。
  * A1 量尺：`on_start_tag_ratio 0.9508→1.0`；`exact_tag_ratio 0.7541→0.8033`。
* **判定：`[REPRODUCED]`**（五素材二次套用 byte 相同、二次 `changed=0`；A1 六個數字全部逐位重現）。

### A-2 鑑別力（monkeypatch；不改檔）

* **宣稱**：rev9「原值已是全域真實段首卻仍被改寫」=0；v1.0 對照合計 23 筆（5 素材）；rev8（無規則 0）在 A1 為 12/9。
* **方法**：`… uv run --frozen python /tmp/rev9fp/a2_discrim.py`（`inspect.getsource`＋字串替換重建變體，各自獨立 namespace）。
* **原始輸出**（本次重跑，`/tmp/rev9fp/rerun_a2.txt`；`gsw`=全域段首被改筆數）：

| 素材 | rev9 changed/gsw | rev8(無規則0) changed/gsw | v1.0序+規則0 changed/gsw | v1.0 全舊 changed/gsw/backward |
|---|---|---|---|---|
| 27B | 0 / 0 | 0 / 0 | 0 / 0 | 4 / 4 / 4 |
| Gemma | 0 / 0 | 0 / 0 | 0 / 0 | 4 / 4 / 4 |
| MoE | 0 / 0 | 0 / 0 | 0 / 0 | 3 / 3 / 3 |
| A1 | 3 / **0** | **12 / 9** | 3 / 0 | **14 / 11** / 14 |
| fixture 跨發言者交界 | 0 / 0 | 1 / 1 | 0 / 0 | 1 / 1 / 1 |
| fixture 段界(額外) | 0 / 0 | 0 / 0 | 0 / 0 | 1 / 1 / 1 |

  * TOTAL：`v1.0_full gsw=24`（前五素材＝23，與 plan 記載一致；第 6 個 fixture 為我另加）、`changed=27`；`rev8 gsw=10`；`rev9 gsw=0`。
* **判定：`[REPRODUCED]`**。如實註記：rev8（保留段首優先、無規則 0）在 27B/Gemma/MoE 上 `changed=0` → **規則 0 的鑑別力只能靠 A1（12/9→3/0）與跨發言者 fixture（1/1→0/0）證明**；這與 plan「規則 0 是跨發言者交界與冪等的必要條件」一致。

### A-3 邊界案例 ①–⑨（每項：rev9 vs v1.0 → 四項絆索反應 → 判定）

絆索操作化（§8.6-4）：①二次套用 `changed==0` 且 byte 相同；②`global_start_rewritten==0`；③`max_backward_seconds ≤120`（`TAG_SNAP_MAX_SHIFT_SECONDS` 上限）；④`on_start_tag_ratio ≥0.95`。
方法：`… python /tmp/rev9fp/a3_boundary.py`（本次重跑與前次 `a3_out.txt` 除 log 行外 byte 相同）、`a3c_tripwire.py`、`a3d_idem_hhmm.py`、`a3e_rand.py`。

**① 同一秒被兩段共用（不同發言者）**
* ①-a1 `tag=發言者2@00:00:10`（=兩段共用段首，但段首「屬於」兩段）：rev9 `kept_on_start=1, changed=0`；v1.0 `snapped=1, changed=0`（同值）→ 無差異。四項不亮。
* ①-a2 `@00:00:12`（落在兩段內、非段首）：rev9 與 v1.0 **完全相同**（snap 到 10s，`backward=1, max=2`）。四項不亮。
* ①-b 跨發言者交界（前段 end＝後段 start，`tag=發言者1@00:00:20`）：rev9 `kept_on_start=1`；v1.0 **倒退 10s**（`gsw=1`）。→ 支撐絆索②。
* ①-c 重疊段（`tag=發言者2@00:00:10`，其段 12s 才開始）：rev9 `kept_on_start`；v1.0 **前進 2s**（`forward=1`）。
* **判定：`[REPRODUCED]`**（行為如實，與 plan「規則 0 保護的是『已是段首』的事實、不是內容歸屬」一致）。**v1.0 既有**（①-b 類後退為 rev9 修正對象）；不影響四項絆索（各案例皆不亮）。

**② 零長度段落（`end == start`）／倒置段**
* ②-a `tag@ 零長段 start`：rev9 `kept_on_start`（v1.0 `snapped`、同值）→ byte 無差。②-b 附近秒數：兩版同（倒退 2s）。
* ②-c 倒置段 `[00:01:00-00:00:30]`：`iter_transcript_segments` 會 **swap** 成 `[30,60]` → `tag@00:00:45` 落空 → `untraceable=1`（兩版相同）→ **絆索④亮**（on_start=0.0）。
* **判定：`[REPRODUCED]`**；①-a 的零長段退化（`_segment_contains` 回 `seconds == start`）為本波新程式、但兩版輸出無差；倒置段 swap 為**既有機制**。非回歸。

**③ 重複的段首**（同發言者兩段同起點）
* `tag@00:00:15`：兩版都吸到**第一個**重複段首（`backward=5`）；`_pick_containing_segment` 對 `start==seconds` 回傳清單中**第一個**。四項不亮。
* **判定：`[REPRODUCED]`**；**v1.0 既有**（順序敏感為兩版共有），rev9 未改此細節、無回歸。

**④ 段落時間表亂序**
* ④-a/④-b：兩版相同（各倒退 30s）。④-c 兩段間空隙（tie）：`min()` 取清單序第一個 → **前進 120s**；兩版相同。四項不亮。
* **判定：`[REPRODUCED]`**；**v1.0 既有**（tie-break 依清單順序），非回歸。

**⑤ 標註精度（MM:SS / HH:MM）**
* ⑤-a `tag=25:21`（MM:SS 意圖）：解析為 **25 小時 21 分** → `untraceable=1`（兩版相同）→ **絆索④亮**（不可回溯標註、非回歸；解析器不支援 2 欄位秒為既有設計）。
* ⑤-b `tag=00:25`（真段首）：rev9 `kept_on_start`；v1.0 `snapped`（同值）。
* ⑤-c `tag=1:43:00`（1 位小時）：rev9 原樣保留；v1.0 `changed=1`（字串正規化 `1:43:00→01:43:00`，**秒數不變**）。
* **判定的重點在下面：**

**⑤-冪等反例（★F1）**
* 方法：`… python /tmp/rev9fp/a3d_idem_hhmm.py`、`/tmp/rev9fp/a3e_rand.py`、`/tmp/rev9fp/precision_count.py`。
* 原始輸出：`tag=（發言者1，00:25）`、逐字稿 `[00:23:20-00:24:05]/[00:24:11-00:26:40]` → **1st `00:24` → 2nd `00:23` → 3rd `00:23`**（`f∘f ≠ f`）；
  隨機 4000 組（一半 2 欄）→ **93 組非冪等**（含實例：`00:03→00:01→00:00`）；純 3 欄 2000 組 → **0 組**；
  四素材 2 欄標註實測 **0 筆**（27B 52／Gemma 20／MoE 14／A1 61 全為 3 欄）。
* 機制：落點以 `_seconds_to_hms(..., with_seconds=False)` 截斷（`00:24:11→00:24`），下一輪 `00:24` 解析為 1440s ≠ 段首 1451 → 再吸到前一段首；`with_seconds` 截斷與 `_seconds_to_hms` 皆為**既有程式**（`git diff b3db34a..HEAD` 中為 context 行）→ **v1.0 既有機制、rev9 未修**；但 plan §8.4 的冪等宣稱**未限定精度** → 宣稱層 `[FALSIFIED]`。
* 如實註記：`a3d` 內建的窗掃描回報「非冪等 0 例」是**掃描涵蓋盲區**（其段落窗構造為 `:05–:20`，不含本反例的跨分界 `:20–:05` 型）；真反例由手動案例＋`a3e` 隨機證實。

**⑥ 標註發言者標籤不在逐字稿**
* ⑥-a「科長@00:00:15」（時間存在、標籤不存在）：兩版同（snap 到 10s；`inside_same=0`）。⑥-b「科長@段首」：rev9 `kept_on_start`；v1.0 同值。
* ⑥-c「發言者9@00:30:00」（遠處）：`untraceable=1`（兩版相同）→ **絆索④亮**（合理：不可回溯）。
* **判定：`[REPRODUCED]`**；**v1.0 既有**（fail-soft 路徑相同），非回歸。

**⑦ 空逐字稿／無段落**
* ⑦-a 空 transcript、⑦-b 只有「發言者統計」列（刻意不算段落）：`snap` 不改寫任何標註；`on_start=0.0` → **絆索④亮**（分母是「有標註但無段落」的輸入）。⑦-c 空紀錄：指標 `None`、四項不亮。兩版相同。
* **判定：`[REPRODUCED]`**（fail-soft 如實）；**v1.0 既有**。絆索④在此類輸入上的亮燈屬結構性，不是回歸（該類輸入量尺本無意義）。

**⑧ 時間戳恰為某段 end 且非任何 start**
* ⑧-a 位移 70s（A1 同型）→ snap（`backward=70`；兩版相同）；⑧-b 位移 120s（上限）→ 仍 snap；⑧-c 121s → `kept_far=1`（不吸收）→ `on_start=0.0` → **絆索④亮**。
* **判定：`[REPRODUCED]`**；**v1.0 既有**（`TAG_SNAP_MAX_SHIFT_SECONDS`/`kept_far` 非本波新增）。如實註記：**絆索④有一個結構性誤報來源＝`kept_far`**（合法但 >120s 的位移刻意保留原值 → 不在段首 → 亮燈）；解讀④時必須確認素材本身可回溯。

**⑨ 大量標註（>50）冪等**
* 200 筆合成標註：rev9 `changed=16, kept_on_start=0, backward=13, max_bwd=113, forward=3, kept_far=4, untraceable=180`、**冪等 True、二次 changed=0**；v1.0 同值。→ **絆索④亮**（合成素材 180/200 時間戳不在段落＝假象）。
* **判定：`[REPRODUCED]`**（冪等在該批成立）；④亮為素材假象，需如實註記。

**絆索鑑別力對照（a3c）**：破壞規則 0 / 用 v1.0 順序時——
* 27B/Gemma v1.0：**冪等 False、二次 changed=4** → **絆索①亮**；A1 rev8：**全域段首被改 9** → **絆索②亮**；fixture rev8：全域段首被改 1 → ②亮。
* **但絆索③（幅度）與④（on_start）在這些破壞下都不亮**（v1.0 的 `max_bwd` 7–72 全 ≤120；破壞後 `on_start` 仍 1.0000）。
* **判定：`[REPRODUCED]`**＋如實註記：四項中**只有①與②具實際鑑別力**；③是「觀察值」定位、④是既有閘門（且受 kept_far／不可回溯素材影響會亮）。這點應寫進對外表述，避免誇大「四項絆索」的防護力。

---

## B. 待辦召回守衛（`backend/services/summarization.py`）

版本事實（唯讀 `git show b3db34a:...`）：v1.0 的判定＝**逐字子字串**（`key not in normalized_summary`→遺漏）→ LCS 滑窗（0.6）、兩守衛（否定詞／≥2 位數字）、局部判定（最佳窗＋最相近句）、不收斂保護 **全部是本波新引入**。以下「v1.0 既有／rev 9 新引入」皆依此定義。

### B-4 `_normalize_action_key` 對稱性

* **方法**：`… python /tmp/rev9fp/b4b5_guard.py`（B4 段）、`/tmp/rev9fp/b4b_simp.py`。
* **原始輸出**：NFKC 全形數字「１３６００元」→`13600元`、全形英數「ＥＭＡＩＬ」→`email`、千分位「13,600元」→`13600元`、`甲\r\n乙`→`甲乙`、`A(1)/B`→`a1b`、空白/全形空白→全折疊：**相等=True**。
  簡繁「征收股」vs「徵收股」：**相等=False**（`征收股`→`征收股`；`徵收股`→`徵收股`）。
  標點「（稽查股、征收股）」vs「稽查股征收股」：**相等=False**（`、` 不折疊）。折疊實測：`，。；／/（）「」『』【】[]：,`；**不折疊**：`、！？…—·．.`（`！？…` 僅 NFKC 變 `!?...`）。
  簡體「严」行：`严禁轉傳科內訊息`→**不轉**（未偵測到）；`严禁转发 message 提醒`→`嚴禁轉發message提醒`（有轉）→ 同一份文字、兩側折疊不一致。
  行級缺口後果（`b4b_simp.py`）：`key='严禁轉傳科內群組訊息至外部'`＋紀錄「將科內群組訊息外流至任何外部渠道」→ **洗白 ratio 0.6923**；改正體 key→**判遺漏**。
* **判定**：NFKC／千分位／CRLF／空白／半形括號斜線 → `[REPRODUCED]`；**標點折疊完整性與「兩側走同一條」的宣稱 → `[FALSIFIED]`（F2）**；簡繁為 docstring 已聲明的界線（靠 LCS 兜底 0.9412，實測走 0.6 門檻）→ `[REPRODUCED]`（如實界線）。注意：`tests/test_t20260922_2037_p3_parity.py:192` 用簡體「征收股」但未斷言正規化相等，宣稱只存在於 docstring。

### B-5 三種判定（真實素材）

* **方法**：`b4b5_guard.py`（B5①）、`b5del3.py`（②嚴格版）、`b5del2.py`＋`b5del_tamper.py`（③tamper）、`b5self.py`（額外假陽性）、`b5fp.py`（忠實改寫假陽性）。
* **① 已涵蓋 → 不誤判**：key＝三份 `backend.log` 的「待辦事項遺漏 N 項：…」清單原文共 **14 筆**（27B 11／Gemma 1／MoE 2）。
  原始輸出：14/14 **涵蓋**；ratio 分布＝`0.6923, 0.7195, 0.8750, 0.9412, 0.9412, 0.9524, 1.0×8`；**守衛判定與「僅 LCS」判定完全一致（差異 0 筆）**。
  → **判定：`[REPRODUCED]`**（真實素材上守衛零誤判；低於 1.0 的 5 筆即 LCS 近似吸收的改寫）。
* **② 整條刪除 → 必判遺漏**：嚴格版（反覆刪除逐行 LCS≥0.5 的行直到無 ≥0.5 殘留）→ **14/14 判遺漏**，刪後 ratio `0.1765–0.4706`。
  如實註記：**只刪「最相近一行」的版本只有 7/14**——原因是紀錄中同一項目常出現兩處（例：「盤點多元支付現狀…」在 27B 紀錄出現 2 次，L26/L99；「了解印花稅…」單刪後仍 ratio 0.6190），殘留近似句讓 ratio 仍 ≥0.6。這是**測試設計問題、不是缺陷**：守衛語意是「紀錄任何一處寫到即算涵蓋」，要驗整條刪除必須移除全部出現。
  → **判定：`[REPRODUCED]`**（以嚴格版為準）。
* **③ tamper（全量取代，避免殘留）**（`b5del2.py`）：
  * `嚴禁→得`（全量）ratio 0.6154 → **判遺漏 ✓**；`嚴禁→嚴加禁止` → 判遺漏；`嚴禁→禁止`（同義）→ **判遺漏（守衛假陽性，已知代價）**。
  * MoE：`10月14日→10月24日` ratio 0.6279 → **判遺漏 ✓**（2 位數守衛攔下，R2 反例類）。
  * `報3件→報5件` ratio 0.6279 → **洗白**；`報3件→報13件` ratio 0.6047 → **洗白**（1 位數不在 `\d{2,}`；`13` 亦未觸發——「3」被改、「13」超字串）。
  → **判定：`[REPRODUCED]`（含界線）**：≥2 位數變更必攔；1 位數／超字串變更洗白（詳見 B-6、缺陷 N5）。
* **額外假陽性（守衛造成、LCS 已達門檻）**：
  * 真實 key（14 筆）：**0 筆**。真實素材自身表格列標籤餵回自身（57 筆：27B 18／Gemma 8／MoE 11／A1 20）：**0 筆**（含 2 位數 token 樣本「11月1日」「10月14日」「90幾歲」皆未被誤判）。
  * 真實 key＋**忠實改寫**（`b5fp.py`）：守衛造成 **2 筆**——`嚴禁→不得` ratio 0.9048、`10月14日→十月十四日` ratio 0.9070；（語序調換 0.5238 判遺漏屬 LCS<0.6、非守衛；`10/14` 0.9535 涵蓋；`一萬三千六百→13,600` 0.6000 恰壓門檻涵蓋）。
  → **判定：`[REPRODUCED]`**；「真實素材上守衛假陽性 0」成立，但「忠實改寫必不誤攔」不成立（方向往更嚴，成本＝一輪補強）。

### B-6 主動反例：哪些寫法讓真失真被洗白（＋判遺漏）

* **方法**：`b6b_embed.py`（真實紀錄樣式包夾）、`b6c_embed.py`、`clause_sep.py`、`window_scan.py`、`window_pick.py`、`b4b_simp.py`。
* **① 否定詞在同句、不同子句**：`K1=嚴禁將科內群組訊息、照片外流至外部群組或平台（如LB）`
  * 紀錄「嚴禁酒後開車，科內群組訊息、照片可外流至外部群組或平台（如LB）。」→ **洗白 ratio 0.6000**；
  * 句尾變體「…（如LB），惟嚴禁酒後開車。」→ **洗白 0.8800**；無否定詞的反轉對照 → 判遺漏 0.8800。
  * 分隔符敏感（`clause_sep.py`，同句同詞、ratio 皆 0.6000）：`，`→**洗白**；`；`／`。`／`！`／換行→**判遺漏**（`_split_record_sentences` 切句後最相近句不含否定詞）。
  * `K2=嚴禁轉傳科內群組訊息至外部`：「嚴禁酒駕，科內群組訊息得轉傳至外部。」（別子句）→ 判遺漏 0.6923；「…另嚴禁酒駕。」（別句）→ 判遺漏 0.6923。
* **② 否定詞緊鄰／遠離**（`b6c_embed.py`）：遠（別句）→判遺漏 0.6923；緊鄰同句反轉「科內群組訊息嚴禁不適用，得轉傳至外部」→判遺漏 0.5385；緊鄰括號→判遺漏 0.6923；同義替換「禁止轉傳…」→**判遺漏 0.9231（FP）**；對照語意保留「嚴禁將科內群組訊息外流至外部渠道」→**判遺漏 0.7692（FP；回傳窗起於「禁」不含「嚴禁」——F4 機制）**。
* **③ 國字數字／子字串**：`一萬三千六百→一萬三千七百` **洗白 0.9333**；`17→117` **洗白 0.8889**；`17→170` **洗白 0.8889**；`800→8000` **洗白 1.0000**。
* **④ 小數／百分比**：`3.5%→5.5%` **洗白 0.8889**；`0.5%→0.8%` **洗白 0.8889**。
* **⑤ 日期 `/` `.`**：`9/30→9/20` **判遺漏 0.9000**（`/` 折疊 → `930` 成 token）；`9.30→9.20` **判遺漏 0.9091**（`30` token 仍在、`.` 不折疊）；但 `9月30日→10月30日` **洗白 0.8333**（月份被改、`30` 仍在）；`9月3日→9月8日` **洗白 0.9091**（1 位數）。
* **⑥ 金額加「約」**：`約13600元→約一萬多元` **判遺漏 0.6667**（嵌入版；非嵌入原版 0.4444）✓；`約800元→約8000元` **洗白 1.0000**；`約13600元→約1360元` 判遺漏 0.8889；國字忠實「約一萬三千六百元」判遺漏 0.4444（FP 代價）。
* **⑦ 詞表外否定詞**（真實嵌入）：`毋須→須` **洗白 0.8333**；`不宜→宜` **洗白 0.8333**；`免→須` **洗白 0.7500**；`不建議→建議` **洗白 0.9000**；詞表命中對照：`請勿→請` 判遺漏 0.8750（`勿` 子字串）、`避免→∅` 判遺漏 0.7778。
* **視窗選擇與確定性（F4）**：`window_scan.py` 對 K2 別子句列出候選 ≥0.6 窗＝2（含／不含「嚴禁」各一），`window_pick.py` 證實**實際回傳不含者（0.6923）**；`PYTHONHASHSEED=0/1/2/3/5/7/9/11/13/42/99/12345` 十二個種子下回傳窗與判定**完全一致**（候選集合為 int set，非 str hash）。
* **判定**：①③④⑤⑦ 多項洗白 → **`[FALSIFIED]`（F3）**；⑤（`/`、`.` 寫法）與⑥（改寫稀釋）→ `[REPRODUCED]`（守衛有效）；②同義替換 FP → `[REPRODUCED]`（plan §8.7 已列已知代價）；窗選擇「最佳」措辭 → **`[FALSIFIED]`（F4）**。

### B-7 不收斂保護

* **方法**：`… python /tmp/rev9fp/b7_convergence.py`（假引擎、真管線 `_summarize_with_local_pipeline`；`settings.LOCAL_LLM_MAX_REFINEMENT_ROUNDS=2`；`_validate_summary_quality` 以腳本化問題集合餵入）。
* **原始輸出**（本次重跑）：
  * 問題集合完全不變（同輪相同）→ generate 呼叫 3（萃取1+初稿1+補強**1**）／驗證呼叫 2／**補強輪數 1**，log 出現 `本地摘要補強未收斂（問題集合與上一輪相同，共 1 項）→ 停止再補強`。
  * 三輪皆同 → 同上（1 輪）。問題集合每輪遞減（A、B、C→A、B→A）→ 2 輪（跑滿上限，真的在進展）。第1→2輪改變、之後相同 → 2 輪（第 2 輪是上限輪，保護無機會再攔）。集合相同但順序不同 → **1 輪**（`tuple(sorted(issues))` 簽章吃順序 → 判定相同）。
* **判定：`[REPRODUCED]`**（「問題集合完全相同的下一輪不會再跑」成立；保護語意＝「上一輪之後問題集合未變即停」，最多多跑 1 輪）。

---

## C. 新發現的缺陷清單（v1.0 既有／rev 9 新引入 ＋ 是否應阻斷本波）

> 判準：**阻斷＝核心目標失效或資料正確性風險在真實素材上已發生**。本波核心（規則 0 有效、真實素材冪等、守衛真實素材 14/14 攔截＋0 假陽性、不收斂保護）全部成立 → 以下均**不阻斷**，但逐項給出建議去處。

* **N1｜2 欄（HH:MM）精度下吸附非冪等（93/4000 隨機反例；`00:25→00:24→00:23`）。**
  分類：**v1.0 既有機制**（截斷回寫為舊程式）；**rev 9 宣稱覆蓋過寬**（§8.4 無條件寫「保證 `f(f(x)) == f(x)`」）。
  絆索影響：絆索①在含 2 欄標註的輸入會亮。真實四素材 2 欄＝0 筆 → 對本波證據無影響。
  是否阻斷：**否**。理由：不影響本波任何素材與 D1 宣稱，但**建議本波文件立即加註精度前提**（或列下一波：2 欄標註落點回寫成「不吸附／改寫成 3 欄」二選一）。若不加註，對外「冪等保證」屬過寬陳述。
* **N2｜否定詞在「同一句、逗號相連的子句」時洗白（0.6000/0.8800）；以 `；`/`。`/`！`/換行相連則攔下。**
  分類：**rev 9 新引入機制的盲區**（v1.0 逐字比對不會洗白此類，只會大量誤判）；plan §8.7 已知風險②（「緊鄰＋落在最佳視窗內」）的**具體化且更寬**（不限於緊鄰視窗，只要同逗號句）。
  是否阻斷：**否**（已知風險、方向複雜需子句/語意層）；**列下一波第一優先**（成本槓桿大：官方紀錄的反轉失真）。
* **N3｜詞表外否定詞洗白：`毋須/不宜/免/不建議`（0.75–0.90）；詞表內有效。**
  分類：**rev 9 新引入守衛的表覆蓋缺口**（非回歸；v1.0 無守衛、但也不會「判定涵蓋」）。
  是否阻斷：**否**；但**建議列本波後續小修**（詞表純資料變更、風險低）：加入 `毋須/無須/不宜/不建議/免/不必/不可/嚴禁` 等（注意「免」可能誤傷「免費」，需詞邊界或以 2 字詞加入）。
* **N4｜數字守衛子字串語意：`17→117`、`800→8000` 洗白。**
  分類：**rev 9 新引入**（`token in text` 為子字串比對）。
  是否阻斷：**否**；列下一波（改為 token 邊界比對：前後不得緊鄰數字）。
* **N5｜數字守衛盲區：1 位數（`報3件→報5件` 0.6279）、國字數字（0.9333）、小數/百分比（0.8889）、`9月30日→10月30日`（0.8333）。**
  分類：**rev 9 新引入**（設計上「≥2 位」為已知取捨，但「月份被改」「國字」「小數」未列已知清單）。
  是否阻斷：**否**（真實素材此類樣本少）；列下一波（國字數字↔阿拉伯數字正規化、1 位數 token 白名單、日期整組比對）。
* **N6｜簡體 key 繞過否定詞守衛（`严禁…` 未折疊 → 洗白 0.6923；正體 key 判遺漏）。**
  分類：**rev 9 守衛的新輸入 robust 缺口**＋底層 `to_taiwan_traditional` 行級偵測為**既有機制**。
  是否阻斷：**否**（key 來自萃取筆記，簡體字機率低）；列下一波（key 側改用字元級簡繁表或與量尺同一條正規化）。
* **N7｜`_action_key_best_window` 回傳「迭代序第一個 ≥0.6 窗」而非最大窗（F4）：K2 回傳 0.6923（不含否定詞）而 0.8462（含）存在；反之忠實改寫被窗邊界誤攔（0.7692）。**
  分類：**rev 9 新引入**（docstring 稱「最佳連續視窗」與實作不符）。現行 CPython 下跨進程確定（int set；12 個 hash seed 實測一致）→ 不影響「同輸入同判定」，但 (a) log／校準所述「最佳 LCS 比例」可能低估、(b) 守衛的嚴/鬆部分仰賴窗邊界的偶然。
  是否阻斷：**否**；列下一波（改為真正 max 或子句級判定；在此之前，校準文件加註「比例為首個達標窗」）。
* **N8｜絆索④的結構性誤報來源：`kept_far`（>120s 合法位移保留）＋不可回溯標註（MM:SS、空/無段落、遠處標籤）皆讓 `on_start_tag_ratio` 降到 0.95 以下。**
  分類：**v1.0 既有機制**（`kept_far`、解析器）＋本波量尺沿用。
  是否阻斷：**否**；建議解讀規則：④亮燈只作「查核線索」，判讀時先確認素材本身可回溯（本波 D1 需附此事實）。
* **N9｜「四項絆索」的實際鑑別力不對稱：①（冪等）與②（規則 0）在破壞下會亮；③（幅度）與④（on_start）在同樣破壞下不亮。**
  分類：描述準確性問題（非程式缺陷）。
  是否阻斷：**否**；建議對外表述改為「兩項鑑別性絆索（①冪等、②規則0）＋兩項觀察值/既有閘門（③幅度、④on_start）」。

---

## D. 判定標籤彙總（本輪全部宣稱）

| # | 宣稱 | 判定 |
|---|---|---|
| A-1 | 四素材統計＋二次套用 byte 相同（含 A1 changed=3/kept58/backward3/max70） | `[REPRODUCED]` |
| A-2 | 鑑別力 rev9 gsw=0；v1.0 對照（5 素材 23）＝重現；規則 0 為跨發言者/冪等必要條件 | `[REPRODUCED]` |
| A-3①–④⑥⑦⑧⑨ | 各邊界行為與 v1.0 對照如實；均非回歸 | `[REPRODUCED]` |
| A-3⑤ | MM:SS 不支援→untraceable（既有）；**2 欄精度冪等在一般輸入不成立** | `[FALSIFIED]`（F1，宣稱過寬） |
| A-3 絆索 | 破壞下①②亮、③④不亮 | `[REPRODUCED]`（附鑑別力不對稱註記） |
| B-4 | NFKC/千分位/CRLF/空白/半形括號斜線對稱 | `[REPRODUCED]` |
| B-4 | 標點折疊完整、簡繁（若宣稱同一條） | `[FALSIFIED]`（F2） |
| B-5① | 真實 14 key 全涵蓋、守衛=僅LCS（0 差異） | `[REPRODUCED]` |
| B-5② | 嚴格整條刪除 14/14 判遺漏 | `[REPRODUCED]` |
| B-5③ | ≥2 位數 tamper 必攔；同義替換誤攔（已知）；1 位數/超字串洗白 | `[REPRODUCED]`（含界線） |
| B-5 假陽性 | 真實素材 0；忠實改寫 2 筆守衛 FP | `[REPRODUCED]` |
| B-6 | 多項洗白反例（逗號子句／詞表外否定詞／子字串數字／國字／小數／月份） | `[FALSIFIED]`（F3） |
| B-6 | 窗選擇「最佳」措辭 | `[FALSIFIED]`（F4） |
| B-6 | 簡體 key 繞守衛 | `[FALSIFIED]`（F5） |
| B-7 | 不收斂保護（集合相同不再跑第二輪） | `[REPRODUCED]` |

（報告完）
