# 離線證據獨立複核：`確定性誤辨校正.txt` 重播（glossary-replay-verify-01）

> 角色：離線證據獨立複核員（**唯讀**；未修改 `backend/**`、`tests/**`、`scripts/**`、`data/**`、
> `plan.md`、`handoff.md`、`CHANGELOG.md`；未 `git add/commit/push/checkout/stash`；**未重跑任何 LLM**、
> 未呼叫 LM Studio／`127.0.0.1:1234`、未碰 `data/cache/e2e/p4-qwen27b-e5/` 與 E5 attempt 目錄）。
> 唯一寫入＝本檔。複核時間：2026-09-23（Asia/Taipei）｜分支 `fix/qwen-local-quality-parity`｜HEAD `ce99502`
> 受審宣稱來源：commit `5247436`（`git log -1 --format=%B 5247436`）。

**一句話結論**：`5247436` 的「**→ 11 處**」「**115 次替換**」「**43 組配對全數命中**」**完全可重現**；
但「**60 處**」在六種可查核定義下**皆無法重現**（實測 24／22／57／116／126／128），且「45 個排除複合詞／
15 個保護詞」與實測 **47／14** 不符 → 宣稱的**前置基準（60）與兩個附帶計數不成立**，其餘宣稱成立。

---

## 一、宣稱

commit `5247436` body 原文（`/tmp/commit_msg_p5a1.txt` 為其落檔版本，兩者一致）：

1. 「`data/glossary/確定性誤辨校正.txt` 由 11 組擴到 **43 組**，另含 **45 個排除複合詞**與 **15 個保護詞**」。
2. 「離線重播（同一份校正前快取逐字稿，零模型成本）：亂碼命中 **60 處 → 11 處**
   （**115 次替換**、**43 組配對全數命中**）；剩餘 11 處全屬無 ground truth 類」。
3. 「誤傷負向對照（12 組）：保護數據／評審員／境內機器／拆除勤務／增收股份／吃麵條配泡麵／
   一隻貓兩隻狗／手錶錶帶／稻穀五穀／一見如故／可長可短／排毒管道 **全部維持原樣**」。

---

## 二、複核方法（零 LLM、可重跑）

### 2.1 「校正前快取逐字稿」的定位與「校正前」證明

受測檔（以下稱 **PRE**）：

```
data/cache/e2e/p1-baseline-01/backend_data/cache/982151f4629ade0c38f164b57a2f105c1e305677e7e10e4dc0e252f1ac012828_55f6458fbc55e768.txt
```

「校正前」的四項獨立證據：

1. **它是 ASR 快取層，不是校正輸出層**：`backend/services/task_processor.py:122-162` 先讀／寫
   transcript cache（`file_manager.save_transcript_cache`），`:240` 才進 `clean_transcript`、
   `:259` 才呼叫 `transcript_correction_service.correct_transcript` → cache 檔在流程上是校正層的**輸入**。
2. **全 18 場同一份**：`for f in $(find data/cache/e2e -path "*backend_data/cache*" -name "*.txt"); do shasum -a 256 "$f" | cut -c1-12; done | sort | uniq -c`
   → `18 809d6a0c6357`（18/18 相同；含提示點的 `p2-27b-01` 與 `p4-qwen27b-e4`）。
   `quality-parity-01/report.md:31-32`、`:151`、`:254` 亦以同一 sha `809d6a0c6357` 認定「四場共用同一份校正前快取」。
3. **交付逐字稿＝PRE＋校正差集**：`/tmp/pairs/fix_inventory.txt`（difflib 逐字元）顯示 5 個交付版本相對 PRE
   各只有 20–27 處字元差異（27B4=22、27B3=21、GEM1=27、GEM2=27、TDL=20；聯集 35 位置）→ PRE 是它們的**共同前置**。
4. **時間序**：PRE mtime＝`2026-09-22 19:36:28`（p2-27b-01 的同一份＝`2026-09-22 20:42:24`），
   本波詞表 mtime＝`2026-09-23 11:56:11`，E1–E4 交付逐字稿（如 E4＝`2026-09-23 11:09:27`）**全早於**詞表
   → 本波 43 組不可能污染任何既有逐字稿；重播是真正的「同一輸入、只加確定性層」。

### 2.2 亂碼清單來源

- **20 型清單（權威定義）**：`.agent/tasks/T20260922-2037-02-local-model-quality-parity/e2e/quality-parity-01/report.md:277`
  （附錄 A.1「亂碼殘留」列：`護數／拆勤／猜情／審員／省員／西龍股／電做科／平上／佩魯／校費稅科／
  雞茶股／增收股／煙酒文神股／排血管／除煤／於正／審兒園／內機／瀋圓圓／工廠科`，共 20 型；
  量測腳本 `/tmp/qp01/garble7.py`）。
- **19 型清單**：`/tmp/qp01/garble7.py` 的 `GARB` 實作（不含 `猜情`；`report.md:304` 註明 `猜情` 於補跑計入 20 型）。

### 2.3 重播命令（零 LLM）

```bash
cd /Users/hsiaojohnny/dev/convert
DATA_DIR=/tmp/probe_scratch uv run --frozen python /tmp/probe_glossary/final_verify.py   # 本報告全部數字來源
```

`final_verify.py` 只呼叫純函式 `backend.core.glossary.apply_known_corrections()`（`:150-176`）、
`load_glossary()`、`protected_terms()`；輸出落 `/tmp/probe_glossary/final_verify.out`。全程無模型、無網路。

---

## 三、實測數字（表）

### 3.1 主結果：20 型清單逐型命中（PRE → 套用後）

```
### 0. PRE 身分
sha256[:12] = 809d6a0c6357 | chars = 15667 | lines = 194
pairs = 43 | exclusions = 47 | protected_terms() = 109 | glossary terms = 121
```

| 型 | PRE | POST | 型 | PRE | POST |
|---|---:|---:|---|---:|---:|
| 護數 | 2 | 0 | 校費稅科 | 1 | 0 |
| 拆勤 | 1 | 0 | 雞茶股 | 1 | 0 |
| 猜情 | 2 | 0 | 增收股 | 1 | 0 |
| 審員 | 1 | **1** | 煙酒文神股 | 0 | **1**（規則生成，見 §5.4） |
| 省員 | 1 | **1** | 排血管 | 2 | 0 |
| 西龍股 | 1 | **1** | 除煤 | 1 | 0 |
| 電做科 | 1 | **1** | 於正 | 1 | **1** |
| 平上 | 1 | **1** | 審兒園 | 1 | **1** |
| 佩魯 | 2 | **2** | 內機 | 2 | 0 |
| 瀋圓圓 | 1 | 0 | 工廠科 | 1 | **1** |

- `TOTAL 20型: PRE=24 POST=11`（型數 PRE=19 → POST=10）。
- `TOTAL 19型（不計 猜情）: PRE=22 POST=11`。
- **宣稱的「→ 11」成立**（兩種清單皆 11；且與 `quality-parity-01/report.md` 定義一致）。
- **宣稱的「60」不成立**：同一份 PRE 在任何「20 型」定義下都是 **24**（不是 60）。其他候選定義見 §3.3。

### 3.2 43 組登錄錯形：逐組命中／替換／殘留

```
  內機=>內稽        PRE=2  applied=2  remaining=0     圓缺生=>原缺升    PRE=2  applied=2  remaining=0
  護數=>戶數        PRE=2  applied=2  remaining=0     原地生=>原地升    PRE=1  applied=1  remaining=0
  拆勤=>差勤        PRE=1  applied=1  remaining=0     鼓障=>股長       PRE=1  applied=1  remaining=0
  猜勤=>差勤        PRE=2  applied=2  remaining=0     可長=>科長       PRE=1  applied=1  remaining=0
  猜情=>差勤        PRE=2  applied=2  remaining=0     主長=>組長       PRE=1  applied=1  remaining=0
  猜庭=>差勤        PRE=1  applied=1  remaining=0     人士室=>人事室    PRE=1  applied=1  remaining=0
  校費稅科=>消費稅科  PRE=1  applied=1  remaining=0     係統=>系統       PRE=5  applied=4  remaining=0
  雞茶股=>稽查股     PRE=1  applied=1  remaining=0     註意=>注意       PRE=3  applied=3  remaining=0
  雞查股=>稽查股     PRE=1  applied=1  remaining=0     一見=>一件       PRE=1  applied=1  remaining=0
  增收股=>徵收股     PRE=1  applied=1  remaining=0     誌願=>志願       PRE=3  applied=3  remaining=0
  征收=>徵收        PRE=1  applied=1  remaining=0     講期=>講習       PRE=1  applied=1  remaining=0
  排血管=>排水管     PRE=2  applied=2  remaining=0     量飯百貨=>量販百貨  PRE=1  applied=1  remaining=0
  排毒管=>排水管     PRE=1  applied=1  remaining=0     瀋圓圓=>沈圓圓    PRE=1  applied=1  remaining=0
  除煤=>除黴        PRE=1  applied=1  remaining=0     麵=>面          PRE=43 applied=43 remaining=0
  土地稅客=>土地稅科  PRE=3  applied=3  remaining=0     隻=>只          PRE=9  applied=9  remaining=0
  房務稅科=>房屋稅科  PRE=1  applied=1  remaining=0     錶=>表          PRE=5  applied=5  remaining=0
  排香味稅科=>牌照稅科 PRE=1  applied=1  remaining=0     穀=>股          PRE=2  applied=2  remaining=0
  潛水管理股=>欠稅管理股 PRE=1 applied=1 remaining=0     （其餘 7 組皆 PRE=1 applied=1 remaining=0：
  洪稅係統=>房稅系統  PRE=1  applied=1  remaining=0      文刊活動／舉務會議／人事市的書記／科原／坑內群組／交論／超廉潔）
```

```
SUM PRE=116  SUM applied=115  SUM remaining=0
pairs with >=1 replacement = 43/43; zero-hit pairs = []
len_delta = -1（排香味稅科=>牌照稅科 少 1 字）
```

- **「115 次替換」成立**；差 1 的原因：`洪稅係統=>房稅系統` 先命中，吃掉 1 個 `係統` 命中（長形優先，`:169`），
  故 `係統` applied=4 < PRE=5。**「43 組配對全數命中」成立**（`zero-hit pairs = []`）。
- 43 組的 PRE 出現總數＝**116**（≠ 60），套用後殘留 **0**。

### 3.3 「60」的候選定義窮舉（全部無法重現 60）

| # | 定義 | PRE | POST |
|---|---|---:|---:|
| 1 | 20 型清單命中 | **24** | 11 |
| 2 | 19 型清單命中（`/tmp/qp01/garble7.py`） | **22** | 11 |
| 3 | 43 組多字錯形命中（不含 `麵/隻/錶/穀`） | **57** | 11 |
| 4 | 43 組錯形命中總數（含單字） | **116** | 0 |
| 5 | 聯集（43 錯形 ∪ 20 型，53 型） | **126** | 11 |
| 6 | E5 量尺 GARBLES（55 型，含 `煙酒為神股` 等） | **128** | 15 |
| — | 逐行上限（型×行去重）變體：20 型 20／聯集 80／43 多字 42 | — | — |

→ **沒有任何一種可查核定義得到 60**；宣稱的 60 來源無法在 repo 或 `/tmp` 找到對應腳本（見 §6.1）。

---

## 四、負向對照（最重要）

### 4.1 A：43 組「正確形」不會被其它規則二次改壞

```
### 6. 負向對照 A：43 組「正確形」不得再被任何規則改動
  FAIL = 0
  土地稅科 => ('土地稅科', [])      # 27B/TDL 曾誤修成「土地稅課」→ 本層不再動它
  消費稅科 => ('消費稅科', [])
  牌照稅科 => ('牌照稅科', [])
  欠稅管理股 => ('欠稅管理股', [])
```

逐條：**43/43 OK，FAIL=0**（`apply_known_corrections(right) == right` 且 `applied == []`）。
二次誤修守衛（`5247436` 明列的 `土地稅客=>土地稅科`）另測家族字串：

```
  '土地稅客戶' → '土地稅科戶'（預期：修，且不再動「科」）
  '土地稅客'   → '土地稅科'
  '土地稅科'   → '土地稅科'（不動）   '土地稅課' → '土地稅課'（不動）   '土地稅卡' → '土地稅卡'（不動）
```

### 4.2 B：47 個 `!複合詞` 逐條（是否真的擋住誤傷）

```
  摘要: OK=42  FAIL=0  inert=5 / 共 47
  OK '境內機'（擋住 ['內機']）      OK '保護數'（擋住 ['護數']）       OK '增收股份'（擋住 ['增收股']）
  OK '除煤灰'／'除煤渣'（擋住 ['除煤']）  OK '排毒管道'（擋住 ['排毒管']）  OK '可長可短'（擋住 ['可長']）
  OK '一見如故'／'一見鍾情'／'一見傾心'／'一見面'（擋住 ['一見']）
  OK '麵條'／'麵包'／'泡麵'／'吃麵'／'牛肉麵'／'拉麵'／'麵店'／'麵食'／'麵粉'／'麵線'（擋住 ['麵']，共 10）
  OK '一隻'…'五隻'／'隻身'／'隻手'／'船隻'／'隻字'（擋住 ['隻']，共 9）
  OK '手錶'／'錶帶'／'錶面'／'電子錶'／'腕錶'（擋住 ['錶']，共 5）
  OK '稻穀'／'穀物'／'五穀'／'穀倉'／'穀類'／'穀雨'（擋住 ['穀']，共 6）
  inert：'評審員'／'複審員'／'覆審員'／'初審員'（`審員` 未登錄，對任何已登錄錯形皆無交集）
```

逐條：**42 條有效擋住；0 條失敗；5 條 inert**（4 個 `審員` 系＋`拆除勤`：`拆勤` 未登錄 →
`拆除勤` 不含任何登錄錯形，故「沒擋到任何東西」，但也沒有誤傷）。
`!` 行數＝**47**（資料檔 `:76-123`），與宣稱「45」不符（見 §5.2）。

### 4.3 C：commit 宣稱的 12 組誤傷對照（原句級）

```
  OK '保護數據'  OK '評審員'  OK '境內機器'  OK '拆除勤務'  OK '增收股份'  OK '吃麵條配泡麵'
  OK '一隻貓兩隻狗'  OK '手錶錶帶'  OK '稻穀五穀'  OK '一見如故'  OK '可長可短'  OK '排毒管道'
```

**12/12 OK**（維持原樣、`applied=[]`）→ 此項宣稱成立。

### 4.4 D：複核員自行構造的誤傷面（corpus 外；全部為**具體反例**）

規則的排除詞只保護「已登錄的複合詞」；**未登錄**的正常用語會被命中。以下為實跑結果
（`apply_known_corrections()` 實際輸出，非推論）：

| 類 | 反例輸入 → 實際輸出 | 為何 |
|---|---|---|
| 單字 `麵`（無 `!` 護欄） | `麵攤→面攤`、`湯麵→湯面`、`乾麵→乾面`、`陽春麵→陽春面`、`炒麵→炒面`、`意麵→意面`、`刀削麵→刀削面` | 排除詞只有 麵條/麵包/泡麵/吃麵/牛肉麵/拉麵/麵店/麵食/麵粉/麵線 |
| 單字 `隻`（無 `!` 護欄） | `六隻貓→六只貓`、`七隻狗→七只狗`、`那隻貓→那只貓`、`這隻筆→這只筆`、`幾隻→几只` | 只登錄 `一隻`…`五隻` |
| 單字 `錶` | `碼錶→碼表`、`測錶→測表`、`馬錶→馬表` | 只登錄 手錶/錶帶/錶面/電子錶/腕錶 |
| 單字 `穀` | `穀種→股種`、`穀粒→股粒` | 只登錄 稻穀/穀物/五穀/穀倉/穀類/穀雨 |
| 多字（未登錄複合詞） | `增收股利→徵收股利`、`除煤油→除黴油`、`一見你就笑→一件你就笑`、`可長久使用→科長久使用`、`排毒管線→排水管線`、`標註意義→標注意義`、`關係統整→關系統整`、`猜情況→差勤況`、`原地生成→原地升成`、`開講期間→開講習間`、`科原型→科員型`、`交論文→討論文`、`內機房→內稽房` | 每組只登錄 1–4 個排除詞；未登錄者一律替換 |
| 不變（好消息） | `保護數量`、`個資保護數據庫` | `_replace_pair` 的 blocked-span 以「整個排除詞」為單位（`glossary.py:187-206`），`保護數` 的 span 覆蓋其內 `護數` → 連同延伸字亦安全 |

→ 43 組在**本 corpus（PRE 15,667 字）內**的負向對照全數通過；但**單字 4 組（`麵/隻/錶/穀`）與
部分多字配對的誤傷面未收斂**：只要新音檔出現未登錄的複合詞，就會被確定性層改壞（本波語料無此類命中，
故不影響 §3.1 數字；但這是機制層的殘餘風險，見 §5.5）。

---

## 五、不一致處

### 5.1 主宣稱「60 處」不成立（唯一重大不一致）

- 實測（同一份 PRE、三種與宣稱最貼近的定義）：**24／22／57**（§3.3）；含單字全量 116；聯集 126。
- `quality-parity-01/report.md:277` 的權威清單在 PRE 上是 **24**，本波前一顆 commit `7176929` 的 body
  自己寫的也是「**24 處 → 9 處**」→ **同一個測量基準在前一顆 commit 是 24，在本顆 commit 變成 60**，
  且找不到任何腳本／報告產出 60（`/tmp/pairs*`、`/tmp/qp01/*`、`/tmp/probe_glossary/*`、repo `.agent/**` 全查過）。
- 「→ 11」則兩種定義都成立（20 型與聯集皆 11），且 11 的組成可逐條列出
  （`佩魯×2、審員、省員、西龍股、電做科、平上、於正、審兒園、工廠科、煙酒文神股`）。

### 5.2 「45 個排除複合詞」→ 實測 47

`data/glossary/確定性誤辨校正.txt:76-123` 共 **47** 行 `!`（多出的 4 行為 `!評審員／!複審員／!覆審員／!初審員`，
`:119` 註解自述「`審員` 目前不登錄…保留供日後登錄參考」）→ 該 4 行目前 **inert**，屬預留資料，不影響行為。

### 5.3 「15 個保護詞」→ 實測 D 區 14 行；`protected_terms()` 實際回傳 109

`:126-139` 的 D 區為 14 詞（防水層…系統）；而 `protected_terms()`（`glossary.py:128-140`）把**整份詞表**
（含 `公務詞彙.txt` 與所有「正確形」）納入 → 109 個。宣稱的「15」既不等於 D 區（14）也不等於回傳值（109）。

### 5.4 「剩餘 11 處全屬無 ground truth 類」大致成立，但 1 處是**規則生成**的

POST 殘留中 `煙酒文神股×1` 在 PRE 並不存在——它是 `穀=>股` 規則把 PRE 的 `煙酒文神穀`
（`…一股叫做煙酒為神穀。…`／`…煙酒文神穀股掌應該是博語…`）換字後產生。
即：該規則把「錯字」修成「正確用字、但實體仍 UNKNOWN」的形態。對讀者是改善，
但對「以字面計數」的亂碼指標是 **0→1 的新命中**；宣稱「11 處皆無 ground truth」在
「無可唯一確定的正解」意義下仍成立，但應註明其中 1 處由本層產生。

### 5.5 單字配對的誤傷面未收斂（機制層，附構造反例）

§4.4 已列具體反例。單字配對的放行條件（`glossary.py:143-147`：單字須至少登錄 1 個含它的 `!` 詞）
只保證「有想過誤傷面」，**不保證**排除詞集合完整（`六隻/碼錶/穀粒/麵攤` 等皆漏）。
本波語料無此類命中，故 §3.1 的 24→11 不受影響；但同一機制換到其他音檔時，
這 4 條單字規則（`麵/隻/錶/穀`）是**唯一可能主動製造錯字**的來源。

### 5.6 其餘重播相關宣稱：一致

- 「43 組」= 實測 43（`:27-73`）。
- 「115 次替換」= 實測 115。
- 「43 組配對全數命中」= 實測 43/43（`zero-hit pairs=[]`）。
- 冪等：`apply(apply(PRE))` 2nd pass replacements = **0**、文字穩定 True（不會二次改壞已修好的字）。

---

## 六、未驗證

1. **`60` 的計算出處 `[UNVERIFIED]`**：已窮舉 6 種定義（§3.3）與 repo/`/tmp` 內全部腳本與報告，
   找不到任何產出 60 的 artifact；commit body 亦未附腳本名。無法排除該數字源自未落檔的臨時計算，
   或誤用了他場／他檔（例如 PRE 以外文本）。**本複核以 24（權威清單）為可查核基準。**
2. **單字規則在真實新音檔的誤傷率 `[UNVERIFIED]`**：§4.4 為人工構造反例；未對其他音檔做統計
   （本任務明文禁止重跑 LLM，且 ASR 亦未重跑）。
3. **`審員/省員` 的正解 `[UNVERIFIED]`**：清單內 `審核員`（雲端）與 `審計員`（地端紀錄）兩形互斥
   （`5247436` body 自述不登錄 `審員`）→ 這 2 處（共 2 次命中）確實無可唯一確定之 ground truth。
4. **E5 實跑結果 `[UNVERIFIED]`**：`attempt-E5-qwen27b-p5/` 於複核期間仍在進行（目錄未追蹤），
   本次**未讀取、未干擾**；「紀錄亂碼 ≤3」等產品端驗收不在本複核範圍。
5. **`!` inert 條目（4 個 `審員` 系、`拆除勤`）的未來語意 `[UNVERIFIED]`**：一旦日後登錄 `審員`，
   這些排除詞才會生效；屆時需重跑本報告 §4.2 的逐條驗證。

---

## 附錄：可重跑命令與落檔

```bash
cd /Users/hsiaojohnny/dev/convert
# 1) PRE 身分與 18 場同 sha
for f in $(find data/cache/e2e -path "*backend_data/cache*" -name "*.txt"); do shasum -a 256 "$f" | cut -c1-12; done | sort | uniq -c
# 2) 全量重播（本報告所有數字；零 LLM）
DATA_DIR=/tmp/probe_scratch uv run --frozen python /tmp/probe_glossary/final_verify.py | tee /tmp/probe_glossary/final_verify.out
# 3) 交付逐字稿與 PRE 的差異清單（前人產物，本次沿用）
python3 /tmp/pairs_fixinv.py | head -20
```

落檔：`/tmp/probe_glossary/final_verify.py`、`/tmp/probe_glossary/final_verify.out`
（本次另留 `/tmp/probe_glossary/replay.py`、`count20.py` 為早期探針，結論一致）。
