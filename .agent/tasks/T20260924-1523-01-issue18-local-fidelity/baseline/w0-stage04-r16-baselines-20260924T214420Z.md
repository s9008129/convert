# Stage04 W0 — pre-mutation baselines (R16)

- TASK_ID: T20260924-1523-01-issue18-local-fidelity
- PLAN_REVISION: 16
- Captured_UTC: 2026-09-24T21:44:20Z
- HEAD: `430950148cf02ece5845cc807665e0e4f35ea7e1`
- Branch: `issue-18-first-divergence-diagnostic`
- Product mutation had not begun.

## C3 focused cloud contract baseline

- Default environment command: `uv run pytest -q tests/test_summarization_service.py -k "cloud_pipeline or cloud_generation or validate_cloud or cloud_finalize or gemini_chat"`
- Exit status: `2`
- Observed result: collection blocked before tests because logger initialization tried to create `/app/data/logs` and the environment returned `OSError [Errno 30] Read-only file system: '/app'`.
- Environment-only rerun (same pytest command/selection, writable temporary test DATA_DIR): exit `0`; `13 passed, 41 deselected in 0.40s`.
- Classification: default runner environment limitation, not a test/product failure. The focused baseline is established only under the test-only DATA_DIR workaround; re-run same workaround after changes. Do not claim default-environment C3 PASS.
- Redaction: temporary directory name omitted; no source/model payload logged.

## C6 full repository baseline

- Command: `uv run pytest tests/ -q` with a writable temporary test-only `DATA_DIR`.
- Exit status: `0`
- Result: `893 passed, 2 skipped in 9.94s`.
- Classification: pre-mutation repository-health baseline under environment workaround. No full-suite rerun is needed until implementation has a focused set and risk-based final verification is selected.

## C7 docs baseline

- Command: `bash scripts/check_docs.sh`
- Exit status: `0`
- Observed result: file naming, required docs structure, and local-link checks passed; two warnings reported possible version mismatch in `README.md` and `doc/README.md`.
- Classification: pre-existing baseline warnings, no task-attributable product edit existed at this time. Re-run after any documentation changes.
