# R9 approved-scope source audit (attempt 03)

This audit independently follows actual production call sites and does not treat the prior audit prose as evidence.

| Approved item | Classification | Evidence and reason |
|---|---|---|
| R5 / W4 template-derived plans, per-section LLM calls, allowed claims/evidence, deterministic template-order assembly | MISSING | `plan_sections` in `backend/services/local_pipeline_v2.py:269-274` hardcodes `general/decisions/actions`, makes only `general` require all asserted claims, and receives no `MeetingTemplate`. The live path calls `plan_sections(ledger)` at `summarization.py:2308`, renders all sections with deterministic `render_section` at `2319`, and makes no model call per section. `SectionPlan` has no validated render-relation metadata; rendered identity is only prose evidence refs at `local_pipeline_v2.py:277-289`. |
| R6 / W5 raw-evidence high-risk alignment, relation metadata/order, number/date/entity/attribution/source-tag/coverage/cross-section/template-term checks | PARTIAL | The selected relation predicate and subject/object/attribution substring checks exist at `local_pipeline_v2.py:162-187`, and C1 live inversion rejection passes. However, relation validation is predicate-in-rendered-text, not a structured relation metadata contract or source ordering comparison. The live extraction request at `summarization.py:2271-2277` omits condition, number/unit, date, attribution, uncertainty/status. No production call-site wiring was found for source-derived numeric/date candidates, entity provenance/registry, source-tag↔span validation, cross-section claim-ID dedupe, coverage against template requirements, or template-scoped glossary/term correction. `snapshot_section` only checks subject/object and same-line duplicates (`296-308`). |
| R7 / W6 guarded targeted patch and rollback in production | PARTIAL | `guarded_section_patch` exists and the synthetic rollback test passes (`local_pipeline_v2.py:320-325`, `tests/test_local_pipeline_v2.py:80-94`), but the live V2 path imports/calls no patch or rollback mechanism; it directly assembles rendered sections at `summarization.py:2319-2337`. |
| R8 / W7 model-independent policy plus live Qwen/Gemma capability/profile controls and repeated sampling | PARTIAL | `QualityPolicy` and `ModelRuntimeProfile` data models exist (`local_pipeline_v2.py:190-211`), and runtime selection validates loaded-instance identity/context (`summarization.py:1594-1784`). The live V2 path hardcodes extraction temperature `0.3` for both initial and repair calls (`summarization.py:2282-2297`), sends only `temperature/max_tokens` plus optional `reasoning_effort` (`summarization.py:3113-3126`), and has no live profile construction, top_p/top_k controls, capability validation, or repeated 0.3/0.5/0.7 candidate sampling/selection. Current LM Studio inventory lists both required models but neither is loaded, so no live sample can be counted. |
| R9 / W9 privacy-safe expanded diagnostics and explicit v1/v2 selection | PARTIAL | Explicit v1 default/v2 selector is present in config and dispatch. The recorder safely hashes inputs/outputs and allow-lists metadata (`local_pipeline_diagnostics.py:63-130`), but the V2 path records only extraction input hashes plus a generic ledger count and final output hash (`summarization.py:2279-2280`, `2335-2337`). It does not emit the approved source raw/corrected roles, schema status/counts, pre/post ledger, section plan/render/validation snapshots, patch/rollback, assembly/final validation, or profile capability diagnostics. |
| R10 / W8 A–I, live selected claim, full/docs, fresh Gemma/Qwen, blind 3+3 | PARTIAL | A–I/helper and live C1 focused tests pass (10 passed), full suite passes (827/2), docs exits 0 with two warnings, and C8 remains dirty-artifact failure. Fresh model E2E, profile samples, and blind 3+3 cannot run: both models are unloaded and frozen evaluator/unrounded baseline are absent. The approved live R5/R6/R7 mechanisms are also not present, so helper test success cannot establish R10 completion. |

Conclusion: C1's repaired inversion boundary is independently PASS, but approved product scope is conclusively incomplete across R5, R6, R7, R8, R9 and the corresponding R10 closure evidence. This is not a new load-bearing plan-premise invalidation; it is an approved-scope implementation completion failure requiring Stage04 repair/reverification. No product files were modified by this attempt.
