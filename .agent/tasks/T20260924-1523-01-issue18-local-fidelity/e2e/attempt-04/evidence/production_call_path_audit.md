# R9 repair production call-path audit

All findings below are from current source and actual call sites, not Stage04 prose.

| R9 requirement | Result | Evidence |
|---|---|---|
| Extraction explicitly requests condition, number/unit, date, attribution, uncertainty/status and raw evidence support | SATISFIED | `backend/services/summarization.py:2299-2310` requests all fields, explicit null/unknown/ambiguous values, and ordered raw-source support. |
| Asserted high-risk claims are hard-failed against raw source before section planning | SATISFIED | `backend/services/summarization.py:2342-2347` calls `validate_asserted_claims_against_source`; `backend/services/local_pipeline_v2.py:357-377` raises on missing evidence or asserted subject/predicate/object/condition/number/unit/date/attribution. |
| Core relation metadata is required and checked by firewall path | PARTIAL | `validate_relation_metadata` requires metadata for required causal/conditional claims and is called at `summarization.py:2390-2393`; unknown IDs/wrong predicates are rejected. However metadata is `Mapping[str,str]`, not a structured relation object with polarity/condition/order, and raw source ordering is only concatenated with `"\n"` (`local_pipeline_v2.py:366-373`), not independently compared. Polarity is not source-grounded in `validate_asserted_claims_against_source`. |
| Template-derived section mapping and one LLM call per planned section | PARTIAL | Production calls `plan_sections(ledger, template_section_titles(template))` at `summarization.py:2348`, and each plan receives one model call at `2376-2383`. But `template_section_titles` only derives from `required_section_patterns` (`local_pipeline_v2.py:329-341`), collapses non-ASCII labels to a generic `section`, and does not map production `record_sections/header/subfields`; current mapping is not complete template topology. |
| Guarded targeted patch/rollback is used in production | SATISFIED | Baseline/candidate snapshots and `guarded_section_patch` are called at `summarization.py:2394-2398`; patch status is recorded at `2400-2402`. Synthetic production probe confirmed an inverted candidate is rolled back to source-backed baseline. |
| Runtime profile is constructed/validated and passed to each V2 generation | PARTIAL | Profile is built and capability-validated before extraction at `summarization.py:2269-2282`; extraction, repair, and section calls pass `runtime_profile` (`2315-2320`, `2327-2332`, `2377-2382`), and LM Studio forwards only validated controls (`summarization.py:3201-3224`). Current capability map explicitly marks context/thinking/top_p/top_k unsupported (`2279-2281`), so those controls are not transmitted; no live candidate A/B repeated sampling/selection is implemented in this path. |
| Expanded privacy-safe diagnostics / v1-v2 selection | PARTIAL | v1/v2 remains explicit; section input/validation/patch and profile events are recorded. The V2 path still emits limited ledger/final diagnostics (`summarization.py:2415-2422`) and does not record complete schema/claim counts, relation metadata verdicts, cross-section/template-term results, or profile sampling summaries. |
| R10 acceptance | PARTIAL/BLOCKED | Focused V2 14 passed, cloud 13 passed, full 831 passed/2 skipped, docs exit 0. `git diff --check` remains blocked by historical execution whitespace. C9 is ENVIRONMENT-blocked (required models listed but unloaded); C10 is AUTHORITY-blocked (frozen evaluator/unrounded baseline absent); C14 is ENVIRONMENT+AUTHORITY-blocked. |

Additional code facts:

- `cross_section_claim_duplicates` and `validate_template_terms` exist at `local_pipeline_v2.py:419-430` but are not called by the live V2 path, so those diagnostics are not actually enforced or recorded.
- `SectionQualitySnapshot.accepted` includes many fields, but `snapshot_section` does not populate all of them from live candidate metadata; see `local_pipeline_v2.py:469+`.
- `validate_runtime_profile` is production-called, but unsupported controls deliberately make `profile.validated` false. This is correctly reported rather than falsely accepted, but the approved profile-control/sampling closure evidence is still unavailable.
