# Handoff — P7-C Local Extraction and Adherence

## TASK
- TASK_ID: T20260923-2050-01-local-extraction-adherence
- STATUS: READY_FOR_IMPLEMENTATION
- PLAN_PATH: `.agent/tasks/T20260923-2050-01-local-extraction-adherence/plan.md`
- PLAN_REVISION: 8
- PLAN_SHA256: `8783d47192cb87886003561ca57f39064c133c69bbb620a4cf138f972d87b26c`
- REVIEW_REQUIRED: YES
- REVIEW_REPORT: `.agent/tasks/T20260923-2050-01-local-extraction-adherence/review/attempt-08/review_report.md`
- REVIEWED_PLAN_REVISION: 8
- REVIEWED_PLAN_SHA256: `8783d47192cb87886003561ca57f39064c133c69bbb620a4cf138f972d87b26c`
- INDEPENDENT_ACCEPTANCE_REQUIRED: YES
- E2E_REQUIRED: YES
- ACCEPTANCE_MODE: E2E
- Fresh Implementer required: YES
- Planner/Reviewer transcript required: NO

## GOAL_ANCHOR
PRIMARY_OUTCOME: 改善 Gemma 4 與 Qwen 3.8 的地端會議紀錄品質，以固定輸入與既有量尺如實呈現數字，不宣稱已達雲端等價。
SUCCESS_EVIDENCE: 零模型呼叫契約與回退證明有效；四場指定 LM Studio E2E 依 Plan §5 達到預註冊品質／成本／DOCX／反 gaming 門檻，Stage 05 獨立核對。
MUST_NOT_BREAK: P7-B 覆蓋與密度、雲端行為、既有 off/no-log／後處理順序、摘要失敗隔離、隱私邊界及模型／平台無關單一路徑。
ROLLBACK: 所有新增 controls 關閉時，保留既有 coverage `enforce` 與 categories 預設，按 Plan §8 rev8 零呼叫方式 byte 級比對固定 P7-B 基準。

## CRITICAL_PATH
Stage 04 核對 rev8/Review freshness、髒工作樹與變更前全庫基線證據 → 依核准順序做 CORE-3、CORE-2、CORE-1 的零模型呼叫實作及 focused contracts → 執行固定 `ab2528b718e8c91557fd72db174b0f9bb78d3a68` 的 hash-only BYTE-ROLLBACK 與全庫 baseline delta → 只提交產品碼＋測試 → 從該 commit 的 detached clean worktree 跑 Gemma 4 ×2、Qwen 3.8 ×2 LM Studio E2E → fresh Stage 05 read-only acceptance → 所有 required checks 閉合後才 push。

## SEMANTIC_INVARIANTS
- 按 Plan §3、§5–8 執行；不可改任何 requiredness、品質門檻、有效性、gate、錯誤／降級語意、優先序或雲端行為。所有 check 初始 `CHECK_RESULT=NOT_RUN`、`WAIVER_STATUS=NOT_ALLOWED`、`WAIVER_ALLOWED=NO`、`WAIVER_AUTHORITY=NONE`；代理不得自行豁免。
- `LOCAL_LLM_RECORD_FACT_COVERAGE_ENABLED` 預設 `false`；關閉不得增加 fact issues／metrics／prompt bytes，保留既有 `LOCAL_LLM_RECORD_COVERAGE_MODE` 與 categories。`LOCAL_LLM_RECORD_ADHERENCE_ENABLED` 預設 `false`；關閉不得插入 adherence prompt bytes。四場 E2E 明確開啟兩者；兩者只改 local path，cloud prompt/output 不變。
- 舊 `LOCAL_LLM_RECORD_COVERAGE_MODE=off` 仍是「完全不跑、無 log/metrics」契約，不得拿來代替 C2 flags；預設 `enforce`、categories `topic,decision,number,date` 必須留在 all-off rollback 比較中。
- C1c `LOCAL_LLM_EXTRACTION_CALL_BUDGET` 是全部 chunks 的總上限，`0` 不限；任何 provider I/O 前完整 chunks 預檢。超限不得丟塊或用部分筆記生成，只降級該次摘要為逐字稿＋明顯失敗警告並記 `extraction_call_budget_exceeded`；不得改 `task_processor.py:259/262`。
- C2b 的 local round/budget 只作用於 local loop；共享上限、Gemini loop、雲端 prompts/output 不變。budget 用盡時保留最後一版完整有效摘要並記 `refinement_call_budget_exceeded`。
- C3 正規化預設關閉、registry 優先且僅唯一 registry 候選可替換；四場 gating E2E 關閉；不可引入模型名／平台名／OS 分支，不得讀取或洩漏 checklist token；後處理順序保持不變。
- BYTE-ROLLBACK 必須照 Plan §8 step 3：固定合成 fixture、mock providers、真實 provider 呼叫數 0、固定 clock、原始 UTF-8 bytes/hash，不正規化字串；基準 hash fixture 與 Stage 04 證據不得含 raw prompt／輸出／逐字稿。
- E2E 僅用 detached worktree，HEAD 固定產品測試 commit、每場前 porcelain 為空、runner 傳 `--expected-revision`。`--artifacts-dir` 與 runtime 全視為 raw，僅存主工作樹 gitignored cache；task evidence 只可存 Plan §8 allowlist derivative/hash。Stage 05 read-only 對照原件，不複製 raw。
- 六個 status subjects 必須正交；Stage 04 execution SHA/status snapshot 不可被 Stage 05 回寫。Verification/E2E blocker 不可抹除已證 implementation/CORE，亦不可誤報 `IMPLEMENTATION_BLOCKED`；`DONE` 只依 workflow-routing.md §7.10。

## BEST_EFFORT_DO_NOT_GATE
- Plan §4 的 supporting/ablation/cross-run 工作（S1–S5）不可延誤或否決 CORE E2E；S3/S5 配對場僅 CORE 後且資源允許時考慮。
- `FULL-SUITE` 是 SUPPORTING / REPOSITORY_HEALTH / BASELINE_DELTA：同等未惡化的既有 failure 須披露；不可用它 veto 已有效的 CORE outcome。基線無效只使 required verification incomplete/closure pending。
- `STATUS-CONTRACT-FIXTURES` 是 SUPPORTING / MUST_NOT_BREAK / HARD_CLEAN：阻止錯誤結案，不改寫產品結果；執行前必須檢查所有 `FIXTURE_ID` 唯一。
- `E2E-COMMIT-PROVENANCE` 是 SUPPORTING / MUST_NOT_BREAK / HARD_CLEAN：失敗只阻止受影響 E2E 證據及依賴它的 CORE acceptance，保留 implementation 狀態。
- Windows／Ollama 實機不在本波驗證，必須保留 `[UNVERIFIED]`。

## DEFERRED_NOT_THIS_TASK
- 不追求或宣稱雲端 parity；不測 `qwen3.6-35b-a3b-splash`；不聲稱 Windows／Ollama 已實機驗證。
- 不新增模型／平台分支，不改 Gemini/cloud prompts、共享 refinement 值、既有品質門檻、off/no-log semantics、後處理順序或 `task_processor.py:259/262`。
- S3/S5 配對 E2E 只在所有 CORE 工作之後且資源允許時進行；S4 尾塊合併不是本波阻斷路徑。

## REPO_ANCHOR
- Project root: `/Users/hsiaojohnny/dev/convert`
- Branch: `fix/qwen-local-quality-parity`
- Anchor HEAD: `ab2528b718e8c91557fd72db174b0f9bb78d3a68`
- Relevant dirty state: preserve modified `.env.example`, `backend/core/config.py`, `backend/core/text_postprocess.py`, `backend/services/summarization.py`, `tests/test_t20260923_p4a_record_coverage.py`; preserve untracked `tests/test_t20260923_p7c_proper_noun_normalize.py` and this task artifact directory. Recheck exact status/diff at Stage 04 startup; do not reset, stash, overwrite, or commit task artifacts.
- Drift since plan/review: current HEAD still matches the rev8 anchor and attempt-08 approval. The rev6 handoff was stale and is archived at `handoff-history/handoff-plan-r6-20260923-232502.md`. Rev6 Stage 04 recorded a semantic escalation; existing product/test WIP and all prior evidence remain uncommitted and are not rev8 acceptance.

## CURRENT_STATE_DELTA
Plan rev8 is approved by attempt-08 at the exact hash above. Existing `execution.md` records a pre-mutation full-suite baseline at that anchor (`1129 passed, 2 skipped`) and CORE-3 focused checks, but those results do not establish CORE acceptance or final rev8 contracts; revalidate the evidence/affected tests. The same execution artifact records the rev6 all-off C2 escalation and six statuses `UNKNOWN / ESCALATED / NOT_RUN / INCOMPLETE / PENDING / REPLAN_REQUIRED`; preserve it as history and append a fresh Stage 04 entry rather than rewriting it. No LM Studio inference or E2E has run (0/4).

## MUST_READ_PLAN
Before first action read Plan §§0.5–0.6, 1–3, 5–8.2, 9–11, especially rev8 byte rollback, C2 flags/all-off defaults, stage sequence, privacy/cache rules and §8.1 routing. Read §4 only if taking optional supporting work. No literal `REQ-*`, `DEC-*`, or `WAVE-*` IDs are defined in the Plan; use named anchors CORE-1/C1a–c, CORE-2/C2a–c, CORE-3/C3, CORE-A–F, and the exact check IDs in §8.1. Do not invent identifiers or decisions.

## SETTLED_DO_NOT_REOPEN
- Rev8 C2a/C2c default-false flags and distinct all-off behavior; retain old coverage mode/categories defaults. Four required E2Es explicitly enable both C2 flags.
- Fixed byte baseline: full commit `ab2528b718e8c91557fd72db174b0f9bb78d3a68`; exact zero-provider, fixed-fixture/hash-only procedure in §8 step 3. Do not substitute real model output.
- CORE thresholds, fixed audio/template/observe mode, call/round/cost ceilings, model set, E2E order and no-parity claim: Plan §§1–8.
- Detached E2E worktree, expected revision, raw-cache/privacy boundary, Stage 05 snapshot and push-after-closure rules: Plan §8.
- Preserve all current WIP and prior append-only evidence; no force push, rebase, reset, task-evidence cleanup, or self-waiver.

## REVERIFY_ON_START
- Verify canonical plan SHA, attempt-08 report `FINAL_STATUS: PLAN_APPROVED`, matching review snapshot SHA, this handoff hash, branch/HEAD, and current `git status`; preserve newly appearing user work.
- Before any product/test mutation, inspect execution evidence to confirm the recorded full-suite baseline is at exact HEAD `ab2528b718e8c91557fd72db174b0f9bb78d3a68` and predates every product/test mutation. If valid, reuse it. Otherwise create a new empty `mktemp -d /tmp/p7c-baseline-XXXXXX` `DATA_DIR` and record exact command/exit/output; never use, overwrite, or delete `/tmp/test_scratch` or old scratch.
- Reconcile existing WIP to the approved rev8 plan; rerun affected focused contracts and do not treat prior green tests as proof of C2/rollback/CORE acceptance.
- Before E2E, verify fixed input/template/mode, that Gemma 4 31B and Qwen 3.8 27B are available in LM Studio, unique cache paths are absent and ignored, and detached commit/zero-porcelain/runner revision prerequisites hold. Availability checks are not model-test evidence.

## TRIGGERED_POLICIES
- `goal-alignment-design-economy.md`
- `testing-verification.md`
- `workflow-routing.md` §7
- `dependencies-contracts.md`
- `security-privacy.md`
- `performance-concurrency.md`
- `git-change-hygiene.md` for commit/push
- `debugging-recovery.md` only if a new failure/regression is observed

## FIRST_ACTION
Fresh Stage 04 must first validate this handoff against the approved rev8 plan/review hashes, inspect the exact dirty WIP and append-only execution history, and verify the recorded pre-mutation baseline evidence. If the baseline is valid, proceed with the approved zero-call CORE-3 contracts/implementation while preserving existing changes; otherwise record the scoped baseline condition and use only a new task-specific scratch path. Do not start LM Studio inference at this stage.

## IMPLEMENTATION_WAVES
- Baseline precondition: validate/reuse the recorded mutation-free baseline; otherwise establish a new one without touching prior scratch.
- Planned order: optional zero-cost S1 only if it does not delay CORE → CORE-3 deterministic normalization → CORE-2 C2a/C2b/C2c local controls → CORE-1 extraction controls. Each slice needs focused zero-model-call tests and `.env.example` updates as specified.
- After all waves: exact BYTE-ROLLBACK fixture/hash capture and comparison; local/cloud contracts, platform audit, status fixtures, full-suite baseline delta. A hash mismatch is `TASK_REGRESSION`; missing comparable baseline/evidence is scoped `BLOCKED`.
- Only after implementation/focused checks, make a product-code+tests-only commit. Use a new detached clean worktree at that SHA for the four actual LM Studio runs: GEMMA-E1, GEMMA-E2, QWEN-E1, QWEN-E2. Each run enables both C2 flags, keeps C3 off, and uses the Plan’s frozen settings and evidence boundary. Stop on any provenance/privacy failure.
- Fresh Stage 05 is read-only and independent. Push the tested commit only after Stage 05 PASS and every required check is closure-satisfied; no force-push. Stop if the remote has advanced or rejects the fast-forward.

## ACCEPTANCE_CONTRACT
Every check is initially `CHECK_RESULT=NOT_RUN`, `WAIVER_STATUS=NOT_ALLOWED`, `WAIVER_ALLOWED=NO`, `WAIVER_AUTHORITY=NONE`; preserve independent status subjects and scoped blockers in `execution.md`.

| CHECK_ID | COMMAND/SCENARIO | GOAL_CRITICALITY | EVIDENCE_ROLE | CLOSURE_GATE | BASELINE_RULE | FAILURE_ROUTING | WAIVER_ALLOWED | WAIVER_AUTHORITY |
|---|---|---|---|---|---|---|---|---|
| CORE-A | Two Gemma E2Es; verify the seven preregistered tail facts at delivery using Plan §5 plus manual/`rg` source check | CORE | OUTCOME | HARD_CLEAN | None | Valid shortfall=FAIL; missing/invalid required input=evidence BLOCKED; not run=NOT_RUN | NO | NONE |
| CORE-B | `scripts/e2e/measure_coverage.py` on both Gemma records | CORE | OUTCOME | HARD_CLEAN | None | Plan §5 per-run threshold; invalid measure=BLOCKED | NO | NONE |
| CORE-C | `scripts/e2e/measure_coverage.py` on both Qwen 3.8 records | CORE | OUTCOME | HARD_CLEAN | None | Plan §5 per-run threshold; shortfall=FAIL; invalid/missing run=BLOCKED/NOT_RUN | NO | NONE |
| CORE-D | `scripts/e2e/measure_record_quality.py` on both Qwen records | CORE | OUTCOME | HARD_CLEAN | None | Any per-run density floor failure=FAIL | NO | NONE |
| CORE-E | Calls/round metrics and `run_summary.json` started→finished wall-clock | CORE | OUTCOME | HARD_CLEAN | Same-path P7-B baseline | Cap exceeded=FAIL; absent/invalid baseline or timing=BLOCKED | NO | NONE |
| CORE-F | DOCX exists and `unzip -t` | CORE | OUTCOME | HARD_CLEAN | None | Missing/invalid=FAIL; environment prevents validation=BLOCKED | NO | NONE |
| ANTI-GAMING | Stage 05 source-check 2–3 new facts and inspect prompt for checklist-token leakage | CORE | OUTCOME | HARD_CLEAN | None | Unsupported fact/leak=FAIL; incomplete source evidence=BLOCKED | NO | NONE |
| LOCAL-CONTRACTS | Focused tests for flags, local/cloud isolation, budgets, off/no-log, fail-safe degradation and post-process order | CORE | MUST_NOT_BREAK | HARD_CLEAN | None | Contract deviation=TASK_REGRESSION; if repair changes semantics, Stage 01 replan | NO | NONE |
| BYTE-ROLLBACK | §8 step 3 harness; compare all listed UTF-8 hashes against pinned full `ab2528b` using `tests/fixtures/p7c_byte_rollback_ab2528b.json`; save hash-only evidence in `verification/byte-rollback.json` | CORE | MUST_NOT_BREAK | HARD_CLEAN | Fixed immutable P7-B commit + synthetic fixture; zero real provider calls | Any mismatch=TASK_REGRESSION; missing checkout/fixture/comparable hashes=evidence BLOCKED | NO | NONE |
| PLATFORM-INDEPENDENCE | Static audit and focused tests; no model/platform/OS conditional branch | CORE | MUST_NOT_BREAK | HARD_CLEAN | None | Forbidden branch=TASK_REGRESSION; Windows/Ollama real-machine result remains `[UNVERIFIED]` | NO | NONE |
| FULL-SUITE | `DATA_DIR=<validated recorded baseline or new task scratch> uv run --frozen pytest tests/ -q`, before/after same conditions | SUPPORTING | REPOSITORY_HEALTH | BASELINE_DELTA | Complete pre-mutation baseline | Same/non-worsened signature=PRE_EXISTING_FAILURE and disclose; new/worsened relevant failure=TASK_REGRESSION; unusable baseline=BLOCKED/INCOMPLETE | NO | NONE |
| STATUS-CONTRACT-FIXTURES | Execute/inspect all Plan §8.2 deterministic cases; assert unique IDs; Stage 05 independently recheck | SUPPORTING | MUST_NOT_BREAK | HARD_CLEAN | None | Mismatch/contradiction/result erasure=TASK_REGRESSION; missing evidence=BLOCKED; cannot close DONE | NO | NONE |
| E2E-COMMIT-PROVENANCE | Each run: detached HEAD/empty porcelain, health expected/actual revision, ignored raw paths, allowlisted derivative and source hashes | SUPPORTING | MUST_NOT_BREAK | HARD_CLEAN | Fixed product+test commit SHA | Failure blocks affected E2E-dependent CORE evidence/CORE acceptance only; preserve implementation status | NO | NONE |
| STAGE05-INDEPENDENT | Fresh read-only acceptance of CORE evidence, original outputs/DOCX/cost, status snapshots and residual risk | CORE | OUTCOME | HARD_CLEAN | Stage 04 execution SHA + immutable status snapshot | Evidence-based FAIL; external blocker scoped BLOCKED; premise invalidation=REPLAN_REQUIRED | NO | NONE |

Stage 04 must record `PRIMARY_OUTCOME_STATUS`, `IMPLEMENTATION_STATUS`, `CORE_ACCEPTANCE_STATUS`, `REQUIRED_VERIFICATION_STATUS`, `INDEPENDENT_ACCEPTANCE_STATUS`, and `TASK_CLOSURE_STATUS` separately, plus check evidence, baseline/waiver fields, blocker scope/next action, plan/handoff hashes and timestamp. Stage 05 records the Stage 04 `execution.md` SHA and three reported status snapshots before read-only verification and never edits that file. `DONE` is only the overall closure state under workflow-routing.md §7.10; verification blockers do not retroactively erase proven implementation/CORE facts.

## STOP_AND_ESCALATE_IF
- New evidence invalidates a load-bearing root-cause, fixed-input, cost, rollback, privacy, architecture, or acceptance premise; stop and write `escalation.md`, then replan/review/handoff before further implementation.
- Any proposed change to requiredness, validity, gate/veto, error/fallback/degradation, priority, shared cloud behavior, old off/no-log contract, output order, or thresholds is a Stage 01/02 decision, never a bounded Stage 04 repair.
- BYTE-ROLLBACK byte/hash mismatch is a named `TASK_REGRESSION`; fix only within approved semantics. Missing baseline/fixture/hash is scoped verification `BLOCKED`, not `IMPLEMENTATION_BLOCKED`.
- E2E input/model not available, chunks exceed approved limit, dirty/wrong detached HEAD, revision mismatch, raw payload in task evidence, or cache boundary failure: stop subsequent E2Es and record the specific scoped blocker; do not alter runner, ignore rules, evidence location or thresholds.
- Stage 05 not PASS, required checks unresolved, remote advance or push rejection: do not report DONE or force-push; follow approved repair/replan route or report the blocker.

## HISTORICAL_TASK_DEPENDENCIES
- `T20260923-1810-01-local-record-fidelity-density` (P7-B): only the specific quality/coverage/density/cost evidence cited by Plan §§1–2, 5 and 11; inspect only those cited files/sections.

TASK_ID: T20260923-2050-01-local-extraction-adherence
HANDOFF_PATH: `.agent/tasks/T20260923-2050-01-local-extraction-adherence/handoff.md`
PLAN_REVISION: 8
STATUS: READY_FOR_IMPLEMENTATION
NEXT_STAGE: 04_IMPLEMENT
