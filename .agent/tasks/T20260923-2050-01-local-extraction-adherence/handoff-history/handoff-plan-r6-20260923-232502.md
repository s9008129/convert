# Handoff — P7-C Local Extraction and Adherence

## TASK
- TASK_ID: T20260923-2050-01-local-extraction-adherence
- STATUS: READY_FOR_IMPLEMENTATION
- PLAN_PATH: `.agent/tasks/T20260923-2050-01-local-extraction-adherence/plan.md`
- PLAN_REVISION: 6
- PLAN_SHA256: `79a92f9ec0e71a031a04656adcb7a5f081ad99574fab4d1fb11334a97d883f72`
- REVIEW_REQUIRED: YES
- REVIEW_REPORT: `.agent/tasks/T20260923-2050-01-local-extraction-adherence/review/attempt-06/review_report.md`
- REVIEWED_PLAN_REVISION: 6
- REVIEWED_PLAN_SHA256: `79a92f9ec0e71a031a04656adcb7a5f081ad99574fab4d1fb11334a97d883f72`
- INDEPENDENT_ACCEPTANCE_REQUIRED: YES
- E2E_REQUIRED: YES
- ACCEPTANCE_MODE: E2E
- Fresh Implementer required: YES
- Planner/Reviewer transcript required: NO

## GOAL_ANCHOR
PRIMARY_OUTCOME: 縮小地端會議紀錄與 Gemini 的品質差距，分別報告 Gemma 4 / Qwen 3.8 的實測數字，不宣稱已達雲端水準。
SUCCESS_EVIDENCE: 四場有效 E2E 達 Plan §5 預註冊的事實覆蓋、密度、成本、DOCX 與反 gaming 驗收，並經 Stage 05 獨立核對。
MUST_NOT_BREAK: 保留 P7-B 覆蓋／密度成果、雲端行為、既有 off/no-log／輸出順序與摘要失敗隔離；新增旋鈕全關時 byte 級回退。
MODEL/PLATFORM: 新邏輯模型與平台無關；禁測 `qwen3.6-35b-a3b-splash`；Windows/Ollama 實機仍標 `[UNVERIFIED]`。

## CRITICAL_PATH
完整、可比的變更前全庫 baseline → 零模型呼叫 CORE-3／CORE-2／CORE-1 實作與 focused contracts → all-off byte rollback → 只含產品碼＋測試的本機 commit → 固定 commit 的 detached clean worktree 四場 E2E（raw output 留 gitignored cache）→ Stage 05 獨立驗收與狀態閉合 → checks closure-satisfied 後才 push 至指定分支。

## SEMANTIC_INVARIANTS
- 按 Plan §§3、5、6、7、8.1 執行；所有檢查初始 `CHECK_RESULT=NOT_RUN`、`WAIVER_STATUS=NOT_ALLOWED`、`WAIVER_ALLOWED=NO`、`WAIVER_AUTHORITY=NONE`，不得自我豁免。
- C1c `LOCAL_LLM_EXTRACTION_CALL_BUDGET` 是跨全部 chunks 的總呼叫上限，`0` 不限；完整 chunks 清單須在 provider I/O 前預檢。超限不得丟塊、部分摘要或繼續生成；只降級該次本地摘要為逐字稿＋明顯失敗警告，記 `extraction_call_budget_exceeded`；不改 `task_processor.py:259/262`。
- C2b 僅 local loop 使用 `LOCAL_LLM_MAX_LOCAL_REFINEMENT_ROUNDS` / `LOCAL_LLM_REFINEMENT_CALL_BUDGET`；共享值、Gemini loop、雲端提示與輸出 bytes 不變。budget 用盡保留最新完整且有效摘要，記 `refinement_call_budget_exceeded`。
- `LOCAL_LLM_RECORD_COVERAGE_MODE=off` ⇒ 完全不跑、無 log/metrics；`LOCAL_LLM_REFINEMENT_NO_REGRESSION=false` ⇒ 無守衛 log；保留處理順序。所有新 controls 預設關閉，全關須 byte 級回到固定 P7-B/`ab2528b` 產品行為。
- 不得新增模型／平台判斷；C3 registry 優先、唯一候選才替換，不讀或洩漏 checklist token。四場 gating E2E 的 C3 全關。
- E2E 必須在產品＋測試 commit 的 detached worktree 執行，HEAD 固定、porcelain 空、runner 使用 `--expected-revision`。runner `--artifacts-dir` 全部視 raw；raw artifacts/runtime 只放主工作樹 gitignored cache，`.agent` 只放固定 allowlist derivative、repo-relative cache key 與 hashes。Stage 05 read-only 對照原件，不複製 raw payload。
- 六個 status subjects 必須正交；保留 Stage 04 `execution.md` hash/status snapshot，Stage 05 不回寫。Verification/E2E blocker 不能抹除已證 implementation/CORE，也不得誤報 `IMPLEMENTATION_BLOCKED`；`DONE` 僅依 workflow-routing.md §7.10。

## BEST_EFFORT_DO_NOT_GATE
- Plan §4 S1–S5 與跨場逐字稿差異為 SUPPORTING/BEST_EFFORT；未做或失敗不可阻擋四場 CORE E2E。
- `FULL-SUITE` 是 SUPPORTING / REPOSITORY_HEALTH / BASELINE_DELTA，不 veto CORE：同等且未惡化的既有失敗須披露；baseline 不可得只使 required verification 不完整、closure pending，不阻止實作或有效 CORE acceptance。
- `STATUS-CONTRACT-FIXTURES` 是 SUPPORTING / MUST_NOT_BREAK / HARD_CLEAN；只為防止錯誤 DONE／狀態證據抹除，fixture 問題阻 closure，不改寫產品 outcome。
- `E2E-COMMIT-PROVENANCE` 是 SUPPORTING / MUST_NOT_BREAK / HARD_CLEAN：失敗只使受影響 E2E-dependent CORE evidence / CORE acceptance scoped BLOCKED，保留 implementation 狀態，不稱 implementation blocker。

## DEFERRED_NOT_THIS_TASK
- 不追求雲端等價，不宣稱 Windows/Ollama 實機已驗證；不加模型或平台分支。
- S3/S5 配對 E2E 僅資源允許時於 CORE 後執行；S4 極小尾塊合併需獨立測試且非本波阻斷路徑。
- 不改 Gemini/cloud prompts、共享 refinement 值、`task_processor.py:259/262`、既有 quality thresholds、off/no-log semantics 或 post-processing order。

## REPO_ANCHOR
- Project root: `/Users/hsiaojohnny/dev/convert`
- Branch: `fix/qwen-local-quality-parity`
- Anchor HEAD: `ab2528b718e8c91557fd72db174b0f9bb78d3a68`
- Relevant dirty state: 本 TASK_ID 目錄為唯一已知 untracked 路徑；尚無產品碼／測試變更。Stage 04 必須重新檢查，保留新出現的 user work。
- Drift since plan/review: rev6 plan 與 attempt-06 approved snapshot/report SHA 相符；舊 rev4 handoff 已封存為 `handoff-history/handoff-plan-r4-20260923-223707.md`。rev4 Stage 04 preflight 已因 dirty-worktree premise escalation；rev6 以 detached clean worktree + raw-cache/redacted-derivative contract 解決，fresh Stage 04 必須重新 preflight。

## CURRENT_STATE_DELTA
Stage 02 attempt-06 核准 PLAN_REVISION 6 / SHA 上列。Stage 03 已編譯本 handoff。前次 rev4 Stage 04 僅記錄 preflight escalation，未改產品／測試／runner、未跑 baseline、backend、model inference 或 E2E；Stage 04 應在既有 `execution.md` 追加 rev6 記錄，不覆寫舊 `ESCALATED` / `REPLAN_REQUIRED` 歷史。

## MUST_READ_PLAN
FIRST_ACTION 前讀 Plan §§0.4、1–3、5–8.2、9–11，尤其 §8.1 驗證矩陣／status routing、§8.2 fixtures、§8 E2E raw-cache/redaction 路徑與 Stage05 後 push 條件；修改 supporting 項才需讀 §4。

## SETTLED_DO_NOT_REOPEN
- RV-001–RV-007 / I1–I13：依 rev6 結案紀錄與 attempt-06 審查；不得無計畫地改成本／門檻／狀態語意、clean-worktree 策略或 raw/redacted 邊界。
- CORE-1/C1b/C1c：4500 token ceiling、E2E extraction budget 4、全 chunks preflight 與單次摘要安全降級。
- CORE-2/C2a/C2b/C2c：事實級期望與逐項處置；local-only round/budget；雲端 loop/prompt/output 不變。
- CORE-3：deterministic registry-first/unique-candidate normalization；四場 E2E 關閉；不得讀 checklist。
- CORE-A–F、噪聲帶／n=2 集合門檻、CORE-E 成本上限、commit provenance 与 all-off rollback 依 Plan §§5–8 固定，不得事後調門檻。
- Stage 05 後才 push；Stage05 FAIL/BLOCKED 或任何 required checks 未閉合時不可發布、不可 force-push；遠端前進／push 拒絕時停下，不自行改寫歷史。

## REVERIFY_ON_START
- 核對 TASK_ID、rev6 plan hash、attempt-06 exact approved hash、handoff hash 與 branch/HEAD；重查 git status 並辨識新的 user work。
- 產品碼／測試變更前執行完整 baseline：`DATA_DIR=/tmp/test_scratch uv run --frozen pytest tests/ -q`；保存命令、exit code、摘要與必要 log。中斷的 pytest 不算 baseline；依 Plan §8.1 路由不可得情形。
- 核對當前 local/cloud loop、chunk、config/default、tests 與 `.env.example` 落點；若證據推翻根因或語意前提，依 escalation 回 Stage 01，不要機械沿用落點圖。
- E2E 前檢查固定素材／模板／quality mode、模型可用性、四個 cache keys/子目錄不存在且 `git check-ignore` 命中、產品測試 commit SHA、detached HEAD/zero porcelain 與 runner health revision；不符合即依 scoped blocker 停止。

## TRIGGERED_POLICIES
- `goal-alignment-design-economy.md`
- `testing-verification.md`
- `workflow-routing.md` §7
- `security-privacy.md`
- `dependencies-contracts.md`
- `debugging-recovery.md`（僅當 implementation/baseline 出現新 bug 或回歸時）

## FIRST_ACTION
Fresh Stage 04 先核對本 handoff 與 approved rev6 plan/review hash、repo state，並在任何產品碼／測試 mutation 前取得完整 `FULL-SUITE` baseline。接著按 Plan §9 由 CORE-3 的 deterministic zero-model-call contracts 開始；S1 仍可省略，不能延誤 CORE。

## IMPLEMENTATION_WAVES
- Gate/precondition: complete pre-mutation `FULL-SUITE` baseline; missing baseline routes to Plan §8.1, not guessed clean.
- Optional S1 (SUPPORTING, projection-only, no new threshold), then CORE-3 deterministic normalization → CORE-2 local adherence/refinement → CORE-1 extraction coverage/budget. 每個 slice 先零模型呼叫驗證；新增 controls 要 focused tests、`.env.example` 文件化、all-off byte rollback。
- Commit 僅產品碼＋測試後，固定該 SHA 建 detached clean worktree；按 Plan §8 step 5 跑 GEMMA-E1/E2、QWEN-E1/E2，順序 Gemma×2 再 Qwen×2。raw `--artifacts-dir` / `--runtime-dir` 分別置於主工作樹 gitignored `data/cache/e2e/p7c-{gemma,qwen}-e{1,2}/{runner-output-raw,runtime-raw}`；task evidence 只能是 Plan §8 allowlist derivative。若四場前提或隱私界線失效，立即停止後續 E2E、記 scoped blocker/replan。
- Stage 05 必須 fresh、read-only，獨立核驗 CORE、原件 hashes、狀態 fixtures 與 Stage04 snapshot；S3/S5 僅核心之後且資源允許。
- 只有 Stage 05 PASS、required checks closure-satisfied、無待修或 blocker 才 push 本機產品/測試 commit 至 `origin/fix/qwen-local-quality-parity`；不 force-push，遠端前進或拒絕即停並回報。

## ACCEPTANCE_CONTRACT
每一 check 在 Stage 04 `execution.md` 獨立記錄；初始 `CHECK_RESULT=NOT_RUN`、`WAIVER_STATUS=NOT_ALLOWED`、`WAIVER_ALLOWED=NO`、`WAIVER_AUTHORITY=NONE`。

| CHECK_ID | COMMAND/SCENARIO | GOAL_CRITICALITY | EVIDENCE_ROLE | CLOSURE_GATE | BASELINE_RULE | FAILURE_ROUTING | WAIVER_ALLOWED | WAIVER_AUTHORITY |
|---|---|---|---|---|---|---|---|---|
| CORE-A | 2 Gemma E2Es；交付層尾段 7 facts 人工 + `rg` 對 Plan §5 | CORE | OUTCOME | HARD_CLEAN | None | 門檻不符=FAIL；必要 evidence 無效/缺場=BLOCKED；未跑=NOT_RUN | NO | NONE |
| CORE-B | `measure_coverage.py`；2 Gemma records | CORE | OUTCOME | HARD_CLEAN | None | 依 §5 集合門檻；無效量測不得估補 | NO | NONE |
| CORE-C | `measure_coverage.py`；2 Qwen 3.8 records | CORE | OUTCOME | HARD_CLEAN | None | 低於門檻=FAIL；缺有效場次=BLOCKED/NOT_RUN | NO | NONE |
| CORE-D | `measure_record_quality.py`；2 Qwen records | CORE | OUTCOME | HARD_CLEAN | None | 每場條目／字數集合門檻不符=FAIL | NO | NONE |
| CORE-E | 呼叫/輪數 metrics + `run_summary.json` started→finished wall clock | CORE | OUTCOME | HARD_CLEAN | 同路徑 P7-B baseline | 超固定上限=FAIL；baseline/log/timing 無效=BLOCKED | NO | NONE |
| CORE-F | DOCX exists + `unzip -t` | CORE | OUTCOME | HARD_CLEAN | None | 缺失/無效=FAIL；環境阻斷=BLOCKED | NO | NONE |
| ANTI-GAMING | Stage05 抽驗 2–3 新命中 facts；查 prompt 無 checklist token | CORE | OUTCOME | HARD_CLEAN | None | 無來源事實或 token 洩漏=FAIL；來源證據不足=BLOCKED | NO | NONE |
| LOCAL-CONTRACTS | local/cloud isolation、budget、off/no-log、degradation、順序 contracts | CORE | MUST_NOT_BREAK | HARD_CLEAN | None | 語意 regression=`TASK_REGRESSION` | NO | NONE |
| BYTE-ROLLBACK | 所有新 controls off 與固定 P7-B / `ab2528b` 比較 | CORE | MUST_NOT_BREAK | HARD_CLEAN | Fixed P7-B baseline | 不一致=`TASK_REGRESSION`；不可比=BLOCKED | NO | NONE |
| PLATFORM-INDEPENDENCE | 靜態/測試查無模型/平台 branch | CORE | MUST_NOT_BREAK | HARD_CLEAN | None | 發現禁用 branch=`TASK_REGRESSION`；Windows/Ollama 實機仍 `[UNVERIFIED]` | NO | NONE |
| FULL-SUITE | 同條件變更前後 `DATA_DIR=/tmp/test_scratch uv run --frozen pytest tests/ -q` | SUPPORTING | REPOSITORY_HEALTH | BASELINE_DELTA | Complete pre-mutation baseline | 相同未惡化 failure=`PRE_EXISTING_FAILURE` 並披露；新增/惡化相關 failure=`TASK_REGRESSION`；無 baseline=BLOCKED/INCOMPLETE | NO | NONE |
| STATUS-CONTRACT-FIXTURES | Plan §8.2 全套 deterministic fixtures；Stage05 獨立複核 | SUPPORTING | MUST_NOT_BREAK | HARD_CLEAN | None | fixture mismatch/矛盾接受/證據抹除=`TASK_REGRESSION`；不能閉合 DONE | NO | NONE |
| E2E-COMMIT-PROVENANCE | 四場前後 detached HEAD/porcelain、expected/actual health revision、ignored raw paths、allowlist/hash | SUPPORTING | MUST_NOT_BREAK | HARD_CLEAN | None | provenance/privacy fail ⇒ 該 check + 受影響 E2E-dependent CORE/CORE acceptance scoped BLOCKED；保留 implementation | NO | NONE |
| STAGE05-INDEPENDENT | Fresh read-only 驗 CORE、來源輸出/DOCX/cost/status snapshot | CORE | OUTCOME | HARD_CLEAN | Stage04 execution SHA/status snapshot | 證據結果可 FAIL；環境阻斷 scoped BLOCKED；load-bearing premise invalid ⇒ REPLAN_REQUIRED | NO | NONE |

Stage 04 `execution.md` 記錄六個正交欄位 `PRIMARY_OUTCOME_STATUS`、`IMPLEMENTATION_STATUS`、`CORE_ACCEPTANCE_STATUS`、`REQUIRED_VERIFICATION_STATUS`、`INDEPENDENT_ACCEPTANCE_STATUS`、`TASK_CLOSURE_STATUS`，以及每 check evidence、scope/blocker、next step、plan/handoff hashes 與時間。Stage05 開始時保存 Stage04 execution SHA 與三項 immutable reported statuses，不修改 execution。`DONE` 僅當 Plan §7.10 全部成立。Push 是 Stage05/required verification 閉合後的交付步驟；remote diverged/rejected 時停止，不自行 rebase/force-push。

## STOP_AND_ESCALATE_IF
- 新證據推翻 root cause、fixed input/chunk/call-count 或其他 load-bearing premise；不得暗中增加 budget、替換素材/模型、移動或刪除 task evidence。
- 需要改 requiredness、validity、gate/veto、stable errors、fallback/degradation、priority、雲端行為、既有 off/no-log 或 processing order；寫 `escalation.md` 回 Stage 01/review/handoff loop。
- E2E chunks >4、clean detached precondition/health revision mismatch、raw data 未隔離或 redaction allowlist 不可滿足：停止後續 E2E，保留原件/歷史，具名記錄 scoped blocker；不修改 runner、ignore/exclude/config 或事後調 threshold。
- CORE regression/quality/cost fail、有效基線缺失、必要輸入／工具／環境或 Stage05 evidence 缺失：依 Plan §8.1 記 FAIL/BLOCKED/NOT_RUN 及 closure route；不把 verification blocker 寫成 `IMPLEMENTATION_BLOCKED`。
- Stage05 未 PASS、required checks 未閉合、remote 已前進或 push 被拒：不可宣稱 DONE/不可 force-push；依核准路由修復或報 scoped blocker。

## HISTORICAL_TASK_DEPENDENCIES
- `T20260923-1810-01-local-record-fidelity-density` (P7-B)：僅 Plan §1–2/5 中指定的 P7-B quality/coverage/density/cost evidence 與 §11 中明確引用的 supporting evidence；只查該等指定檔案/區段。

TASK_ID: T20260923-2050-01-local-extraction-adherence
HANDOFF_PATH: `.agent/tasks/T20260923-2050-01-local-extraction-adherence/handoff.md`
PLAN_REVISION: 6
STATUS: READY_FOR_IMPLEMENTATION
NEXT_STAGE: 04_IMPLEMENT
