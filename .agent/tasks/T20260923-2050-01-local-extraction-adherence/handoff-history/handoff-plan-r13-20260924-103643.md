# Handoff — P7-C product-profile parity and independent acceptance

## TASK
- TASK_ID: T20260923-2050-01-local-extraction-adherence
- STATUS: READY_FOR_IMPLEMENTATION
- PLAN_PATH: `.agent/tasks/T20260923-2050-01-local-extraction-adherence/plan.md`
- PLAN_REVISION: 13
- PLAN_SHA256: `2b663c68a8f49d0cf769000db5d6d7629c54e589bff8e6c62a5168bc34fbe588`
- REVIEW_REQUIRED: YES
- REVIEW_REPORT: `.agent/tasks/T20260923-2050-01-local-extraction-adherence/review/attempt-14/review_report.md`
- REVIEWED_PLAN_REVISION: 13
- REVIEWED_PLAN_SHA256: `2b663c68a8f49d0cf769000db5d6d7629c54e589bff8e6c62a5168bc34fbe588`
- INDEPENDENT_ACCEPTANCE_REQUIRED: YES
- E2E_REQUIRED: YES
- ACCEPTANCE_MODE: E2E
- Fresh Implementer required: YES
- Planner/Reviewer transcript required: NO

## GOAL_ANCHOR
PRIMARY_OUTCOME (CORE): after persistence, Gemma 4 31B and Qwen 3.8 27B product-local summaries on the same canonical transcript are each within a strict `<10%` gap of the Gemini 3.5 Flash-lite median on every frozen quality dimension.
SUCCESS_EVIDENCE (CORE): candidate cloud/Gemma/Qwen 3x3 passes; only then the approved six local-only defaults are persisted, a new commit produces a fresh final 3x3, and Stage 04 plus fresh Stage 05 independently score all final outputs with valid provenance and no hard factual error.
MUST_NOT_BREAK: cloud/local isolation, explicit `.env`/process precedence, model/input/config/rubric provenance, no partial complete record, raw/derivative privacy separation, rollback, and orthogonal truthful status routing.
SUPPORTING: two true local audio-to-DOCX runs after final parity; ASR/DOCX details, timing, counts, and near-duplicate metrics do not replace or veto valid parity quality evidence. BEST_EFFORT/non-goals remain cross-meeting generalization, Windows/Ollama, Qwen 3.6, extra ablations, and lowered/favorable thresholds.

## CRITICAL_PATH
Fresh Stage 04 verifies Plan/review/handoff identity and current state → freezes and hashes the rubric at the exact task-scoped path `.agent/tasks/T20260923-2050-01-local-extraction-adherence/quality/rubric-v1.md` before any output is generated or read → runs candidate 3x3 with fixed transcript/checklist/profile and blind scoring → on candidate PASS only, persists the six approved local-only defaults and proves precedence/cloud isolation with focused, rollback, and baseline-delta checks → commits the product/test change → runs a new final 3x3 with effective settings and independent scoring → runs the two supporting audio-to-DOCX E2E checks → fresh Stage 05 read-only acceptance and closure.

## SEMANTIC_INVARIANTS
- Do not change Plan §0.13 rubric dimensions, `<10%` formula, hard-fail rules, blind aliases/disagreement handling, checklist, models, transcript, template, or provenance requirements. Candidate scores cannot close the task or substitute for final scores.
- Candidate profile is exactly: chunk ceiling `4500`; extraction call budget `4`; fact coverage `true`; adherence `true`; local refinement rounds `3`; refinement call budget `3`; proper-noun normalization `false`; existing coverage mode/categories remain `enforce` / `topic,decision,number,date`.
- Persistence may touch only the six approved local-only defaults in `backend/core/config.py` and matching `.env.example` documentation. Do not alter user `.env`, process environment, cloud/provider/model settings, shared refinement defaults, or model identity. Process environment > `.env` > code defaults; an override that differs from the profile makes `PROFILE-PERSISTENCE: BLOCKED` and requires owner direction, not silent rewriting.
- Keep extraction/refinement budgets atomic and local; never emit partial notes as a complete record, globally veto unrelated service work, or change validity/requiredness/gating/error/fallback/priority/status semantics. Preserve the approved byte rollback, local/cloud isolation, platform-independence, and privacy contracts.
- Freeze linkage must be explicit: the task-scoped rubric file is created and SHA-256 recorded before first output generation/read; `execution.md` and Stage 05 evidence must link rubric version/hash to Plan revision/hash, canonical transcript hash, and checklist hash. The reviewer’s RV-014-01 is resolved only as this exact path/linkage clarification; rubric semantics are unchanged.
- Raw outputs, prompts, responses, transcript/DOCX, filenames, absolute paths, and identity mappings stay in unique gitignored cache. Task artifacts contain only the Plan-approved allowlist: aliases, hashes, IDs, booleans, approved non-sensitive settings, classifications, and minimal score/status data.

## BEST_EFFORT_DO_NOT_GATE
- Candidate 3x3 is a conditional decision gate, not final acceptance; supporting timings/counts/near-duplicates and the two post-parity true E2E runs do not independently pass or veto primary parity.
- E2E helper/provenance checks are SUPPORTING integration gates: a failure blocks the affected integration evidence only and does not retroactively invalidate valid same-transcript parity or product implementation.
- Windows/Ollama, cross-meeting generalization, Qwen 3.6, extra S3/S5 ablations, and historical P7-C controls are not prerequisites. Do not spend work on them before CORE candidate/final quality.

## DEFERRED_NOT_THIS_TASK
- No runner repair, new product semantic redesign, rubric/checklist/threshold change, model download or substitution, favorable resampling, extra meeting, Qwen 3.6 run, or claim of broad parity/generalization.
- Do not push until fresh Stage 05 PASS and every required closure gate is satisfied; the conditional post-acceptance publish authority is not a Stage 04 prerequisite.

## REPO_ANCHOR
- Project root: `/Users/hsiaojohnny/dev/convert`
- Branch: `fix/qwen-local-quality-parity`
- Anchor HEAD: `a576b06fc93c4a9d057ae59cdb54f07426ce343d` (ahead of origin by one commit)
- Relevant dirty state: tracked product/test tree is clean; the only observed porcelain is untracked `.agent/tasks/T20260923-2050-01-local-extraction-adherence/`, which must be preserved. The prior handoff was archived at `.agent/tasks/T20260923-2050-01-local-extraction-adherence/handoff-history/handoff-plan-r11-20260924-092557.md` before this replacement (old SHA-256 `aa114a9b769d91a80b4f90f9303f3874570656bdc3d1a3899428c5f04d9d2d2c`).
- Drift since plan/review: none observed. Current Plan and attempt-14 snapshot are byte-identical at the approved SHA; task `quality/` directory/rubric artifact does not yet exist and must be created only at the frozen Stage 04 first action. No product files were changed for this handoff.

## CURRENT_STATE_DELTA
The latest durable execution state is historical attempt-03. Its W0 helper build/probe passed (`exit=0`, locale/transcriber/asset booleans true), but `GEMMA-E1R` then ended with runner exit `1`, `RUN_RESULT: BLOCKED`, `INFERENCE_REACHED: UNPROVEN`, zero runner/runtime artifacts, preserved raw cache, and stop reason `runner_nonzero_or_exception`; remaining runs are `NOT_RUN`. Detached HEAD and pre/post porcelain were empty. It is not a model-quality result and must not be scored or rewritten. Fresh Stage 04 must append the terminal-state acknowledgement to `execution.md` before new work. No valid current P7-C quality sample exists; Stage 05 has not run.

## MUST_READ_PLAN
Before first action, read Plan §§0.12–0.13 (goal and scoring freeze), §§1–4 (goal, root cause, approved controls, supporting/non-goal boundaries), §5 (canonical input/profile, rubric, final parity formula and gates), §§6–7 (rollback and compatibility invariants), §8 and §8.1–8.2 (candidate/persistence/final/E2E sequence, privacy, status matrix/fixtures), §§9–10 (budget and stop conditions). Read §11 only for historical design provenance; do not reopen it.

## SETTLED_DO_NOT_REOPEN
- `PLAN_REVISION=13` and review attempt-14 approval at the exact Plan SHA; no earlier review/handoff is valid.
- RV-014-01 is resolved at handoff level by the exact task-scoped rubric path and hash-linkage requirement; do not alter rubric scoring semantics or revise the Plan for this path clarification.
- Candidate-before-persistence, persistence-before-final, and final-before-supporting-E2E ordering; candidate outputs/scores never substitute for final outputs/scores.
- Fixed canonical transcript/checklist/model IDs, six candidate values, strict `<10%` per-output/per-dimension rule, hard-fail behavior, privacy boundary, status-contract routing, and conditional post-Stage05 publish authority.
- Attempt-03 is preserved as a blocked runner launch, not a quality FAIL; do not reuse its cache or overwrite its evidence.

## REVERIFY_ON_START
- Recompute Plan SHA and confirm revision 13, attempt-14 `FINAL_STATUS: PLAN_APPROVED`, reviewed revision/SHA, byte-identical snapshot, and this handoff’s revision/hash before any output generation/read.
- Recheck branch, HEAD, porcelain, and product/test diff; preserve new user work. Confirm fixed transcript/checklist hashes, template/mode, model keys, and effective settings before each cohort.
- Confirm `.agent/tasks/T20260923-2050-01-local-extraction-adherence/quality/rubric-v1.md` is not already present; create/freeze it once, before any output, with the required Plan/transcript/checklist linkage and SHA. If it exists or differs after freeze, stop and replan.
- Use unique gitignored raw-cache/output paths; never precreate runner-owned directories, reuse attempt-03 cache, inspect/copy raw payload into task artifacts, or expose sensitive input names/content.
- Before persistence, establish the same-HEAD complete `FULL-SUITE` baseline required by Plan §8; after approved changes verify focused contracts, byte rollback, and `FULL-SUITE` `BASELINE_DELTA`. Do not infer a baseline.

## TRIGGERED_POLICIES
- `testing-verification.md`
- `workflow-routing.md` §7 Status Semantics Contract v2
- `security-privacy.md`
- `dependencies-contracts.md` for config/semantic-contract preservation
- `debugging-recovery.md` on any new failure or invalidated premise
- `git-change-hygiene.md` only for the already-authorized conditional post-Stage05 commit/push

## FIRST_ACTION
Fresh Stage 04 must verify the exact Plan/review/snapshot/handoff hashes, append the attempt-03 terminal-state record, and confirm current branch/HEAD/dirty/product-diff state. Then freeze and hash the rubric at `.agent/tasks/T20260923-2050-01-local-extraction-adherence/quality/rubric-v1.md` before generating or reading any candidate output. The first candidate wave is the fixed cloud/Gemma/Qwen 3x3 with blind aliases; do not run supporting E2E or persist defaults before candidate PASS.

## IMPLEMENTATION_WAVES
- **W0 — CORE decision-validity setup:** terminal-state acknowledgement, fixed input/model/settings/provenance checks, task-scoped rubric freeze/hash, privacy/cache isolation, and baseline preconditions.
- **W1 — CORE candidate quality:** cloud/Gemma/Qwen 3x3 on the canonical transcript using the exact profile; anonymous aliases and two independent blind scorers. Candidate FAIL or invalid evidence routes to fresh Stage 01/02/03 replan.
- **W2 — CORE profile persistence and contracts (conditional on W1 PASS):** modify only the six approved local defaults plus `.env.example`; prove precedence, local/cloud isolation, rollback, focused contracts, and `FULL-SUITE` baseline delta; create the product/test commit.
- **W3 — CORE final quality (conditional on W2 closure):** fresh cloud/Gemma/Qwen 3x3 using effective persisted settings and new aliases; independent scoring and strict per-output/per-dimension thresholds. Any valid shortfall/hard fail routes to replan.
- **W4 — SUPPORTING integration:** only after W3 PASS, run one Gemma and one Qwen audio-to-DOCX E2E with unique ignored caches and provenance; helper/provenance failure scopes to integration evidence.
- **W5 — CORE independent acceptance:** fresh Stage 05 read-only scoring/closure, preserving Stage 04 execution hash/status snapshot. No push before Stage 05 PASS and all required checks closure-satisfied.

## ACCEPTANCE_CONTRACT
Every material check preserves `CHECK_ID | COMMAND/SCENARIO | GOAL_CRITICALITY | EVIDENCE_ROLE | CLOSURE_GATE | BASELINE_RULE | FAILURE_ROUTING | WAIVER_ALLOWED | WAIVER_AUTHORITY`, plus `CHECK_RESULT` and `WAIVER_STATUS`; Plan gates are non-waivable (`WAIVER_ALLOWED: NO`, `WAIVER_AUTHORITY: NONE`).

- `RUBRIC-FREEZE`: task-scoped rubric exists and is hash-linked before any output; CORE / OUTCOME / HARD_CLEAN / no baseline; missing, changed, or unlinked artifact is BLOCKED or REPLAN_REQUIRED, never guessed.
- `CANDIDATE-PARITY`: fixed cloud/Gemma/Qwen 3x3, anonymous blind scoring, each local output and dimension strict `<10%` versus Gemini median, no hard fail; CORE / OUTCOME / HARD_CLEAN / no baseline; candidate shortfall is `FAIL` and routes to evidence-backed Stage 01 replan, not persistence.
- `PROFILE-PERSISTENCE`: only approved six local defaults plus `.env.example`, effective values match, explicit overrides remain authoritative, local/cloud isolation and rollback pass; CORE / MUST_NOT_BREAK / HARD_CLEAN; override mismatch is scoped `BLOCKED`/authority decision, not silent mutation.
- `FINAL-PARITY`: new persisted-profile cloud/Gemma/Qwen 3x3 independently scored with the same frozen rubric, hashes and provenance; CORE / OUTCOME / HARD_CLEAN / no baseline; valid gap `>=10%`, hard fail, or invalid identity routes to FAIL or BLOCKED per evidence, then Stage 01 replan.
- `LOCAL-CONTRACTS` and `BYTE-ROLLBACK`: fixed-product focused semantic/isolation/budget/fail-safe and all-off byte comparisons; CORE / MUST_NOT_BREAK / HARD_CLEAN / approved fixed-commit baseline; regression is TASK_REGRESSION, missing evidence BLOCKED.
- `FULL-SUITE`: same-HEAD pre-mutation baseline versus post-change suite; SUPPORTING / REPOSITORY_HEALTH / `BASELINE_DELTA` / baseline required; same/non-worse signatures are disclosed pre-existing failures, new/worse signatures are TASK_REGRESSION, unusable baseline is INCOMPLETE.
- `E2E-ASR-HELPER-PREFLIGHT` and `E2E-COMMIT-PROVENANCE`: only for the two post-parity supporting E2E runs; SUPPORTING / DIAGNOSTIC or MUST_NOT_BREAK / HARD_CLEAN; helper/provenance failure blocks affected integration evidence only and does not erase valid parity scores.
- `STAGE05-INDEPENDENT`: fresh read-only verification of final score sheets, hashes, effective config, supporting E2E, status fixtures, and privacy; CORE / OUTCOME / HARD_CLEAN; preserve Stage 04 snapshot and route environment blockers to independent `BLOCKED`, product defects to `FAIL`, premise invalidation to `REPLAN_REQUIRED`.
- `STATUS-CONTRACT-FIXTURES`: unique IDs and §7 routes recomputed independently; SUPPORTING / MUST_NOT_BREAK / HARD_CLEAN; mismatch or missing evidence is TASK_REGRESSION/BLOCKED. Preserve all six orthogonal statuses; never collapse verification or acceptance blockers into implementation status.

## STOP_AND_ESCALATE_IF
- Any Plan/review/handoff hash or revision mismatch, unexpected product/branch/HEAD/dirty-state drift, input/model/config/rubric identity mismatch, privacy leak, reused/colliding cache path, or missing provenance: do not generate, score, launch, or persist; stop and route to Stage 01/02/03 as applicable.
- Candidate/final rubric or model evidence invalid, candidate/final quality shortfall, hard factual error, or effective override mismatch: preserve allowlisted evidence and replan; never lower thresholds, resample, or silently rewrite settings.
- Any proposed product/runner change outside the approved six-default persistence wave, or any change to validity, requiredness, gating/veto, error/fallback, priority, privacy, or status semantics: stop, write escalation evidence, and return to Stage 01/02/03.
- Any helper/provenance/E2E task failure: preserve raw cache and allowlisted derivative, stop the affected integration wave, and keep parity status separate. Do not count model-load-only or incomplete tasks as inference/quality evidence.
- Missing/incomparable baseline, full-suite regression, unresolved required check, Stage 05 not PASS, or remote divergence at authorized publish time: no DONE and no push; no force-push/rebase/history rewrite.

## HISTORICAL_TASK_DEPENDENCIES
- `T20260923-1810-01-local-record-fidelity-density`: only the explicitly cited P7-B quality/coverage/density/cost evidence in Plan §§1–2, 5, and 11; it is historical evidence, not current acceptance.
- `T20260922-2037-02-local-model-quality-parity`: only the explicitly cited canonical fact checklist artifact/hash in Plan §5; do not copy or mutate that task’s artifact.

TASK_ID: T20260923-2050-01-local-extraction-adherence
HANDOFF_PATH: `.agent/tasks/T20260923-2050-01-local-extraction-adherence/handoff.md`
PLAN_REVISION: 13
STATUS: READY_FOR_IMPLEMENTATION
NEXT_STAGE: 04_IMPLEMENT
