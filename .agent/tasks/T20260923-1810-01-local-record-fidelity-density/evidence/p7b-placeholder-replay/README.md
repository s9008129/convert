# P7-B SUPPORTING-1：佔位符正規化在既有交付紀錄上的離線重播

- 產生時間：2026-09-23（Asia/Taipei）；分支 `fix/qwen-local-quality-parity`
- 儀器：產品自身純函式 `backend.core.text_postprocess.normalize_unfilled_placeholders_ext`
  （確定性；**0 次模型呼叫**、無網路、無 I/O 副作用）
- 原始輸出：`replay.json`（同目錄）
- 量測定義：**同一行內相鄰**（可夾空白、不含換行）的兩個「（待確認）」，半形 `(待確認)` 視為同一符號

## 結果

| 交付紀錄 | 相鄰重複（前 → 後） | 被改動的行 | 位元組變化 | 冪等 |
|---|---|---|---|---|
| qwen 3.8 27B／P6-A E6b | **2 → 0** | 3、4 | −10 | 是 |
| gemma 4 31B／P7-A E7C | **1 → 0** | 3 | −5 | 是 |

* 被改動的行都是「標題行／開頭欄位」的相鄰重複（例：
  `（待確認）（待確認）年…` → `（待確認）年…`），與計畫 §6 SUPPORTING-1 的作用域一致。
* `changed_lines` 之外**一個位元組都沒動**（見 `replay.json`），且函式冪等。
* 量測踩雷紀錄（誠實登錄）：第一版重播把「行尾（待確認）＋下一行行首（待確認）」也算成相鄰
  （`\s*` 會吃換行），得到「2 → 1」的假殘留；改為**行內**量測後為 2 → 0／1 → 0。
  本 README 與 `replay.json` 皆為修正後版本。
* 未涵蓋（已知）：E6 場 gemma 另 2 處「（（待確認））」出現在彙整表**第 1 欄（案由）**，
  依計畫「僅第 2 欄」的作用域**不會**被本步驟修正（由
  `tests/test_t20260923_p7b_placeholder_normalize.py` 明文釘住）；如需涵蓋第 1 欄須回 Planner 決策。
