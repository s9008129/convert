# 🚀 會議轉錄工具 - 跨平台 Docker 服務系統開發及實作規劃

> **專案代號**: MeetingScribe Docker  
> **版本**: v2.1.3  
> **建立日期**: 2025-11-29  
> **最後更新**: 2025-11-29  
> **架構師**: AI System Architect  
> **目標**: 一包帶走，直接 Docker 部署  
> **狀態**: ✅ 開發完成，已進入部署測試階段

---

## 📑 目錄

1. [執行摘要](#執行摘要)
2. [第一性原理分析](#第一性原理分析)
3. [現況問題診斷](#現況問題診斷)
4. [解決方案總覽](#解決方案總覽)
5. [系統架構設計](#系統架構設計)
6. [前端設計規劃](#前端設計規劃)
7. [後端設計規劃](#後端設計規劃)
8. [Docker 部署架構](#docker-部署架構)
9. [雙模式處理方案（本地/雲端）](#雙模式處理方案本地雲端)
10. [智能裝置偵測與降級機制](#智能裝置偵測與降級機制)
11. [友善管理腳本設計](#友善管理腳本設計)
12. [實作計劃與里程碑](#實作計劃與里程碑)
13. [風險評估與對策](#風險評估與對策)
14. [附錄](#附錄)

---

## 執行摘要

### 📌 一句話說明這個專案

**將會議音訊/視訊自動轉換成結構化會議記錄的工具，打包成 Docker 映像後，可以在任何電腦上「一包帶走，直接部署」，不再需要每次都設定環境。**

### 🎯 核心目標

| 目標 | 說明 |
|------|------|
| **一包帶走** | Docker 映像包含所有依賴，不需要額外下載 Python、CUDA、Whisper 等 |
| **跨平台支援** | 支援 macOS (MPS)、Windows (CUDA)、Linux (CUDA) |
| **零環境問題** | 不同電腦部署時，不會遇到環境設定問題 |
| **友善使用** | 提供現代化網頁介面，非技術人員也能輕鬆使用 |
| **雙模式選擇** | 本地處理（機敏資料）或雲端處理（高品質）兩種方案 |
| **智能降級** | GPU 不可用時自動降級到 CPU，確保服務不中斷 |
| **服務隔離** | 絕對不影響 Docker 中其他運行的服務 |

### 🆕 v2.0.0 重大更新

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      v2.0.0 優化重點                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ✅ Ollama 外部連接：使用主機已安裝的 Ollama，不打包進 Docker           │
│  ✅ 服務隔離：獨立網路，不影響 Docker 中其他服務                        │
│  ✅ 雙模式選擇：本地 (Gemma3:12B) / 雲端 (Gemini API)                    │
│  ✅ 智能裝置偵測：自動偵測 GPU/MPS/CPU，資源不可用時自動降級            │
│  ✅ 友善管理腳本：非技術人員可輕鬆設定 API Key 和重啟服務               │
│  ✅ 安全機制：API Key 透過安全環境變數處理                              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 🏆 預期成果

```
部署前（現況）                    部署後（目標）
───────────────────              ───────────────────
❌ 每台電腦要裝 Python            ✅ 只需要 Docker + Ollama
❌ 要處理 CUDA 版本相容           ✅ Docker 內已配置好
❌ 要下載 Whisper 模型            ✅ 模型已打包在映像中
❌ 不同電腦有不同問題             ✅ 統一環境，零問題
❌ 需要技術人員協助               ✅ 雙擊啟動，網頁操作
❌ 只能本地處理                   ✅ 本地/雲端雙模式選擇
❌ GPU 故障服務中斷               ✅ 自動降級到 CPU 繼續處理
```

### 🔒 隱私與安全設計

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        雙模式隱私保護設計                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  🔒 方案 A：本地模式（機敏資料）                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│  • 所有處理都在本機完成                                                 │
│  • 音訊和逐字稿不會上傳到任何伺服器                                     │
│  • 使用本地 Ollama + Gemma3:12B 模型                                    │
│  • 適合：政府機關、醫療記錄、法律文件、商業機密                         │
│                                                                         │
│  ☁️ 方案 B：雲端模式（非機敏資料）                                       │
│  ─────────────────────────────────────────────────────────────────────  │
│  • 本地完成語音轉錄（Whisper）                                          │
│  • 逐字稿傳送至 Google Gemini API 生成摘要                              │
│  • 更高品質的摘要結果                                                   │
│  • 適合：一般會議、教育訓練、公開活動                                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 第一性原理分析

### 🔍 問題本質：您真正的需求是什麼？

讓我們從最根本的角度思考：

```
使用者真正的需求
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│   「我有一段會議錄音，我需要快速得到會議記錄」           │
│                                                         │
│   不是：我需要安裝 Python                               │
│   不是：我需要設定 CUDA                                 │
│   不是：我需要學習命令列                                │
│   不是：我需要解決環境問題                              │
│                                                         │
│   只是：把檔案丟進去 → 自動處理 → 拿到會議記錄          │
└─────────────────────────────────────────────────────────┘
```

### 🧩 拆解核心問題

#### 問題一：環境依賴地獄

```
目前的痛點：

電腦 A（Windows）          電腦 B（Windows）          電腦 C（macOS）
├── Python 3.11           ├── Python 3.9            ├── Python 3.12
├── CUDA 11.8             ├── CUDA 12.0             ├── 無 CUDA (MPS)
├── cuDNN 8.6             ├── cuDNN 8.9             ├── Metal
└── 需要不同的設定        └── 需要不同的設定        └── 需要不同的設定

每台電腦都要花數小時設定 → 非常痛苦
```

**解決思路**：把所有環境打包成 Docker 映像，環境跟著容器走。

#### 問題二：模型下載耗時

```
目前的痛點：

每次部署都要下載：
├── Whisper large-v3 模型 ≈ 3GB
├── Ollama 模型 ≈ 5GB
└── Python 套件 ≈ 2GB

總計 ≈ 10GB，網路慢時要下載數小時
```

**解決思路**：模型預先載入 Docker 映像，或使用 Docker Volume 持久化。

#### 問題三：操作門檻高

```
目前的痛點：

使用者需要會：
├── 開啟命令列
├── 輸入指令
├── 理解錯誤訊息
└── 排除問題

對非技術人員來說太困難
```

**解決思路**：提供網頁介面，拖放檔案即可使用。

### 💡 解決方案的第一性原理

```
┌─────────────────────────────────────────────────────────────────────┐
│                      解決方案核心原則                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  原則 1：容器化                                                      │
│  ──────────────────────────────────────────────────────────────     │
│  「環境問題」的根本原因是環境不一致                                  │
│  → 解法：把環境固化在容器中，讓環境跟著應用走                        │
│                                                                     │
│  原則 2：預載依賴                                                    │
│  ──────────────────────────────────────────────────────────────     │
│  「下載耗時」的根本原因是每次都要重新取得資源                        │
│  → 解法：把所有資源打包進映像，或持久化到 Volume                     │
│                                                                     │
│  原則 3：使用者友善                                                  │
│  ──────────────────────────────────────────────────────────────     │
│  「操作困難」的根本原因是需要技術知識                                │
│  → 解法：提供圖形化介面，隱藏技術細節                                │
│                                                                     │
│  原則 4：平台抽象                                                    │
│  ──────────────────────────────────────────────────────────────     │
│  「跨平台問題」的根本原因是不同平台有不同的加速方式                  │
│  → 解法：建立抽象層，自動偵測並使用最佳的加速方式                    │
│                                                                     │
│  原則 5：服務隔離（v2.0 新增）                                       │
│  ──────────────────────────────────────────────────────────────     │
│  「影響其他服務」的根本原因是資源競爭和網路衝突                      │
│  → 解法：獨立網路、資源限制、使用外部 Ollama 服務                    │
│                                                                     │
│  原則 6：優雅降級（v2.0 新增）                                       │
│  ──────────────────────────────────────────────────────────────     │
│  「GPU 故障導致服務中斷」的根本原因是沒有備援機制                    │
│  → 解法：自動偵測裝置可用性，不可用時降級到 CPU 處理                 │
│                                                                     │
│  原則 7：彈性選擇（v2.0 新增）                                       │
│  ──────────────────────────────────────────────────────────────     │
│  「隱私與品質無法兼顧」的根本原因是只有單一處理路徑                  │
│  → 解法：提供本地/雲端雙模式，由使用者依據資料敏感度選擇             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 現況問題診斷

### 📊 現有系統分析

根據現有程式碼的分析，目前系統具備以下能力：

| 組件 | 現況 | 評估 |
|------|------|------|
| **Whisper 轉錄** | 支援 faster-whisper-xxl.exe 和 Python 套件 | ✅ 功能完整 |
| **Ollama LLM** | 本地 LLM 摘要生成 | ✅ 功能完整 |
| **快取機制** | 相同檔案不重複轉錄 | ✅ 已實作 |
| **批次處理** | 支援多檔案處理 | ✅ 已實作 |
| **跨平台** | 支援 Windows/macOS/Linux | ⚠️ 需手動設定環境 |
| **部署方式** | 批次腳本 + Python | ❌ 環境依賴問題嚴重 |
| **使用介面** | 命令列 | ❌ 操作門檻高 |

### 🔴 核心痛點

```
痛點 1：環境設定噩夢
─────────────────────────────────────────────────────────
每台 Windows 電腦都有不同的問題：
• CUDA 版本不相容
• cuDNN 版本不對
• Python 套件衝突
• DLL 找不到
• 權限問題
```

```
痛點 2：重複下載浪費時間
─────────────────────────────────────────────────────────
每次部署到新電腦：
• 下載 Whisper 模型 ≈ 30 分鐘
• 下載 Ollama 模型 ≈ 20 分鐘
• 安裝 Python 套件 ≈ 10 分鐘
總計超過 1 小時，且可能失敗
```

```
痛點 3：操作門檻
─────────────────────────────────────────────────────────
非技術人員無法自行使用：
• 不會開啟命令列
• 看不懂錯誤訊息
• 不知道如何排除問題
```

---

## 解決方案總覽

### 🎨 整體方案架構

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        會議轉錄工具 - Docker 版                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   使用者                                                                 │
│     │                                                                   │
│     │ 開啟瀏覽器 http://localhost:9527                                  │
│     ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                    前端介面 (Web UI)                             │   │
│   │                                                                   │   │
│   │  ┌────────────┐   ┌────────────┐   ┌────────────┐               │   │
│   │  │  上傳檔案   │   │  處理進度   │   │  下載結果   │               │   │
│   │  │  拖放區域   │   │  即時顯示   │   │  會議記錄   │               │   │
│   │  └────────────┘   └────────────┘   └────────────┘               │   │
│   │                                                                   │   │
│   │  🍎 蘋果風格：簡約、優雅、直覺                                    │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              │ REST API                                 │
│                              ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                   後端服務 (FastAPI + Python)                     │   │
│   │                                                                   │   │
│   │  ┌────────────┐   ┌────────────┐   ┌────────────┐               │   │
│   │  │  檔案管理   │   │  任務佇列   │   │  API 端點   │               │   │
│   │  │  上傳/下載  │   │  非同步處理  │   │  健康檢查   │               │   │
│   │  └────────────┘   └────────────┘   └────────────┘               │   │
│   │         │                │                                        │   │
│   │         ▼                ▼                                        │   │
│   │  ┌────────────────────────────────────────────────────────────┐  │   │
│   │  │                    處理管線                                 │  │   │
│   │  │                                                             │  │   │
│   │  │   音訊輸入 → Whisper 轉錄 → LLM 摘要 → Markdown 輸出       │  │   │
│   │  │              (faster-whisper)  (Ollama)                    │  │   │
│   │  └────────────────────────────────────────────────────────────┘  │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              │ HTTP API                                 │
│                              ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                     Ollama 服務                                   │   │
│   │                  (本地 LLM 推理引擎)                               │   │
│   │                                                                   │   │
│   │           gemma3:12b (中文優化，預設模型)                         │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   ════════════════════════════════════════════════════════════════      │
│                         GPU 加速層                                       │
│   ════════════════════════════════════════════════════════════════      │
│                                                                         │
│   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐               │
│   │   Windows    │   │    macOS     │   │    Linux     │               │
│   │   (CUDA)     │   │    (MPS)     │   │   (CUDA)     │               │
│   │  RTX 4090    │   │  M1/M2/M3    │   │  任意 GPU    │               │
│   └──────────────┘   └──────────────┘   └──────────────┘               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 📦 Docker 映像策略

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       Docker 映像打包策略                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  映像 1：主應用服務 (meetingscribe-app)                                 │
│  ─────────────────────────────────────────────────────────────────────  │
│  包含：                                                                 │
│  ├── Python 3.11 執行環境                                               │
│  ├── FastAPI 後端服務                                                   │
│  ├── faster-whisper Python 套件                                         │
│  ├── Whisper large-v3 模型 (預載入)                                     │
│  ├── 前端靜態檔案                                                       │
│  └── CUDA 執行環境 (nvidia/cuda 基礎映像)                               │
│                                                                         │
│  大小估計：約 8-10 GB                                                   │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  ❌ 不包含 Ollama（v2.0 變更）                                           │
│  ─────────────────────────────────────────────────────────────────────  │
│  原因：                                                                 │
│  ├── 主機已安裝 Ollama 並下載 Gemma3:12B 模型                           │
│  ├── 避免資源衝突和重複下載                                             │
│  └── 透過 host.docker.internal 連接主機 Ollama                          │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  🔒 服務隔離策略（首要任務）                                            │
│  ─────────────────────────────────────────────────────────────────────  │
│  ├── 獨立的 Docker 網路 (meetingscribe-isolated-net)                    │
│  ├── 設定資源限制（CPU、記憶體上限）                                    │
│  ├── 不與其他 Docker 服務共享網路                                       │
│  └── 絕對不影響 Docker 中其他運行的服務                                 │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  總計：首次下載約 8-10 GB（不含 Ollama）                                │
│  優勢：下載一次，永久使用                                               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 系統架構設計

### 🏗️ 技術堆疊選型（v2.0 更新）

| 層級 | 技術選型 | 選擇理由 |
|------|----------|----------|
| **前端框架** | 原生 HTML + CSS + JavaScript | 簡單、輕量、無需建構工具 |
| **UI 風格** | Apple Human Interface Guidelines | 簡約、優雅、使用者友善 |
| **後端框架** | FastAPI (Python) | 非同步支援、自動 API 文件、高效能 |
| **語音轉錄** | faster-whisper | CTranslate2 優化、4 倍速提升、低記憶體 |
| **本地 LLM** | Ollama (主機) + Gemma3:12B | 本地執行、機敏資料保護 |
| **雲端 LLM** | Google Gemini API | 高品質摘要、gemini-2.5-flash-lite |
| **容器化** | Docker + Docker Compose | 標準化部署、跨平台支援 |
| **GPU 支援** | NVIDIA Container Toolkit | CUDA 加速、無縫整合 |
| **裝置偵測** | 自研智能偵測模組 | GPU/MPS/CPU 自動選擇與降級 |

### 📁 專案結構（v2.0 更新）

```
convert/
├── 📁 docker/                    # Docker 相關檔案
│   ├── Dockerfile                # 主應用 Dockerfile (CUDA)
│   └── docker-compose.yml        # 服務編排（單服務）
│
├── 📁 frontend/                  # 前端介面
│   ├── index.html                # 主頁面
│   ├── 📁 css/
│   │   └── style.css             # Apple 風格樣式
│   └── 📁 js/
│       └── app.js                # 前端邏輯
│
├── 📁 backend/                   # 後端服務
│   ├── main.py                   # FastAPI 入口
│   ├── 📁 api/
│   │   ├── routes.py             # API 路由
│   │   └── websocket.py          # WebSocket 進度推送
│   ├── 📁 services/
│   │   ├── transcription.py      # Whisper 轉錄服務
│   │   ├── summarization.py      # LLM 摘要服務（本地/雲端）
│   │   ├── gemini_client.py      # Gemini API 客戶端（新增）
│   │   ├── device_detector.py    # 智能裝置偵測（新增）
│   │   └── file_manager.py       # 檔案管理服務
│   ├── 📁 models/
│   │   └── schemas.py            # Pydantic 資料模型
│   └── 📁 core/
│       ├── config.py             # 配置管理
│       └── logger.py             # 日誌系統
│
├── 📁 models/                    # AI 模型 (Volume 掛載)
│   └── whisper-large-v3/         # Whisper 模型
│
├── 📁 data/                      # 資料目錄 (Volume 掛載)
│   ├── uploads/                  # 上傳的檔案
│   ├── outputs/                  # 處理結果
│   └── cache/                    # 快取
│
├── 📁 scripts/                   # 部署腳本（v2.0 強化）
│   ├── setup-api-key.ps1         # API Key 設定腳本（友善版）
│   ├── restart-service.ps1       # 服務重啟腳本（友善版）
│   ├── deploy.ps1                # Windows 部署
│   └── health-check.ps1          # 健康檢查腳本
│
├── 📁 doc/                       # 文件
│   ├── README.md
│   ├── DESIGN.md
│   └── 系統開發及實作規劃.md
│
├── requirements.txt              # Python 依賴
├── config.yaml                   # 應用配置
├── .env.example                  # 環境變數範例
└── .env                          # 實際環境變數（不納入版控）
```

---

## 前端設計規劃

### 🎨 設計理念：Apple 風格

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      Apple 設計原則應用                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. 簡潔性 (Simplicity)                                                 │
│  ──────────────────────────────────────────────────────────────────     │
│  • 移除所有非必要的視覺元素                                             │
│  • 一個頁面只做一件事                                                   │
│  • 不需要說明書就能使用                                                 │
│                                                                         │
│  2. 直覺性 (Intuitiveness)                                              │
│  ──────────────────────────────────────────────────────────────────     │
│  • 拖放上傳（不需要找按鈕）                                             │
│  • 即時反饋（處理進度可視化）                                           │
│  • 結果直接顯示（不需要另外下載）                                       │
│                                                                         │
│  3. 一致性 (Consistency)                                                │
│  ──────────────────────────────────────────────────────────────────     │
│  • 統一的色彩系統                                                       │
│  • 統一的字體層級                                                       │
│  • 統一的互動反饋                                                       │
│                                                                         │
│  4. 美感 (Aesthetics)                                                   │
│  ──────────────────────────────────────────────────────────────────     │
│  • 大量留白，呼吸感                                                     │
│  • 柔和的陰影                                                           │
│  • 流暢的動畫                                                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 🖼️ 頁面佈局設計

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                     MeetingScribe 會議轉錄                       │   │
│   │                                                                   │   │
│   │              將會議錄音轉換為結構化會議記錄                       │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                     選擇處理模式                                  │   │
│   │                                                                   │   │
│   │  ┌─────────────────────────┐  ┌─────────────────────────┐        │   │
│   │  │  🔒 方案 A：本地模式     │  │  ☁️ 方案 B：雲端模式     │        │   │
│   │  │                         │  │                         │        │   │
│   │  │  ✓ 完全離線處理         │  │  ✓ 高品質摘要           │        │   │
│   │  │  ✓ 資料不外傳           │  │  ✓ 處理速度較快         │        │   │
│   │  │  ✓ 適合機敏資料         │  │  ✓ 適合非機敏資料       │        │   │
│   │  │                         │  │                         │        │   │
│   │  │  使用：Gemma3:12B       │  │  使用：Gemini API       │        │   │
│   │  │  (本地 Ollama)          │  │  (gemini-2.5-flash-lite)│        │   │
│   │  └─────────────────────────┘  └─────────────────────────┘        │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                                                                   │   │
│   │                    ┌─────────────────────┐                       │   │
│   │                    │                     │                       │   │
│   │                    │    📁 拖放檔案     │                       │   │
│   │                    │                     │                       │   │
│   │                    │  或點擊選擇檔案    │                       │   │
│   │                    │                     │                       │   │
│   │                    └─────────────────────┘                       │   │
│   │                                                                   │   │
│   │              支援格式：MP3, MP4, WAV, M4A, MKV...                 │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                       處理進度                                   │   │
│   │                                                                   │   │
│   │   ○ 檔案上傳     ● 語音轉錄     ○ 生成摘要     ○ 完成            │   │
│   │                                                                   │   │
│   │   ████████████████████░░░░░░░░░░░░░░░░░░░░  45%                  │   │
│   │                                                                   │   │
│   │   正在轉錄語音...預計剩餘 3 分 20 秒                             │   │
│   │   📍 使用裝置：NVIDIA RTX 4090 (CUDA)                            │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                       處理結果                                   │   │
│   │                                                                   │   │
│   │  ┌───────────────────────────────────────────────────────────┐  │   │
│   │  │                                                           │  │   │
│   │  │  # 會議記錄摘要                                           │  │   │
│   │  │                                                           │  │   │
│   │  │  ## 一、會議概要                                          │  │   │
│   │  │  - 會議主題：專案進度討論                                 │  │   │
│   │  │  - 與會者：王經理、李組長、張工程師                       │  │   │
│   │  │  ...                                                      │  │   │
│   │  │                                                           │  │   │
│   │  └───────────────────────────────────────────────────────────┘  │   │
│   │                                                                   │   │
│   │       [ 複製文字 ]    [ 下載 Markdown ]    [ 重新處理 ]          │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   ───────────────────────────────────────────────────────────────────   │
│                                                                         │
│   🔒 本地模式：完全離線，資料不外傳  │  ☁️ 雲端模式：使用 Gemini API  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 🎨 色彩系統

```css
/* Apple 風格色彩系統 */

/* 淺色模式 */
:root {
  --bg-primary: #ffffff;           /* 主背景 */
  --bg-secondary: #f5f5f7;         /* 次要背景 */
  --text-primary: #1d1d1f;         /* 主要文字 */
  --text-secondary: #86868b;       /* 次要文字 */
  --accent-blue: #0071e3;          /* 主要強調色 */
  --accent-green: #28cd41;         /* 成功狀態 */
  --border: #d2d2d7;               /* 邊框 */
  --shadow: 0 4px 12px rgba(0,0,0,0.08);
}

/* 深色模式 */
@media (prefers-color-scheme: dark) {
  :root {
    --bg-primary: #000000;
    --bg-secondary: #1d1d1f;
    --text-primary: #f5f5f7;
    --text-secondary: #86868b;
    --accent-blue: #2997ff;
    --accent-green: #30d158;
    --border: #424245;
    --shadow: 0 4px 12px rgba(0,0,0,0.4);
  }
}
```

### ✨ 互動設計

| 互動行為 | 設計 | 反饋 |
|---------|------|------|
| **拖放檔案** | 拖放區域高亮 + 虛線邊框動畫 | 「放開以上傳檔案」提示 |
| **上傳進度** | 圓形進度環 | 百分比數字 + 剩餘時間 |
| **處理階段** | 步驟指示器 | 當前步驟高亮 + 脈動動畫 |
| **完成** | 打勾動畫 | 成功音效（可選）|
| **錯誤** | 紅色警示 | 友善錯誤訊息 + 重試按鈕 |

---

## 後端設計規劃

### 🔧 API 端點設計

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          REST API 端點                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  健康檢查                                                                │
│  ─────────────────────────────────────────────────────────────────────  │
│  GET  /api/health                                                       │
│       回傳系統狀態、GPU 資訊、模型載入狀態                              │
│                                                                         │
│  檔案上傳（v2.1 更新）                                                  │
│  ─────────────────────────────────────────────────────────────────────  │
│  POST /api/upload                                                       │
│       上傳音訊/視訊檔案，回傳任務 ID                                    │
│       • 檔案大小限制：預設 100MB（可配置）                              │
│       • 單次上傳：僅允許單檔（批次功能保留但預設關閉）                   │
│       • 自動加入排隊佇列                                                │
│                                                                         │
│  任務管理（v2.1 更新）                                                  │
│  ─────────────────────────────────────────────────────────────────────  │
│  GET  /api/tasks/{task_id}                                              │
│       查詢任務狀態和進度                                                │
│                                                                         │
│  GET  /api/tasks/{task_id}/result                                       │
│       取得處理結果（Markdown）                                          │
│                                                                         │
│  DELETE /api/tasks/{task_id}                                            │
│       取消/刪除任務                                                     │
│                                                                         │
│  排隊系統（v2.1 新增）                                                  │
│  ─────────────────────────────────────────────────────────────────────  │
│  GET  /api/queue/status                                                 │
│       查詢排隊狀態（排隊人數、預估等待時間）                            │
│                                                                         │
│  GET  /api/queue/position/{task_id}                                     │
│       查詢特定任務在佇列中的位置                                        │
│                                                                         │
│  WebSocket 即時更新                                                      │
│  ─────────────────────────────────────────────────────────────────────  │
│  WS   /ws/tasks/{task_id}                                               │
│       即時推送處理進度（含排隊位置更新）                                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 📊 API 資料模型（v2.1 更新）

```python
# 任務狀態（v2.1 新增 QUEUED 狀態）
class TaskStatus(str, Enum):
    QUEUED = "queued"          # 排隊中（v2.1 新增）
    PENDING = "pending"        # 等待處理
    UPLOADING = "uploading"    # 上傳中
    TRANSCRIBING = "transcribing"  # 轉錄中
    SUMMARIZING = "summarizing"    # 生成摘要中
    COMPLETED = "completed"    # 已完成
    FAILED = "failed"          # 失敗

# 任務資訊（v2.1 更新）
class TaskInfo(BaseModel):
    task_id: str
    filename: str
    status: TaskStatus
    progress: float  # 0-100
    stage: str
    queue_position: Optional[int]     # 排隊位置（v2.1 新增）
    estimated_wait: Optional[int]     # 預估等待秒數（v2.1 新增）
    user_prompt: Optional[str]        # 使用者自訂 prompt（v2.1 新增）
    created_at: datetime
    completed_at: Optional[datetime]
    error_message: Optional[str]

# 處理結果
class TranscriptionResult(BaseModel):
    task_id: str
    filename: str
    transcript: str      # 原始逐字稿
    summary: str         # 摘要（Markdown 格式）
    duration_seconds: float
    processing_time: float
    word_count: int
    model_info: dict
    user_prompt_used: Optional[str]   # 使用的 prompt（v2.1 新增）

# WebSocket 進度訊息（v2.1 更新）
class ProgressMessage(BaseModel):
    task_id: str
    status: TaskStatus
    progress: float
    stage: str
    message: str
    eta_seconds: Optional[int]
    queue_position: Optional[int]     # 排隊位置（v2.1 新增）
    queue_total: Optional[int]        # 排隊總數（v2.1 新增）

# 排隊狀態（v2.1 新增）
class QueueStatus(BaseModel):
    total_queued: int                 # 排隊總數
    processing_count: int             # 正在處理的任務數
    estimated_wait_seconds: int       # 預估等待時間
    max_concurrent: int               # 最大同時處理數

# 上傳請求（v2.1 新增）
class UploadRequest(BaseModel):
    user_prompt: Optional[str] = None  # 使用者自訂 prompt
    processing_mode: str = "local"     # local 或 cloud
```

### 🔄 處理管線

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         處理管線流程                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│    1. 檔案上傳                                                          │
│    ─────────────                                                        │
│    • 驗證檔案格式                                                       │
│    • 儲存到 uploads 目錄                                                │
│    • 產生唯一任務 ID                                                    │
│    • 推送進度：0%                                                       │
│                                                                         │
│                    │                                                    │
│                    ▼                                                    │
│                                                                         │
│    2. 快取檢查                                                          │
│    ─────────────                                                        │
│    • 計算檔案 hash                                                      │
│    • 檢查是否有快取的逐字稿                                             │
│    • 如有快取，跳到步驟 4                                               │
│    • 推送進度：5%                                                       │
│                                                                         │
│                    │                                                    │
│                    ▼                                                    │
│                                                                         │
│    3. Whisper 轉錄                                                      │
│    ─────────────                                                        │
│    • 載入 large-v3 模型（如未載入）                                     │
│    • 執行語音辨識                                                       │
│    • 儲存逐字稿到快取                                                   │
│    • 推送進度：10% → 60%                                                │
│                                                                         │
│                    │                                                    │
│                    ▼                                                    │
│                                                                         │
│    4. LLM 摘要生成                                                      │
│    ─────────────                                                        │
│    • 檢查逐字稿長度                                                     │
│    • 若過長，分段處理                                                   │
│    • 呼叫 Ollama API 生成摘要                                           │
│    • 推送進度：60% → 90%                                                │
│                                                                         │
│                    │                                                    │
│                    ▼                                                    │
│                                                                         │
│    5. 結果輸出                                                          │
│    ─────────────                                                        │
│    • 格式化為 Markdown                                                  │
│    • 儲存到 outputs 目錄                                                │
│    • 更新任務狀態                                                       │
│    • 推送進度：100%                                                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Docker 部署架構

### 🐳 Dockerfile 設計（v2.0 - CUDA 版本 + 智能偵測）

```dockerfile
# ============================================
# MeetingScribe - 會議轉錄工具 Docker 映像
# v2.0 - 支援 CUDA GPU 加速 + 智能降級
# ============================================

# 基礎映像：NVIDIA CUDA + cuDNN
FROM nvidia/cuda:12.1-cudnn8-runtime-ubuntu22.04

# 設定環境變數
ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    DEBIAN_FRONTEND=noninteractive \
    TZ=Asia/Taipei

# 安裝系統依賴
RUN apt-get update && apt-get install -y --no-install-recommends \
    python3.11 \
    python3.11-venv \
    python3-pip \
    ffmpeg \
    curl \
    && rm -rf /var/lib/apt/lists/*

# 建立非 root 用戶
RUN useradd -m -u 1000 appuser

# 設定工作目錄
WORKDIR /app

# 複製依賴檔案
COPY requirements.txt .

# 安裝 Python 依賴（包含 CUDA 支援 + Gemini API）
RUN pip3 install --no-cache-dir -r requirements.txt && \
    pip3 install --no-cache-dir \
    nvidia-cublas-cu12 \
    nvidia-cudnn-cu12 \
    google-generativeai \
    openai

# 複製應用程式碼
COPY --chown=appuser:appuser . .

# 預載 Whisper 模型
# 這會在建構時下載模型，打包進映像中
RUN python3 -c "from faster_whisper import WhisperModel; WhisperModel('large-v3')"

# 切換到非 root 用戶
USER appuser

# 暴露端口
EXPOSE 9527

# 健康檢查
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
    CMD curl -f http://localhost:9527/api/health || exit 1

# 啟動命令
CMD ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "9527"]
```

### 📦 Docker Compose 配置（v2.0 - 服務隔離版）

```yaml
# docker-compose.yml
# v2.0 - 服務隔離版（不影響其他 Docker 服務）
version: '3.8'

services:
  # ========================================
  # 主應用服務（唯一服務）
  # ========================================
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: meetingscribe:latest
    container_name: meetingscribe-app
    restart: unless-stopped
    
    # GPU 支援配置（可選，無 GPU 時自動降級到 CPU）
    deploy:
      resources:
        # 資源限制 - 避免影響其他服務
        limits:
          cpus: '8'           # 最多使用 8 核心
          memory: 16G         # 最多使用 16GB 記憶體
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    
    ports:
      - "9527:9527"
    
    environment:
      # ========================================
      # 本地模式設定（Ollama - 連接主機）
      # ========================================
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
      - LOCAL_LLM_MODEL=gemma3:12b
      
      # ========================================
      # 雲端模式設定（Gemini API）
      # ========================================
      - GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/models
      - GEMINI_MODEL=gemini-2.5-flash-lite
      # API Key 透過 .env 檔案注入
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      
      # ========================================
      # Whisper 轉錄設定
      # ========================================
      - WHISPER_MODEL=large-v3
      - WHISPER_DEVICE=auto    # auto = 自動偵測最佳裝置
      
      # ========================================
      # 系統設定
      # ========================================
      - LOG_LEVEL=INFO
      - DEFAULT_MODE=local     # 預設使用本地模式
    
    volumes:
      - ./data/uploads:/app/data/uploads
      - ./data/outputs:/app/data/outputs
      - ./data/cache:/app/data/cache
      - whisper-models:/app/models
    
    # 允許連接主機網路（存取 Ollama）
    extra_hosts:
      - "host.docker.internal:host-gateway"
    
    # 獨立網路 - 不影響其他 Docker 服務
    networks:
      - meetingscribe-isolated-net
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9527/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

# ========================================
# 網路配置 - 獨立隔離網路
# ========================================
networks:
  meetingscribe-isolated-net:
    driver: bridge
    name: meetingscribe-isolated-net
    # 不連接其他網路，確保隔離
    internal: false

# ========================================
# 持久化存儲
# ========================================
volumes:
  whisper-models:
    driver: local

# ⚠️ 注意：不包含 Ollama 服務
# Ollama 使用主機已安裝的版本，透過 host.docker.internal 連接
```

### 🔐 環境變數檔案 (.env.example)

```bash
# ============================================
# MeetingScribe 環境變數配置
# 請複製此檔案為 .env 並填入實際值
# ============================================

# ========================================
# Gemini API 設定（雲端模式使用）
# ========================================
# 取得 API Key: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ========================================
# 進階設定（通常不需要修改）
# ========================================
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# LOCAL_LLM_MODEL=gemma3:12b
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/models
# GEMINI_MODEL=gemini-2.5-flash-lite
# WHISPER_MODEL=large-v3
# WHISPER_DEVICE=auto
# LOG_LEVEL=INFO
# DEFAULT_MODE=local
```

### 🚀 部署腳本

#### Windows PowerShell (deploy.ps1)

```powershell
# ============================================
# MeetingScribe Docker 部署腳本 (Windows)
# ============================================

param(
    [Parameter(Position=0)]
    [string]$Command = "help"
)

$ProjectName = "meetingscribe"

function Write-Info { Write-Host "[INFO] $args" -ForegroundColor Cyan }
function Write-Success { Write-Host "[OK] $args" -ForegroundColor Green }
function Write-Warn { Write-Host "[WARN] $args" -ForegroundColor Yellow }
function Write-Err { Write-Host "[ERROR] $args" -ForegroundColor Red }

# 檢查 Docker
function Test-Docker {
    try {
        docker info | Out-Null
        return $true
    } catch {
        Write-Err "Docker 未運行，請先啟動 Docker Desktop"
        exit 1
    }
}

# 檢查 GPU
function Test-GPU {
    $gpu = docker run --rm --gpus all nvidia/cuda:12.1-base-ubuntu22.04 nvidia-smi 2>$null
    if ($LASTEXITCODE -eq 0) {
        Write-Success "GPU 可用"
        return $true
    } else {
        Write-Warn "GPU 不可用，將使用 CPU 模式"
        return $false
    }
}

# 建構映像
function Invoke-Build {
    Write-Info "建構 Docker 映像..."
    Write-Info "這可能需要 10-20 分鐘（首次建構需下載模型）"
    
    docker compose build --progress=plain
    
    if ($LASTEXITCODE -eq 0) {
        Write-Success "映像建構完成"
    } else {
        Write-Err "映像建構失敗"
        exit 1
    }
}

# 啟動服務
function Invoke-Up {
    Write-Info "啟動服務..."
    docker compose up -d
    
    Write-Info "等待服務啟動..."
    Start-Sleep -Seconds 10
    
    # 檢查並下載 Ollama 模型
    Write-Info "檢查 Ollama 模型..."
    docker exec meetingscribe-ollama ollama pull gemma3:12b
    
    Write-Success "服務已啟動！"
    Write-Host ""
    Write-Host "═══════════════════════════════════════════" -ForegroundColor Green
    Write-Host "  🎉 MeetingScribe 已就緒！" -ForegroundColor Green
    Write-Host "═══════════════════════════════════════════" -ForegroundColor Green
    Write-Host "  📍 網址: http://localhost:9527" -ForegroundColor Cyan
    Write-Host "═══════════════════════════════════════════" -ForegroundColor Green
}

# 停止服務
function Invoke-Down {
    Write-Info "停止服務..."
    docker compose down
    Write-Success "服務已停止"
}

# 查看狀態
function Invoke-Status {
    Write-Info "服務狀態:"
    docker compose ps
}

# 查看日誌
function Invoke-Logs {
    docker compose logs -f --tail 100
}

# 主程式
Test-Docker

switch ($Command.ToLower()) {
    "build" { Invoke-Build }
    "up" { Invoke-Up }
    "down" { Invoke-Down }
    "restart" { Invoke-Down; Invoke-Up }
    "status" { Invoke-Status }
    "logs" { Invoke-Logs }
    default {
        Write-Host ""
        Write-Host "MeetingScribe Docker 部署工具"
        Write-Host ""
        Write-Host "使用方式: .\deploy.ps1 [command]"
        Write-Host ""
        Write-Host "可用命令:"
        Write-Host "  build   - 建構 Docker 映像"
        Write-Host "  up      - 啟動服務"
        Write-Host "  down    - 停止服務"
        Write-Host "  restart - 重啟服務"
        Write-Host "  status  - 查看狀態"
        Write-Host "  logs    - 查看日誌"
        Write-Host ""
        Write-Host "快速開始:"
        Write-Host "  .\deploy.ps1 build"
        Write-Host "  .\deploy.ps1 up"
        Write-Host ""
    }
}
```

---

## 雙模式處理方案（本地/雲端）

### 🔄 處理模式架構

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      雙模式處理架構                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│                         音訊/視訊輸入                                    │
│                              │                                          │
│                              ▼                                          │
│                    ┌─────────────────┐                                  │
│                    │  Whisper 轉錄   │ ← 本地 GPU 加速（固定）          │
│                    │  (large-v3)     │                                  │
│                    └────────┬────────┘                                  │
│                              │                                          │
│                              ▼                                          │
│                         逐字稿文字                                       │
│                              │                                          │
│                    ┌────────┴────────┐                                  │
│                    │   使用者選擇    │                                  │
│                    └────────┬────────┘                                  │
│              ┌──────────────┼──────────────┐                            │
│              ▼              │              ▼                            │
│   ┌─────────────────┐      │    ┌─────────────────┐                    │
│   │ 🔒 方案 A：本地  │      │    │ ☁️ 方案 B：雲端  │                    │
│   │                 │      │    │                 │                    │
│   │  Ollama 服務    │      │    │  Gemini API     │                    │
│   │  (主機安裝)     │      │    │                 │                    │
│   │                 │      │    │  Base URL:      │                    │
│   │  模型：         │      │    │  googleapis.com │                    │
│   │  Gemma3:12B    │      │    │                 │                    │
│   │                 │      │    │  模型：         │                    │
│   │  特點：         │      │    │  gemini-2.5-    │                    │
│   │  ✓ 完全離線     │      │    │  flash-lite     │                    │
│   │  ✓ 資料不外傳   │      │    │                 │                    │
│   │  ✓ 適合機敏資料 │      │    │  特點：         │                    │
│   │                 │      │    │  ✓ 高品質摘要   │                    │
│   │  缺點：         │      │    │  ✓ 速度較快     │                    │
│   │  △ 品質略低     │      │    │                 │                    │
│   │                 │      │    │  缺點：         │                    │
│   │                 │      │    │  △ 需要網路     │                    │
│   │                 │      │    │  △ 資料上傳     │                    │
│   └────────┬────────┘      │    └────────┬────────┘                    │
│              │              │              │                            │
│              └──────────────┴──────────────┘                            │
│                              │                                          │
│                              ▼                                          │
│                    ┌─────────────────┐                                  │
│                    │  Markdown 輸出  │                                  │
│                    └─────────────────┘                                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 📋 模式比較表

| 特性 | 🔒 方案 A：本地模式 | ☁️ 方案 B：雲端模式 |
|------|-------------------|-------------------|
| **LLM 服務** | Ollama (主機安裝) | Gemini API |
| **模型** | Gemma3:12B | gemini-2.5-flash-lite |
| **隱私保護** | ✅ 完全離線 | ⚠️ 逐字稿上傳雲端 |
| **摘要品質** | ⭐⭐⭐⭐ 良好 | ⭐⭐⭐⭐⭐ 優秀 |
| **處理速度** | 中等 | 快速 |
| **網路需求** | ❌ 不需要 | ✅ 需要 |
| **適用場景** | 政府、醫療、法律、商業機密 | 一般會議、教育、公開活動 |
| **成本** | 免費（本地資源） | API 費用 |

### 🔐 Gemini API 安全設定

```python
# backend/services/gemini_client.py

import os
from openai import OpenAI

class GeminiClient:
    """
    Gemini API 客戶端
    使用 OpenAI 相容介面存取 Gemini API
    """
    
    def __init__(self):
        # API Key 從環境變數取得（安全方式）
        api_key = os.environ.get("GEMINI_API_KEY")
        if not api_key:
            raise ValueError("未設定 GEMINI_API_KEY 環境變數")
        
        self.client = OpenAI(
            api_key=api_key,
            base_url="https://generativelanguage.googleapis.com/v1beta/openai/"
        )
        self.model = os.environ.get("GEMINI_MODEL", "gemini-2.5-flash-lite")
    
    def summarize(self, transcript: str, system_prompt: str) -> str:
        """生成會議摘要"""
        response = self.client.chat.completions.create(
            model=self.model,
            messages=[
                {"role": "system", "content": system_prompt},
                {"role": "user", "content": f"請整理以下會議逐字稿：\n\n{transcript}"}
            ]
        )
        return response.choices[0].message.content
```

---

## 智能裝置偵測與降級機制

### 🔍 裝置偵測流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      智能裝置偵測與降級機制                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│                         系統啟動                                         │
│                            │                                            │
│                            ▼                                            │
│                   ┌────────────────┐                                    │
│                   │  偵測 NVIDIA   │                                    │
│                   │  GPU (CUDA)    │                                    │
│                   └───────┬────────┘                                    │
│                           │                                            │
│              ┌────────────┼────────────┐                               │
│              │ 可用       │            │ 不可用                         │
│              ▼            │            ▼                               │
│   ┌─────────────────┐    │    ┌─────────────────┐                      │
│   │ ✅ 使用 CUDA    │    │    │  偵測 Apple     │                      │
│   │                 │    │    │  MPS (Metal)    │                      │
│   │ device="cuda"   │    │    └───────┬─────────┘                      │
│   │ compute="fp16"  │    │            │                                │
│   │                 │    │   ┌────────┼────────┐                       │
│   │ 速度：最快      │    │   │ 可用   │        │ 不可用                │
│   └────────┬────────┘    │   ▼        │        ▼                       │
│            │             │  ┌─────────┴───┐  ┌─────────────────┐       │
│            │             │  │ ✅ 使用 MPS  │  │ ⚠️ 使用 CPU    │       │
│            │             │  │             │  │                 │       │
│            │             │  │ device="mps"│  │ device="cpu"    │       │
│            │             │  │             │  │ compute="int8"  │       │
│            │             │  │ 速度：快    │  │                 │       │
│            │             │  └──────┬──────┘  │ 速度：慢        │       │
│            │             │         │         │ (仍可運作)      │       │
│            │             │         │         └────────┬────────┘       │
│            └─────────────┴─────────┴──────────────────┘                │
│                                    │                                    │
│                                    ▼                                    │
│                         ┌─────────────────┐                            │
│                         │  開始轉錄處理   │                            │
│                         └─────────────────┘                            │
│                                                                         │
│  ═══════════════════════════════════════════════════════════════════   │
│                          執行時動態降級                                  │
│  ═══════════════════════════════════════════════════════════════════   │
│                                                                         │
│  當 GPU 資源不可用（超載、逾時、錯誤）時：                              │
│                                                                         │
│     GPU 處理中 ──→ 偵測到問題 ──→ 自動切換到 CPU ──→ 繼續處理          │
│                                                                         │
│  使用者會收到通知：                                                     │
│  「⚠️ GPU 資源暫時不可用，已自動切換到 CPU 模式繼續處理」              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 💻 裝置偵測程式碼

```python
# backend/services/device_detector.py

import logging
import subprocess
from enum import Enum
from typing import Tuple, Optional

logger = logging.getLogger(__name__)


class DeviceType(Enum):
    CUDA = "cuda"
    MPS = "mps"
    CPU = "cpu"


class DeviceDetector:
    """
    智能裝置偵測器
    自動偵測最佳運算裝置，並支援動態降級
    """
    
    def __init__(self):
        self.current_device: Optional[DeviceType] = None
        self.fallback_count = 0
    
    def detect_best_device(self) -> Tuple[DeviceType, str]:
        """
        偵測並回傳最佳可用裝置
        
        Returns:
            (裝置類型, 計算精度)
        """
        # 優先嘗試 CUDA
        if self._check_cuda():
            self.current_device = DeviceType.CUDA
            logger.info("✅ 偵測到 NVIDIA GPU，使用 CUDA 加速")
            return DeviceType.CUDA, "float16"
        
        # 嘗試 MPS (Apple Silicon)
        if self._check_mps():
            self.current_device = DeviceType.MPS
            logger.info("✅ 偵測到 Apple Silicon，使用 MPS 加速")
            return DeviceType.MPS, "float16"
        
        # 降級到 CPU
        self.current_device = DeviceType.CPU
        logger.warning("⚠️ 無 GPU 可用，使用 CPU 模式（處理速度較慢）")
        return DeviceType.CPU, "int8"
    
    def _check_cuda(self) -> bool:
        """檢查 CUDA 是否可用"""
        try:
            result = subprocess.run(
                ["nvidia-smi", "--query-gpu=memory.free", "--format=csv,noheader,nounits"],
                capture_output=True, text=True, timeout=10
            )
            if result.returncode == 0:
                free_memory = int(result.stdout.strip().split('\n')[0])
                # 至少需要 4GB VRAM
                return free_memory >= 4000
        except Exception as e:
            logger.debug(f"CUDA 偵測失敗：{e}")
        return False
    
    def _check_mps(self) -> bool:
        """檢查 Apple MPS 是否可用"""
        try:
            import torch
            return torch.backends.mps.is_available()
        except Exception:
            return False
    
    def fallback_to_cpu(self) -> Tuple[DeviceType, str]:
        """
        降級到 CPU 模式
        當 GPU 處理失敗時呼叫
        """
        self.fallback_count += 1
        self.current_device = DeviceType.CPU
        logger.warning(f"⚠️ 第 {self.fallback_count} 次降級到 CPU 模式")
        return DeviceType.CPU, "int8"
    
    def get_device_info(self) -> dict:
        """取得目前裝置資訊"""
        return {
            "current_device": self.current_device.value if self.current_device else "unknown",
            "fallback_count": self.fallback_count,
            "gpu_available": self._check_cuda() or self._check_mps()
        }
```

---

## 友善管理腳本設計

### 📝 API Key 設定腳本（非技術人員友善版）

```powershell
# scripts/setup-api-key.ps1
# ============================================
# MeetingScribe - Gemini API Key 設定工具
# 專為非技術人員設計，友善易用
# ============================================

# 設定視窗標題
$Host.UI.RawUI.WindowTitle = "MeetingScribe API Key 設定工具"

# 顯示歡迎訊息
Clear-Host
Write-Host ""
Write-Host "╔═══════════════════════════════════════════════════════════════╗" -ForegroundColor Cyan
Write-Host "║                                                               ║" -ForegroundColor Cyan
Write-Host "║         🔐 MeetingScribe API Key 設定工具                     ║" -ForegroundColor Cyan
Write-Host "║                                                               ║" -ForegroundColor Cyan
Write-Host "║    此工具將協助您設定 Gemini API Key                          ║" -ForegroundColor Cyan
Write-Host "║    設定後即可使用「雲端模式」產生高品質的會議摘要             ║" -ForegroundColor Cyan
Write-Host "║                                                               ║" -ForegroundColor Cyan
Write-Host "╚═══════════════════════════════════════════════════════════════╝" -ForegroundColor Cyan
Write-Host ""

# 檢查是否已有 .env 檔案
$envPath = Join-Path $PSScriptRoot "..\..\.env"
$envExamplePath = Join-Path $PSScriptRoot "..\..\..\.env.example"

if (Test-Path $envPath) {
    Write-Host "📁 找到現有的設定檔" -ForegroundColor Yellow
    $overwrite = Read-Host "   是否要更新 API Key？(Y/N)"
    if ($overwrite -ne "Y" -and $overwrite -ne "y") {
        Write-Host "   已取消操作" -ForegroundColor Gray
        exit 0
    }
}

# 顯示取得 API Key 的說明
Write-Host ""
Write-Host "📋 如何取得 Gemini API Key：" -ForegroundColor White
Write-Host ""
Write-Host "   1. 開啟瀏覽器，前往：" -ForegroundColor Gray
Write-Host "      https://makersuite.google.com/app/apikey" -ForegroundColor Cyan
Write-Host ""
Write-Host "   2. 使用 Google 帳號登入" -ForegroundColor Gray
Write-Host ""
Write-Host "   3. 點擊「Create API Key」建立新的 API Key" -ForegroundColor Gray
Write-Host ""
Write-Host "   4. 複製產生的 API Key" -ForegroundColor Gray
Write-Host ""

# 等待使用者準備好
Write-Host "準備好後，請按 Enter 繼續..." -ForegroundColor Yellow
Read-Host

# 輸入 API Key
Write-Host ""
Write-Host "請貼上您的 Gemini API Key：" -ForegroundColor White
$apiKey = Read-Host

# 驗證 API Key 格式
if ($apiKey.Length -lt 20) {
    Write-Host ""
    Write-Host "❌ API Key 格式不正確，請確認後重試" -ForegroundColor Red
    Write-Host "   API Key 通常以 'AIza' 開頭，長度約 39 個字元" -ForegroundColor Gray
    exit 1
}

# 寫入 .env 檔案
try {
    # 如果有 .env.example，複製一份
    if ((Test-Path $envExamplePath) -and !(Test-Path $envPath)) {
        Copy-Item $envExamplePath $envPath
    }
    
    # 讀取現有內容或建立新檔案
    if (Test-Path $envPath) {
        $content = Get-Content $envPath -Raw
        # 替換或新增 API Key
        if ($content -match "GEMINI_API_KEY=") {
            $content = $content -replace "GEMINI_API_KEY=.*", "GEMINI_API_KEY=$apiKey"
        } else {
            $content += "`nGEMINI_API_KEY=$apiKey"
        }
        $content | Set-Content $envPath -Encoding UTF8
    } else {
        "GEMINI_API_KEY=$apiKey" | Set-Content $envPath -Encoding UTF8
    }
    
    Write-Host ""
    Write-Host "╔═══════════════════════════════════════════════════════════════╗" -ForegroundColor Green
    Write-Host "║                                                               ║" -ForegroundColor Green
    Write-Host "║         ✅ API Key 設定成功！                                 ║" -ForegroundColor Green
    Write-Host "║                                                               ║" -ForegroundColor Green
    Write-Host "║    現在您可以使用「雲端模式」了                               ║" -ForegroundColor Green
    Write-Host "║                                                               ║" -ForegroundColor Green
    Write-Host "╚═══════════════════════════════════════════════════════════════╝" -ForegroundColor Green
    Write-Host ""
    Write-Host "⚠️  請重新啟動服務以套用新設定" -ForegroundColor Yellow
    Write-Host "   執行：.\restart-service.ps1" -ForegroundColor Cyan
    Write-Host ""
    
} catch {
    Write-Host ""
    Write-Host "❌ 設定失敗：$_" -ForegroundColor Red
    exit 1
}

Write-Host "按任意鍵結束..." -ForegroundColor Gray
$null = $Host.UI.RawUI.ReadKey("NoEcho,IncludeKeyDown")
```

### 🔄 服務重啟腳本（安全版，不影響其他服務）

```powershell
# scripts/restart-service.ps1
# ============================================
# MeetingScribe - 服務重啟工具
# 專為非技術人員設計，安全、不影響其他服務
# ============================================

# 設定視窗標題
$Host.UI.RawUI.WindowTitle = "MeetingScribe 服務重啟工具"

# 顯示歡迎訊息
Clear-Host
Write-Host ""
Write-Host "╔═══════════════════════════════════════════════════════════════╗" -ForegroundColor Cyan
Write-Host "║                                                               ║" -ForegroundColor Cyan
Write-Host "║         🔄 MeetingScribe 服務重啟工具                         ║" -ForegroundColor Cyan
Write-Host "║                                                               ║" -ForegroundColor Cyan
Write-Host "║    此工具將安全地重啟 MeetingScribe 服務                      ║" -ForegroundColor Cyan
Write-Host "║    不會影響 Docker 中的其他服務                               ║" -ForegroundColor Cyan
Write-Host "║                                                               ║" -ForegroundColor Cyan
Write-Host "╚═══════════════════════════════════════════════════════════════╝" -ForegroundColor Cyan
Write-Host ""

# 切換到專案目錄
$projectRoot = Split-Path -Parent (Split-Path -Parent $PSScriptRoot)
Set-Location $projectRoot

# 確認重啟
Write-Host "您即將重啟 MeetingScribe 服務" -ForegroundColor Yellow
Write-Host ""
Write-Host "⚠️  注意事項：" -ForegroundColor Yellow
Write-Host "   • 正在處理中的任務會被中斷" -ForegroundColor Gray
Write-Host "   • 已完成的結果不受影響" -ForegroundColor Gray
Write-Host "   • 其他 Docker 服務不受影響" -ForegroundColor Gray
Write-Host ""
$confirm = Read-Host "確定要重啟嗎？(Y/N)"

if ($confirm -ne "Y" -and $confirm -ne "y") {
    Write-Host ""
    Write-Host "已取消操作" -ForegroundColor Gray
    exit 0
}

Write-Host ""
Write-Host "🔄 正在重啟服務..." -ForegroundColor Cyan

# 只重啟 meetingscribe-app 容器，不影響其他服務
try {
    # 檢查容器是否存在
    $containerExists = docker ps -a --filter "name=meetingscribe-app" --format "{{.Names}}" 2>$null
    
    if ($containerExists) {
        # 停止 meetingscribe-app
        Write-Host "   📦 停止 MeetingScribe 服務..." -ForegroundColor Gray
        docker stop meetingscribe-app 2>$null
        
        # 移除容器（保留映像和資料）
        Write-Host "   📦 移除舊容器..." -ForegroundColor Gray
        docker rm meetingscribe-app 2>$null
    }
    
    # 使用 docker compose 重新啟動（只啟動我們的服務）
    Write-Host "   📦 啟動新服務..." -ForegroundColor Gray
    docker compose up -d
    
    # 等待服務啟動
    Write-Host "   ⏳ 等待服務就緒..." -ForegroundColor Gray
    $timeout = 60
    $elapsed = 0
    $ready = $false
    
    while ($elapsed -lt $timeout) {
        Start-Sleep -Seconds 2
        $elapsed += 2
        
        # 檢查健康狀態
        $health = docker inspect --format='{{.State.Health.Status}}' meetingscribe-app 2>$null
        if ($health -eq "healthy") {
            $ready = $true
            break
        }
        
        Write-Host "   ⏳ 已等待 $elapsed 秒..." -ForegroundColor Gray
    }
    
    if ($ready) {
        Write-Host ""
        Write-Host "╔═══════════════════════════════════════════════════════════════╗" -ForegroundColor Green
        Write-Host "║                                                               ║" -ForegroundColor Green
        Write-Host "║         ✅ 服務重啟成功！                                     ║" -ForegroundColor Green
        Write-Host "║                                                               ║" -ForegroundColor Green
        Write-Host "║    網址：http://localhost:9527                                ║" -ForegroundColor Green
        Write-Host "║                                                               ║" -ForegroundColor Green
        Write-Host "╚═══════════════════════════════════════════════════════════════╝" -ForegroundColor Green
    } else {
        Write-Host ""
        Write-Host "⚠️  服務啟動中，請稍後再試" -ForegroundColor Yellow
        Write-Host "   您可以執行 'docker logs meetingscribe-app' 查看詳細日誌" -ForegroundColor Gray
    }
    
} catch {
    Write-Host ""
    Write-Host "❌ 重啟失敗：$_" -ForegroundColor Red
    Write-Host ""
    Write-Host "如果問題持續，請嘗試：" -ForegroundColor Yellow
    Write-Host "   1. 確認 Docker Desktop 已啟動" -ForegroundColor Gray
    Write-Host "   2. 執行 'docker compose logs' 查看錯誤訊息" -ForegroundColor Gray
    exit 1
}

Write-Host ""
Write-Host "按任意鍵結束..." -ForegroundColor Gray
$null = $Host.UI.RawUI.ReadKey("NoEcho,IncludeKeyDown")
```

### 🩺 健康檢查腳本

```powershell
# scripts/health-check.ps1
# ============================================
# MeetingScribe - 系統健康檢查工具
# ============================================

Clear-Host
Write-Host ""
Write-Host "╔═══════════════════════════════════════════════════════════════╗" -ForegroundColor Cyan
Write-Host "║         🩺 MeetingScribe 系統健康檢查                         ║" -ForegroundColor Cyan
Write-Host "╚═══════════════════════════════════════════════════════════════╝" -ForegroundColor Cyan
Write-Host ""

# 檢查 Docker
Write-Host "1. Docker 狀態" -ForegroundColor White
try {
    $dockerInfo = docker info 2>&1
    if ($LASTEXITCODE -eq 0) {
        Write-Host "   ✅ Docker 運行中" -ForegroundColor Green
    } else {
        Write-Host "   ❌ Docker 未啟動" -ForegroundColor Red
    }
} catch {
    Write-Host "   ❌ 無法連接 Docker" -ForegroundColor Red
}

# 檢查 MeetingScribe 容器
Write-Host ""
Write-Host "2. MeetingScribe 服務" -ForegroundColor White
$container = docker ps --filter "name=meetingscribe-app" --format "{{.Status}}" 2>$null
if ($container) {
    $health = docker inspect --format='{{.State.Health.Status}}' meetingscribe-app 2>$null
    if ($health -eq "healthy") {
        Write-Host "   ✅ 服務運行中 (健康)" -ForegroundColor Green
    } else {
        Write-Host "   ⚠️ 服務運行中 (狀態：$health)" -ForegroundColor Yellow
    }
} else {
    Write-Host "   ❌ 服務未啟動" -ForegroundColor Red
}

# 檢查 Ollama（主機）
Write-Host ""
Write-Host "3. Ollama 服務（本地模式）" -ForegroundColor White
try {
    $ollamaResponse = Invoke-RestMethod -Uri "http://localhost:11434/api/tags" -TimeoutSec 5 2>$null
    Write-Host "   ✅ Ollama 運行中" -ForegroundColor Green
    
    # 檢查 Gemma3:12B 模型
    $hasGemma = $ollamaResponse.models | Where-Object { $_.name -like "*gemma3*12b*" }
    if ($hasGemma) {
        Write-Host "   ✅ Gemma3:12B 模型已安裝" -ForegroundColor Green
    } else {
        Write-Host "   ⚠️ Gemma3:12B 模型未安裝" -ForegroundColor Yellow
        Write-Host "      執行：ollama pull gemma3:12b" -ForegroundColor Gray
    }
} catch {
    Write-Host "   ❌ Ollama 未啟動或無法連接" -ForegroundColor Red
}

# 檢查 GPU
Write-Host ""
Write-Host "4. GPU 狀態" -ForegroundColor White
try {
    $nvidiaSmi = nvidia-smi --query-gpu=name,memory.total,memory.free --format=csv,noheader 2>$null
    if ($LASTEXITCODE -eq 0) {
        $gpuInfo = $nvidiaSmi -split ","
        Write-Host "   ✅ GPU: $($gpuInfo[0].Trim())" -ForegroundColor Green
        Write-Host "      記憶體：$($gpuInfo[2].Trim()) MB 可用 / $($gpuInfo[1].Trim()) MB 總計" -ForegroundColor Gray
    }
} catch {
    Write-Host "   ⚠️ 無 NVIDIA GPU 或驅動未安裝" -ForegroundColor Yellow
}

# 檢查 API Key
Write-Host ""
Write-Host "5. Gemini API Key（雲端模式）" -ForegroundColor White
$envPath = Join-Path $PSScriptRoot "..\..\.env"
if (Test-Path $envPath) {
    $envContent = Get-Content $envPath -Raw
    if ($envContent -match "GEMINI_API_KEY=.+") {
        Write-Host "   ✅ API Key 已設定" -ForegroundColor Green
    } else {
        Write-Host "   ⚠️ API Key 未設定" -ForegroundColor Yellow
        Write-Host "      執行：.\setup-api-key.ps1" -ForegroundColor Gray
    }
} else {
    Write-Host "   ⚠️ .env 檔案不存在" -ForegroundColor Yellow
}

Write-Host ""
Write-Host "═══════════════════════════════════════════════════════════════" -ForegroundColor Cyan
Write-Host ""
Write-Host "按任意鍵結束..." -ForegroundColor Gray
$null = $Host.UI.RawUI.ReadKey("NoEcho,IncludeKeyDown")
```

## 實作計劃與里程碑

### 📅 開發時程（v2.0 更新）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       開發時程總覽 (v2.0)                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Phase 1：基礎架構建立                                    預計 2 天      │
│  ─────────────────────────────────────────────────────────────────────  │
│  □ 建立專案結構                                                         │
│  □ 設計 FastAPI 後端骨架                                                │
│  □ 建立 Dockerfile (CUDA + 智能偵測)                                    │
│  □ 建立 docker-compose.yml (服務隔離版)                                 │
│  □ 驗證服務隔離（不影響其他 Docker 服務）                               │
│                                                                         │
│  Phase 2：後端核心功能                                    預計 4 天      │
│  ─────────────────────────────────────────────────────────────────────  │
│  □ 整合 faster-whisper 轉錄服務                                         │
│  □ 實作智能裝置偵測與降級機制                                           │
│  □ 整合 Ollama LLM 摘要服務（本地模式）                                 │
│  □ 整合 Gemini API 摘要服務（雲端模式）                                 │
│  □ 實作檔案上傳 API                                                     │
│  □ 實作任務管理系統                                                     │
│  □ 實作 WebSocket 進度推送                                              │
│                                                                         │
│  Phase 3：前端介面開發                                    預計 3 天      │
│  ─────────────────────────────────────────────────────────────────────  │
│  □ 建立 HTML 頁面結構                                                   │
│  □ 實作 Apple 風格 CSS                                                  │
│  □ 實作模式選擇 UI（本地/雲端）                                         │
│  □ 實作拖放上傳功能                                                     │
│  □ 實作即時進度顯示（含裝置資訊）                                       │
│  □ 實作結果展示和下載                                                   │
│                                                                         │
│  Phase 4：管理腳本開發                                    預計 1 天      │
│  ─────────────────────────────────────────────────────────────────────  │
│  □ 開發 API Key 設定腳本（友善版）                                      │
│  □ 開發服務重啟腳本（安全版）                                           │
│  □ 開發健康檢查腳本                                                     │
│  □ 測試腳本在非技術人員環境下的可用性                                   │
│                                                                         │
│  Phase 5：整合測試與優化                                  預計 2 天      │
│  ─────────────────────────────────────────────────────────────────────  │
│  □ 端對端測試（本地模式 + 雲端模式）                                    │
│  □ GPU/CPU 降級機制測試                                                 │
│  □ 服務隔離驗證（確保不影響其他服務）                                   │
│  □ 錯誤處理完善                                                         │
│  □ 部署腳本測試                                                         │
│                                                                         │
│  Phase 6：文件與發佈                                      預計 1 天      │
│  ─────────────────────────────────────────────────────────────────────  │
│  □ 撰寫使用說明（非技術人員友善版）                                     │
│  □ 建立快速入門指南                                                     │
│  □ 在 Windows 11 + RTX 4090 環境最終驗收                                │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│  總計：約 13 個工作天                                                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### ✅ 驗收標準（v2.0 更新）

| 項目 | 驗收條件 | 優先級 |
|------|----------|--------|
| **服務隔離** | 不影響 Docker 中其他運行的服務 | **P0 首要** |
| **Docker 部署** | `docker compose up` 一鍵啟動成功 | P0 |
| **GPU 支援** | CUDA (RTX 4090) 加速正常運作 | P0 |
| **智能降級** | GPU 不可用時自動切換 CPU | P0 |
| **本地模式** | Ollama + Gemma3:12B 摘要生成正確 | P0 |
| **雲端模式** | Gemini API 摘要生成正確 | P0 |
| **Web 介面** | 瀏覽器可正常訪問並操作 | P0 |
| **模式選擇** | 可切換本地/雲端模式 | P0 |
| **檔案上傳** | 拖放上傳功能正常 | P0 |
| **轉錄功能** | Whisper large-v3 轉錄正確 | P0 |
| **API Key 設定** | 非技術人員可輕鬆設定 | P1 |
| **服務重啟** | 非技術人員可安全重啟 | P1 |
| **進度顯示** | 即時顯示處理進度和裝置資訊 | P1 |
| **結果下載** | 可下載 Markdown 檔案 | P1 |
| **健康檢查** | 可一鍵檢查系統狀態 | P2 |

---

## 風險評估與對策

### ⚠️ 潛在風險（v2.0 更新）

| 風險 | 機率 | 影響 | 對策 |
|------|------|------|------|
| **影響其他 Docker 服務** | 低 | **極高** | 獨立網路、資源限制、嚴格隔離 |
| **Docker 映像過大** | 高 | 中 | 使用多階段建構、精簡依賴 |
| **GPU 驅動不相容** | 中 | 高 | 智能降級機制，自動切換到 CPU |
| **Ollama 服務不可用** | 低 | 中 | 提供雲端模式作為備援 |
| **Gemini API 費用超支** | 低 | 低 | 預設使用本地模式，雲端模式需手動選擇 |
| **記憶體不足** | 中 | 高 | 限制同時處理的任務數、資源上限 |
| **長音訊處理失敗** | 低 | 中 | 實作分段處理和重試機制 |

### 🛡️ 緩解措施

```
風險 0（首要）：影響其他 Docker 服務
─────────────────────────────────────────────────────────
對策：
• 使用獨立的 Docker 網路 (meetingscribe-isolated-net)
• 設定 CPU 和記憶體資源上限
• 不共享任何 Volume 或網路
• 使用 host.docker.internal 連接主機 Ollama

驗證方式：
• 啟動/停止/重啟 MeetingScribe 時，其他服務完全不受影響
• 資源使用不會超過設定上限
```

```
風險 1：Docker 映像過大
─────────────────────────────────────────────────────────
對策：
• 使用多階段建構，分離建構環境和執行環境
• 僅安裝必要的系統套件
• 不包含 Ollama（使用主機已安裝的）

預期效果：映像大小約 8-10GB（含 Whisper 模型）
```

```
風險 2：GPU 驅動不相容或資源不可用
─────────────────────────────────────────────────────────
對策：
• 啟動時自動偵測最佳可用裝置
• 執行時監控 GPU 狀態，異常時自動降級
• 使用者會收到降級通知

預期效果：即使 GPU 完全不可用，服務仍能以 CPU 模式運行
```

```
風險 3：本地 Ollama 服務不可用
─────────────────────────────────────────────────────────
對策：
• 啟動時檢查 Ollama 服務狀態
• 若 Ollama 不可用，提示使用者切換到雲端模式
• 健康檢查腳本可快速診斷問題

預期效果：使用者可選擇雲端模式繼續使用
```

---

## 附錄

### A. 系統需求（目標環境：Windows 11 + RTX 4090）

#### 目標部署環境

| 組件 | 規格 |
|------|------|
| **作業系統** | Windows 11 |
| **GPU** | NVIDIA RTX 4090 (24GB VRAM) |
| **已安裝軟體** | Ollama + Gemma3:12B 模型 |
| **Docker** | Docker Desktop for Windows |

#### 最低需求

| 組件 | 規格 |
|------|------|
| **作業系統** | Windows 10/11, Ubuntu 20.04+, macOS 12+ |
| **CPU** | 4 核心 |
| **RAM** | 16 GB |
| **儲存空間** | 30 GB（含 Docker 映像）|
| **Docker** | Docker Desktop 4.0+ 或 Docker Engine 20.10+ |

#### 建議配置（GPU 加速）

| 組件 | 規格 |
|------|------|
| **GPU** | NVIDIA RTX 3060+ (8GB VRAM) |
| **RAM** | 32 GB |
| **儲存空間** | SSD 50GB+ |
| **NVIDIA Driver** | 525.0+ |

### B. 相關連結

- [Docker Desktop 下載](https://www.docker.com/products/docker-desktop)
- [NVIDIA Container Toolkit 安裝](https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/install-guide.html)
- [faster-whisper 文件](https://github.com/SYSTRAN/faster-whisper)
- [Ollama 官網](https://ollama.ai)
- [Gemini API 文件](https://ai.google.dev/gemini-api)
- [取得 Gemini API Key](https://makersuite.google.com/app/apikey)

### C. 詞彙表（v2.0 更新）

| 術語 | 說明 |
|------|------|
| **Docker** | 容器化技術，讓應用程式和環境打包在一起 |
| **Docker Compose** | Docker 的多容器管理工具 |
| **CUDA** | NVIDIA 的 GPU 平行運算平台 |
| **Whisper** | OpenAI 開發的語音辨識模型 |
| **faster-whisper** | Whisper 的高效能實作版本 |
| **Ollama** | 本地執行 LLM 的工具 |
| **Gemini API** | Google 的雲端 LLM API 服務 |
| **LLM** | 大型語言模型（如 GPT、Gemini、Gemma）|
| **本地模式** | 所有處理都在本機完成，資料不外傳 |
| **雲端模式** | 使用 Gemini API 生成摘要，需要網路 |
| **智能降級** | GPU 不可用時自動切換到 CPU 處理 |
| **服務隔離** | 確保不影響 Docker 中其他運行的服務 |
| **排隊系統** | 任務佇列管理機制（v2.1 新增）|
| **User Prompt** | 使用者自訂 prompt 功能（v2.1 新增）|

### D. 環境變數說明（v2.1 更新）

| 變數名稱 | 說明 | 預設值 |
|----------|------|--------|
| `GEMINI_API_KEY` | Gemini API 金鑰（雲端模式必需） | 無 |
| `GEMINI_BASE_URL` | Gemini API 端點 | `https://generativelanguage.googleapis.com/v1beta/models` |
| `GEMINI_MODEL` | Gemini 模型名稱 | `gemini-2.5-flash-lite` |
| `OLLAMA_BASE_URL` | Ollama 服務端點 | `http://host.docker.internal:11434` |
| `LOCAL_LLM_MODEL` | 本地 LLM 模型名稱 | `gemma3:12b` |
| `WHISPER_MODEL` | Whisper 模型名稱 | `large-v3` |
| `WHISPER_DEVICE` | Whisper 運算裝置 | `auto`（自動偵測）|
| `DEFAULT_MODE` | 預設處理模式 | `local`（本地模式）|
| `LOG_LEVEL` | 日誌等級 | `INFO` |
| `MAX_FILE_SIZE_MB` | 單檔大小上限（MB）| `100`（v2.1 新增）|
| `ENABLE_BATCH_UPLOAD` | 是否允許批次上傳 | `false`（v2.1 新增）|
| `MAX_CONCURRENT_TASKS` | 最大同時處理任務數 | `1`（v2.1 新增）|
| `QUEUE_MAX_SIZE` | 排隊佇列最大長度 | `50`（v2.1 新增）|

---

## 🆕 v2.1 新增功能設計

### 📝 User Prompt 自訂功能

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      User Prompt 設計架構                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  🎯 設計目的                                                            │
│  ─────────────────────────────────────────────────────────────────────  │
│  預設的 System Prompt 無法滿足所有使用者的需求，因此提供                │
│  User Prompt 功能讓使用者自訂會議記錄的產出格式。                       │
│                                                                         │
│  📋 Prompt 層級架構                                                     │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│    ┌─────────────────────────────────────────────────────────────┐     │
│    │                    System Prompt（固定）                    │     │
│    │                                                             │     │
│    │  定義 AI 的角色、基本行為規範                               │     │
│    │  「你是一位專業的會議記錄整理助手...」                     │     │
│    └─────────────────────────────────────────────────────────────┘     │
│                              │                                          │
│                              ▼                                          │
│    ┌─────────────────────────────────────────────────────────────┐     │
│    │               User Prompt（使用者自訂，可選）                │     │
│    │                                                             │     │
│    │  使用者可自行定義輸出格式、重點提取方式等                   │     │
│    │  範例：                                                     │     │
│    │  「請以條列式列出：                                        │     │
│    │    1. 會議決議事項                                          │     │
│    │    2. 各部門待辦事項及負責人                                │     │
│    │    3. 下次會議時間」                                        │     │
│    └─────────────────────────────────────────────────────────────┘     │
│                              │                                          │
│                              ▼                                          │
│    ┌─────────────────────────────────────────────────────────────┐     │
│    │                  逐字稿內容（自動帶入）                      │     │
│    └─────────────────────────────────────────────────────────────┘     │
│                              │                                          │
│                              ▼                                          │
│    ┌─────────────────────────────────────────────────────────────┐     │
│    │                    LLM 生成會議記錄                          │     │
│    └─────────────────────────────────────────────────────────────┘     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 🔧 User Prompt 前端 UI 設計

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                   📝 自訂會議記錄格式（選填）                    │   │
│   │                                                                   │   │
│   │  ┌───────────────────────────────────────────────────────────┐  │   │
│   │  │                                                           │  │   │
│   │  │  請輸入您希望的會議記錄格式或特殊要求...                  │  │   │
│   │  │                                                           │  │   │
│   │  │  範例：                                                   │  │   │
│   │  │  • 請列出所有決議事項和負責人                             │  │   │
│   │  │  • 請用表格呈現待辦事項                                   │  │   │
│   │  │  • 請特別標註預算相關討論                                 │  │   │
│   │  │                                                           │  │   │
│   │  └───────────────────────────────────────────────────────────┘  │   │
│   │                                                                   │   │
│   │  💡 留空則使用預設格式                                           │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 📊 排隊系統設計

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        排隊系統架構設計                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  🎯 設計目的                                                            │
│  ─────────────────────────────────────────────────────────────────────  │
│  避免多位使用者同時上傳時資源被大量消耗，                               │
│  透過排隊機制確保系統穩定運作。                                         │
│                                                                         │
│  📋 排隊流程                                                            │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│    使用者上傳檔案                                                       │
│         │                                                               │
│         ▼                                                               │
│    ┌─────────────┐    否    ┌─────────────────────────────┐            │
│    │ 檔案大小    │──────────→│ 拒絕上傳，顯示錯誤訊息      │            │
│    │ ≤ 100MB?   │          │ 「檔案大小超過 100MB 限制」 │            │
│    └──────┬──────┘          └─────────────────────────────┘            │
│           │ 是                                                          │
│           ▼                                                             │
│    ┌─────────────┐    否    ┌─────────────────────────────┐            │
│    │ 佇列未滿?   │──────────→│ 拒絕上傳，顯示排隊已滿      │            │
│    │ < 50 任務   │          │ 「系統繁忙，請稍後再試」    │            │
│    └──────┬──────┘          └─────────────────────────────┘            │
│           │ 是                                                          │
│           ▼                                                             │
│    ┌─────────────────────────────────────────────────────┐             │
│    │              加入排隊佇列                            │             │
│    │                                                     │             │
│    │  任務狀態：QUEUED                                   │             │
│    │  排隊位置：N                                        │             │
│    │  預估等待：X 分鐘                                   │             │
│    └──────────────────────┬──────────────────────────────┘             │
│                           │                                             │
│                           ▼                                             │
│    ┌─────────────────────────────────────────────────────┐             │
│    │              排隊佇列（FIFO）                        │             │
│    │                                                     │             │
│    │  ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐                     │             │
│    │  │ 1 │→│ 2 │→│ 3 │→│ 4 │→│ N │                     │             │
│    │  └───┘ └───┘ └───┘ └───┘ └───┘                     │             │
│    │    ↑                                               │             │
│    │    │ 正在處理                                       │             │
│    └──────────────────────┬──────────────────────────────┘             │
│                           │                                             │
│                           ▼                                             │
│    ┌─────────────────────────────────────────────────────┐             │
│    │              處理引擎（單執行緒）                    │             │
│    │                                                     │             │
│    │  MAX_CONCURRENT_TASKS = 1（預設）                   │             │
│    │                                                     │             │
│    │  1. Whisper 轉錄                                    │             │
│    │  2. LLM 摘要生成                                    │             │
│    │  3. 結果輸出                                        │             │
│    └─────────────────────────────────────────────────────┘             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 🖥️ 排隊狀態前端顯示

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                       排隊狀態                                   │   │
│   │                                                                   │   │
│   │  📊 系統狀態：                                                    │   │
│   │  ├── 目前排隊人數：3 人                                          │   │
│   │  ├── 正在處理：1 個任務                                          │   │
│   │  └── 預估等待時間：約 8 分鐘                                     │   │
│   │                                                                   │   │
│   │  ─────────────────────────────────────────────────────────────   │   │
│   │                                                                   │   │
│   │  📍 您的任務：                                                    │   │
│   │  ├── 排隊位置：第 2 位                                           │   │
│   │  ├── 狀態：排隊中 ⏳                                             │   │
│   │  └── 預估開始處理時間：約 4 分鐘後                               │   │
│   │                                                                   │   │
│   │  ████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░  排隊中                │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### ⚙️ 參數化配置設計

```python
# backend/core/config.py

from pydantic_settings import BaseSettings
from typing import Optional

class Settings(BaseSettings):
    """
    系統配置 - 所有參數都可透過環境變數覆蓋
    """
    
    # ========================================
    # 檔案上傳限制（v2.1 新增）
    # ========================================
    MAX_FILE_SIZE_MB: int = 100           # 單檔大小上限（MB）
    ENABLE_BATCH_UPLOAD: bool = False     # 是否允許批次上傳
    ALLOWED_EXTENSIONS: str = ".mp3,.mp4,.wav,.m4a,.mkv,.webm,.ogg,.flac"
    
    # ========================================
    # 排隊系統設定（v2.1 新增）
    # ========================================
    MAX_CONCURRENT_TASKS: int = 1         # 最大同時處理任務數
    QUEUE_MAX_SIZE: int = 50              # 排隊佇列最大長度
    TASK_TIMEOUT_SECONDS: int = 3600      # 單一任務超時時間（秒）
    ESTIMATED_MINUTES_PER_TASK: int = 4   # 預估每個任務處理時間（分鐘）
    
    # ========================================
    # LLM 設定
    # ========================================
    OLLAMA_BASE_URL: str = "http://host.docker.internal:11434"
    LOCAL_LLM_MODEL: str = "gemma3:12b"
    GEMINI_API_KEY: Optional[str] = None
    GEMINI_BASE_URL: str = "https://generativelanguage.googleapis.com/v1beta/models"
    GEMINI_MODEL: str = "gemini-2.5-flash-lite"
    DEFAULT_MODE: str = "local"
    
    # ========================================
    # Whisper 設定
    # ========================================
    WHISPER_MODEL: str = "large-v3"
    WHISPER_DEVICE: str = "auto"
    
    # ========================================
    # 系統設定
    # ========================================
    LOG_LEVEL: str = "INFO"
    
    class Config:
        env_file = ".env"
        env_file_encoding = "utf-8"

settings = Settings()
```

---

## 📋 下一步行動

當您審核通過此規劃文件後，我將依照以下順序執行開發：

1. **建立專案結構** - 建立目錄和基礎檔案
2. **實作 Docker 配置** - Dockerfile 和 docker-compose.yml（服務隔離版）
3. **開發後端服務** - FastAPI + Whisper + 雙模式 LLM（本地/雲端）
4. **實作智能降級** - 裝置偵測與自動降級機制
5. **實作排隊系統** - 任務佇列、檔案大小限制（v2.1 新增）
6. **實作 User Prompt 功能** - 自訂會議記錄格式（v2.1 新增）
7. **開發前端介面** - Apple 風格 Web UI + 模式選擇 + 排隊狀態顯示
8. **開發管理腳本** - API Key 設定、服務重啟、健康檢查
9. **整合測試** - 確保所有功能正常運作
10. **在 Windows 11 + RTX 4090 環境最終驗收**

**請審閱此文件，如有任何疑問或需要調整的地方，請告知。**

---

## 📌 v2.1 關鍵變更摘要

| 項目 | v2.0 | v2.1 |
|------|------|------|
| **Ollama 部署** | 使用主機已安裝的 | 維持 |
| **LLM 模式** | 本地 + 雲端雙模式 | 維持 |
| **User Prompt** | 無 | ✅ 使用者可自訂 prompt |
| **排隊系統** | 無 | ✅ FIFO 任務佇列 |
| **批次上傳** | 支援 | ❌ 預設關閉（可配置開啟）|
| **檔案大小限制** | 無 | ✅ 預設 100MB（可配置）|
| **排隊顯示** | 無 | ✅ 前端即時顯示排隊狀態 |
| **參數化配置** | 基本 | ✅ 完整參數化設計 |

---

> 📝 **文件狀態**: v2.1.3 開發完成  
> 👤 **作者**: AI System Architect  
> 📅 **建立日期**: 2025-11-29  
> 📅 **最後更新**: 2025-11-29  
> 🎯 **目標**: 一包帶走，直接 Docker 部署  
> 🖥️ **目標環境**: Windows 11 + RTX 4090 + Ollama (Gemma3:12B)  
> ✅ **開發狀態**: 完成，已進入部署測試階段
