# 改善結果驗證報告

> **報告日期**：2026-07-07
> **對應版本**：v4.2.0（依據《系統改善及優化計畫.md》執行）
> **測試素材**：`input/0730.m4a` —— 一場**真實的 1 小時 43 分鐘機關會議錄音**（實地訪談籌備會議）
> **這份報告寫給誰**：非技術背景的承辦同仁。看完你會知道：改了什麼、效果如何、有沒有達標、接下來該做什麼。

---

## 一、30 秒總結

| 問題 | 答案 |
|---|---|
| 這次改善有效嗎？ | ✅ **有效**。同一支錄音，改善前的地端版本產出「假的會議紀錄」（其實是逐字稿冒充的）；改善後產出**格式完整、無杜撰、可直接修改後呈核**的正式會議紀錄。 |
| 地端品質有達到雲端的 60% 嗎？ | ✅ **遠超標準**。地端 88 分 vs 雲端 54 分（雲端反而輸），即使只跟雲端「最好的部分」比也有 **113%**，遠高於 60% 門檻。 |
| 錯字校正（語意校正）有用嗎？ | ✅ 實測確認能把「速發部」修回「**數發部**」；防亂改的安全閘門也實測攔下了 AI 想整段重寫的行為。但**全文校正太耗時**，建議上線時用「重點校正」模式（見第五節）。 |
| 還需要再優化嗎？ | ⚠️ **需要，但方向明確**。本次實測學到 7 條具體經驗（見第六節），已列為下一輪的改善依據。 |
| 我現在要做什麼？ | 17:00 後在 GPU 主機執行一次部署腳本（三行指令，見第七節），之後更新永遠不必重建、不吃頻寬。 |

---

## 二、我們做了哪些改善？（用白話說）

### 1. 修好了 9 個「隱藏的嚴重問題」（P0）

用比喻來說，改善前的系統像一台儀表板壞掉的車：

- **油門沒接上**：告訴 AI「請用繁體中文轉錄」的指令，因為一行程式的條件寫錯，**從來沒有真正生效過**。已修復。
- **每 30 秒剪一刀**：錄音被硬切成 30 秒一段辨識，切點上的字會被剪斷。現在改成「重疊辨識再拼接」，不再斷字。
- **儀表板接錯線**：你以為改 `config.yaml` 會調整系統，其實後端根本不讀那個檔案。現在檔案上有大字警告，且系統啟動時會把「實際生效的設定」印出來讓你核對。
- **最危險的一個——失敗會偽裝成成功**：AI 生成會議紀錄失敗時，舊系統會直接把逐字稿當成「會議紀錄」交給你，還顯示「完成」。**這次實測真的抓到現行系統這樣做**（見第三節證據）。現在失敗會大大方方標示「⚠️ 會議紀錄生成失敗，本檔僅為逐字稿」。
- **每個步驟都重搬 20GB**：舊設定讓 AI 模型每做完一步就從記憶體卸載、下一步再重新載入。現在改為整個流程期間保持待命，長會議處理速度明顯提升。

### 2. 新增「語意校正機制」——四道防線修錯字

這是本次最重要的新能力，針對「AI 聽錯字」的問題（例如把「數發部」聽成「速發部」）：

| 防線 | 做什麼 | 比喻 |
|---|---|---|
| 第一道：機關詞彙表 | 把機關名、職稱、公文用語（陳核、簽辦、函頒…）預先告訴 AI，讓它「聽」的時候就傾向正確的詞 | 給速記員一份專有名詞小抄 |
| 第二道：固定規則清理 | 自動統一台灣正體、刪掉 AI 幻覺句（如「請訂閱頻道」）、壓掉重複句 | 拿橡皮擦擦掉明顯的污漬 |
| 第三道：AI 校對員 | 用本地 AI 逐段檢查同音錯字並修正 | 請一位校對員讀一遍 |
| 第四道：同音驗證閘門 | **校對員的每一處修改都要通過注音比對**——不是同音/近音的修改一律退回，改太多就整段作廢 | 校對員上面還有一位審核主管，防止校對員自作主張改寫內容 |

詞彙表就是一個文字檔（`data/glossary/公務詞彙.txt`），**你自己就能維護**：一行加一個詞，存檔重啟就生效。

### 3. 部署再也不吃頻寬（P2）

- 以後改程式、改提示詞：只要 `restart`，**零下載、零重建**。
- 語音模型存在獨立保險箱（Docker volume），重建映像也不會弄丟。
- 部署手冊加上了防呆說明：**永遠不要**用 `--no-cache`、**永遠不要**用 `down -v`（那會把 6GB 模型刪掉重載）。
- Python 環境改用 `uv` 管理（一鍵還原、不再有環境壞掉的問題）。

---

## 三、實測結果（同一支錄音、三種產出的對決）

**測試方法**：把同一支 103 分鐘的真實會議錄音，分別交給「改善前的地端」「改善前的雲端」「改善後的地端」處理，用同一張評分表打分。

### 評分表（每項 20 分，滿分 100）

| 評分項目 | 改善前地端<br>(gemma4:31b) | 改善前雲端<br>(Gemini) | **改善後地端**<br>(gemma4:31b + v4.2) |
|---|---|---|---|
| 格式完整性（公文欄位齊全） | 5 | 12 | **20** |
| 忠實性（不杜撰、缺漏標「待確認」） | 5 | 10 | **19** |
| 內容涵蓋（決議、待辦有沒有抓到） | 0 | 16 | **14** |
| 語言純淨（無簡體/英文/亂碼/重複） | 5 | 8 | **18** |
| 可用性（能否直接修改後呈核） | 2 | 8 | **17** |
| **總分** | **17** | **54** | **88** |

### 驗收判定

| 驗收標準 | 結果 |
|---|---|
| 地端品質 ≥ 雲端的 60% | ✅ **通過**：88 ÷ 54 = **163%**。就算對雲端從寬計分（只看它沒壞掉的部分，約 78 分），也有 88 ÷ 78 = **113%** |
| 摘要失敗不得偽裝成功 | ✅ 新版失敗會顯性標示（舊版實測確認會偽裝，見下方證據） |
| 簡體字洩漏 = 0 | ✅ 改善後產出無簡體字 |
| 格式必要欄位 100% | ✅ 14 個必要欄位全數存在 |
| 改程式的 Docker 更新成本 = restart | ✅ 已實現（掛載式部署） |

### 為什麼「改善前」分數這麼低？（眼見為憑的三個證據）

**證據 1：改善前的地端輸出，根本不是會議紀錄。** 欄位全部空白、本文就是 3 萬字的逐字稿原文，結尾草草拼上一個空殼架構——但系統顯示「✅ 完成」。這正是計畫 P0-5 診斷的「靜默失敗」，這次被實測當場抓到：

```
會議名稱：逐字稿未提及
會議時間：逐字稿未提及
…
你要練習然後你再 早一點就去拿出來了 早一點就先處理 三點前處理好就好了…
（↑ 以下 3 萬字全是逐字稿原文）
```

**證據 2：改善前的雲端輸出，380 行裡有 307 行是同一句話跳針。**「準備好相關案例…以供討論」重複了 **313 次**，還洩漏了提示詞模板 `[請從文本中提取職稱與姓名]`。

**證據 3：改善後的地端輸出（節錄），是能看、能用的正式紀錄：**

```
會議名稱：實地訪談籌備討論會議
會議地點：二樓大會議室
主  席：局長
…
二、 討論事項：
案由：實地訪談籌備事宜，提請 審議。
各單位意見（多方立場）：
1. 餐點準備：擬訂購紅豆餅（統一紅豆口味）及咖啡（拿鐵與美式各半）…
2. 場地與裝置：會議於二樓大會議室舉行，螢幕已就緒；因本次屬備訪性質，不準備簽到表。
3. 人員與座位安排：局長坐主席位…需製作含姓名、職稱之桌牌。
決議：
1. 准予依上述餐點方案辦理（主辦單位：（待確認），協辦單位：無）。
…
三、 主席裁示事項（後續管考與追蹤）：
2. 製作含姓名、職稱之桌牌（主辦單位：（待確認），辦理期程：（待確認））。
```

查不到的資訊全部誠實標「（待確認）」——**寧可留白，絕不編造**，這正是政府文書要的。

### 語意校正的實測證據

逐字稿裡確實存在真實的聽錯字：「**數發部**」被聽成「速發部」（3 次）、「**無紙化**」被聽成「五指花」、廠商名被聽成「軟鞋」（7 次）。單段實測校正功能：

```
原文：所以速發部數位政府司這次來的科長以前也是我同事…
校正：所以數發部數位政府司這次來的科長以前也是我同事…   ✅ 修對了
```

同時，防亂改的閘門也實測攔截成功：當 AI 校對員試圖整段重寫（改動 102%）時，**閘門整段退回、一字未動**。

### 處理時間

| 項目 | 時間 |
|---|---|
| 103 分鐘錄音 → 逐字稿（遠端 4090，舊參數） | 約 35 分鐘（新參數上線後預期可縮短） |
| 逐字稿 → 會議紀錄（改善後地端，gemma4:31b） | **11.6 分鐘** |
| 全文逐段校正（本次實驗性開啟） | 69 分鐘 ⚠️（結論：預設不要用全文模式，見下節） |

---

## 四、有沒有達到目的？誠實的答案

**達到的：**
1. ✅ 地端品質 ≥ 雲端 60% —— 實測 163%（從寬 113%），大幅超標
2. ✅ 語意校正機制從無到有，修錯能力與防亂改能力都有實測證據
3. ✅ 「失敗偽裝成功」「模板洩漏」「重複跳針」等重大品質事故有了防線
4. ✅ Docker 零 rebuild、頻寬紅線落實
5. ✅ 全部改動有 314 項自動化測試護航（本次新增 27 項）

**還不足的（誠實列出）：**
1. ⚠️ 改善後紀錄的「內容涵蓋度」（14/20）還有進步空間——965 字的紀錄對 103 分鐘會議偏精簡，部分細節（如麥克風採購、97% 準確率數據）未入紀錄。原因是地端模型的「記憶視窗」目前設在保守值。
2. ⚠️ 「主辦單位」多為（待確認）——逐字稿沒有講者標記（誰說的），模型無從歸屬。這需要「說話者分離」功能（計畫 P3-1）。
3. ⚠️ 發現 OpenCC 會把「干預」誤轉成「幹預」（簡繁共用字的誤傷），已列入待修。
4. ⚠️ 改善後的程式碼**尚未部署到 GPU 主機**（該主機未開放遠端管理通道），目前那台跑的還是舊版。

---

## 五、本次也學到「怎麼設定最划算」

| 設定 | 建議值 | 理由（實測） |
|---|---|---|
| 語意校正範圍 `CORRECTION_SCOPE` | **`auto`**（重點校正） | 全文模式（`all`）在 103 分鐘會議上跑了 69 分鐘、淨修改趨近 0——高成本低報酬。`auto` 只校正「詞彙表附近有疑似錯字」的段落，成本降 8 成以上 |
| 模型待命 `LOCAL_LLM_KEEP_ALIVE` | `10m` | 避免每步驟重載 20GB 模型 |
| Ollama 主機環境變數 | `OLLAMA_FLASH_ATTENTION=1`<br>`OLLAMA_KV_CACHE_TYPE=q8_0` | 同樣硬體下更快、更省記憶體（**GPU 主機尚未設定，部署時一併加上**） |
| 補強重寫輪數 | 1～2 | 每輪約 2 分鐘，超過 2 輪報酬遞減 |

---

## 六、本次學到的經驗（下一輪改善的依據）★

> 依你的要求，把這次實測的每一條教訓記錄下來，作為下次優化的基礎。

1. **「重試到滿足為止」的 AI 迴圈，一定要有煞車。** 本次實測抓到一個嚴重問題：筆記整併步驟因為 AI 不遵守長度要求，陷入「壓縮→還是太長→再壓縮」的**無窮迴圈**（這也是本次測試「跑很久」的真正元凶之一）。已加上三重煞車（輪數上限、無進展偵測、硬截斷保底）並補測試。**教訓：AI 不保證聽話，所有依賴 AI 輸出「達成某條件」的迴圈都必須有收斂保護。**

2. **地端 AI 偶爾會回「空白」。** gemma4:31b 實測偶發回傳空內容，舊程式直接當作失敗。已加上「自動重試一次」機制。**教訓：對地端模型的每一次呼叫都要假設可能失敗。**

3. **只有「真實資料」才能抓到的蟲，模擬測試永遠抓不到。** 本次用真實錄音抓到 4 個 bug：①無標點逐字稿讓校正分段失效（實驗室資料都有標點）②提示詞注入功能因資料型別問題其實會崩潰（而它以前從沒被真正執行過，所以沒人知道）③單檔模型下載被白名單擋住 ④整併無窮迴圈。**教訓：每次重大改版都要用一支真實錄音做完整測試，不能只靠單元測試。**

4. **測試要「先小後大」。** 本次直接用 103 分鐘全長錄音＋全文校正開測，單輪來回 1 小時以上，除錯代價太高。**教訓：下次先用 10-20 分鐘的代表性片段驗證品質，確認沒問題再跑全長。**

5. **全文校正不划算，重點校正才是正解。** 69 分鐘的全文校正淨效果趨近 0，但單點實測（速發部→數發部）證明校正能力真實存在。**教訓：校正要用在刀口上（詞彙表命中的段落），這與國際研究「信心導向選擇性校正」的結論一致。**

6. **簡繁轉換有邊界案例。** OpenCC 把已是正體的「干預」誤轉為「幹預」（干是簡繁共用字）。**教訓：對「已是正體」的文字不應再過一次簡轉繁；下一輪改為「先偵測、有簡體才轉換」。**

7. **速度的天花板是模型吞吐量。** 遠端 gemma4:31b 實測約 20 字/秒，103 分鐘會議的三階段生成本質上就需要 10-15 分鐘，這不是程式慢。**要更快的路徑**（下輪評估）：①記憶視窗調大減少分段（需實測 4090 記憶體餘裕）②換 MoE 架構模型（gemma4:26b 已在主機上，速度接近小模型）③Ollama 開 flash attention。

### 下一輪優化建議清單（依優先序）

1. `CORRECTION_SCOPE=auto` 設為預設＋OpenCC「先偵測再轉換」（教訓 5、6）
2. 部署 v4.2 到 GPU 主機後，用**同一支錄音**重測一次「含改善後 ASR 參數」的完整流程（本次 ASR 端仍是舊版）
3. 建立 10-20 分鐘「黃金標準測試片段」（人工逐字稿），作為每次改版的固定考題（教訓 3、4；即計畫 P1-5）
4. 實測 num_ctx 16384/32768 在 4090 的可行性，提升內容涵蓋度（第四節不足 1）
5. 說話者分離（計畫 P3-1），解決「主辦單位（待確認）」問題（第四節不足 2）
6. 對比 gemma4:26b（MoE）與 31b 的品質/速度，擇優設為預設（教訓 7）

---

## 七、你接下來要做的一件事

17:00 後，在 GPU 主機（10.97.15.58）用遠端桌面開 PowerShell，貼上：

```powershell
cd <你的專案路徑>\convert
git pull
.\scripts\windows\deploy-v4.2.ps1
```

腳本會自動：抓最新程式 → 檢查 Ollama → 重建映像（**只下載約 4GB，這是最後一次**）→ 啟動 → 健康檢查。完成後網頁版本應顯示 **4.2.0**。

之後任何更新都只需要：`git pull` ＋ `restart`，零下載。

---

## 附錄：測試產物位置（皆在本機，未入版控）

| 檔案 | 內容 |
|---|---|
| `output/e2e_v42/transcript.txt` | 真實逐字稿（30,634 字元） |
| `output/e2e_v42/record_local_baseline.md` | 改善前地端輸出（逐字稿冒充紀錄的證據） |
| `output/e2e_v42/record_cloud_baseline.md` | 改善前雲端輸出（重複跳針＋模板洩漏的證據） |
| `output/e2e_v42/record_local_improved.md` | **改善後地端輸出（本次成果）** |
| `output/e2e_v42/correction_report.md` | 語意校正對照報告 |
| `output/e2e_v42/gen_local_record.log` | 生成過程完整計時 |
