# ============================================
# 語意校正機制依賴（v4.2 新增）
#
# 獨立於 requirements.txt 之外，讓 Docker 的 pip 大層快取不因
# 新增小套件而失效（避免 rebuild 重新下載 torch 等數 GB 套件）。
# 兩個套件合計約 3MB。
# ============================================

# OpenCC 簡體→台灣正體轉換（純 Python 實作，無需編譯）
opencc-python-reimplemented>=0.1.7

# 注音/拼音比對（同音驗證閘門）
pypinyin>=0.53.0
